# System 1 implementation evidence

Tracks the [implementation plan](superpowers/plans/2026-09-06-system-one-redesign.md). The full redesign remains in progress. A passing stone fixture does not establish completion of the iron-pickaxe or game-completion gates.

## Implemented so far

- `TaskKernel`: immutable inputs/results and bounded transitions; child tasks retain parent continuations; commands have session/run/task/attempt identity; replacement work waits for acknowledged release. Synthetic-domain tests cover repair/resumption, stale feedback, cancellation, alternate-method recovery, budgets, and replay.
- Opt-in replacement entrance: `scripts/run-evaluation-scenarios --system-one` implies no-LLM mode. Its tick path bypasses the legacy execution/reflex loop. Structured inventory goals enter reactive production tasks.
- Geometric perception: bounded rays stop at the first occupied voxel, do not cross occluding corners, hide unlit identities, and restrict dark geometry to the near field. This is a geometric sensor, not screenshot recognition or proof of human-equivalent visibility.
- Baritone terrain boundary: block-state interfaces capture immutable observed terrain; hidden live/cached cells are replaced by a non-traversable, non-supporting sentinel. Execution-time direct reads also use observations. Navigation cannot issue attack/use input, perform mining/placement preparation, or select buckets. Scoped settings disable independent avoidance, inventory/tool selection, parkour, sprint, and unloaded-goal simplification. See the [source audit and live probe](system-one-baritone-boundary.md); paired autonomous playthroughs and broader terrain variants remain required.
- Exact breaking aims at and ray-checks the target face, verifies reach/tool eligibility, and avoids breaking the player's own footing. Soil excavation prioritizes lower exposed surfaces and preserves tools when an empty hand is equally effective.
- Excavation surveys on demand rather than performing four quarter-turns after every move. This correction follows the user's live observation of repeated spins.
- Decision capture records an initial mission plus every tick's observation delta, motor feedback, cancellation, effects, and lifecycle events. A bounded background writer reports overflow/storage failure as incomplete; evaluator cleanup waits for motor release and writer completion. Offline replay rejects missing turns, incompatible versions, altered decisions, and missing/incomplete footers.

- Production tasks load crafting patterns from the active recipe catalog, preserve parent ingredient reservations, account for recipe yields, and terminate prerequisite cycles. Observed resource sources affect recipe choice. Child tasks harvest logs, bootstrap tools, acquire/place tables, and return toward remembered workstations. Crafting, exact placement, and inventory selection stay under the motor owner; crafting release drains the cursor and input grid. Production recordings include the complete immutable recipe/prior catalog and support offline replay.

Current checkpoint (2026-09-08, policy v45): 133 System 1 and 35 evaluator tests pass. The fresh distant-approach fixture passes live with exact replay. Under v43, descent, lighting exhaustion, and tool replacement pass together. The original iron-pickaxe scenario has one older v24 pass; the latest completed original run (v44) obtained three raw iron but failed its furnace interaction. Targeted success does not establish original-scenario reliability.

The generic lifecycle, fair observation/motor boundary, production, exploration, lighting repair, tool replacement, local access preparation, and limited lava/death recovery have working implementations and specific evidence below. Maintained conditions across every task type, broader survival, injected feedback failures, visual recording validation, held-out evaluation, cutover/deletion, and multi-seed autonomous game completion remain unfinished. The opt-in replacement is still a temporary migration entrance.

## Tests and live evidence

Focused command:

```bash
source .envrc
./gradlew :test --tests 'ai.moeru.airicraft.systemone.*'
```

Initial 2026-09-07 checkpoint (historical): 56 tests passed: 11 kernel, 6 geometric sensing, 13 acquisition and 1 observed-reach, 9 production, 6 decision replay, 3 recording tests, and 7 smelting-production tests. The 32 evaluator tests and evaluation-launcher Python suite also pass (24 launcher tests). Five recording-inspector tests cover old-policy diagnosis and incomplete input detection.

Live fixture command:

```bash
source .envrc
scripts/run-evaluation-scenarios --system-one --scenario system-one-stone \
  --recorder-jar /absolute/path/to/recorder-profile.jar \
  --stop-client-after-scenario --bridge-timeout-seconds 240
```

The evaluator prepares a disposable platform: stone underneath three soil layers, with one wooden pickaxe supplied. Fixture construction executes on the integrated-server thread before goal submission. Setup state and coordinates are not supplied to the agent. This fixture deliberately isolates acquisition; it does not replace the original iron-pickaxe scenario. Use `--scenario system-one-production` for the empty-inventory version with an observed log column and a stone-pickaxe goal. That fixture supplies no tools or ingredients directly to the player.

| Run directory under `eval-output/` | Evidence |
| --- | --- |
| `20260906-195642-628152-51720` | First look/break succeeded; first navigation crashed because one Baritone constructor overload had not captured its terrain view. Corrected the constructor selector. |
| `20260906-195834-754333-53801` | Interrupted rerun exited before a terminal scenario report. No gameplay success claim. |
| `20260907-154110-841283-61691` | Navigation worked, but excavation widened the surface and wore out the pickaxe. The fixture incorrectly gated evaluator ticks on remaining at the starting height, causing recording to stop after descent. Recovered 485 remaining System 1 events from the live event buffer; earlier events were truncated. Gracefully stopped the client. |
| `20260907-154739-926189-63272` | Obtained three cobblestone in 446 elapsed ticks; scenario PASSED and harness OK. Recorder Play finalized. This run still performed routine panorama scans; the follow-up run tests their removal. |
| `20260907-155134-120434-64304` | Obtained three cobblestone in 376 elapsed ticks with zero survey-look commands. Scenario PASSED; harness OK; Recorder Play finalized. `system-one-final.json` confirms SUCCEEDED and no remaining motor command, pathing process, or pending release. Planner journal is empty and final LLM records are empty. |
| `20260907-160438-513978-67297` | Obtained three cobblestone in 376 elapsed ticks; scenario PASSED and harness OK. Decision recording completed with 312 input rows. Offline replay verified all 311 turns, matching effects, lifecycle events, and SUCCEEDED inventory evidence. |
| `20260907-162016-195995-72174` | Live startup caught an incorrect overload selected when remapping the passability hook. Client failed before gameplay; corrected the selector to an explicit descriptor. |
| `20260907-162237-150390-72866` | Live adapter probe held its authorized corridor fixed while changing 102 hidden cells. All 132 movement results, the A* route, and cost matched. Subsequent autonomous stone goal PASSED in 388 ticks with zero planner turns. |
| `20260907-162605-529498-73805` | Final probe also checks placement rejection and bucket-selection isolation. Paired route/cost checks passed; autonomous stone goal PASSED in 382 ticks, harness OK, zero planner calls, and motor fully released. All 312 decision turns replayed offline. |
| `20260907-164921-022470-78966` | First production attempt chose acacia ingredients despite observed oak. Saved events identified the incorrect cost estimate; stopped the run and added an observed-resource preference regression. No terminal success claim. |
| `20260907-165313-281422-80092` | Harvested logs, crafted the table and wooden pickaxe, and obtained three cobblestone. Failed because a nearby remembered table was occluded. Replay reproduced all 693 decision turns and the failure. Added station visibility/return logic and placement body-clearance checks. |
| `20260907-170343-610028-83181` | Return attempts failed: excavation had removed earlier footholds. The fallback created another table but could not place it from the shaft. Scenario FAILED in 824 elapsed ticks; zero planner turns. This exposes an acquisition defect that the stone-only fixture did not exercise. |
| `20260907-171617-714595-86198` | Protecting visited footholds alone caused surface widening: walking onto each cleared cell immediately protected the floor that needed excavation. Stopped this repetitive run and corrected the step sequence. |
| `20260907-171921-268801-87086` | The corrected sequence descended two steps and preserved footholds. Later descent failed because the destination ceiling blocked entry despite two-block standing clearance. Recorded observations identified the obstructing grass block. All 967 turns replayed with policy v3 at checkpoint `dc70f25`. Added a retained step destination and overhead preparation. |
| `20260907-172346-877489-88255` | Empty-inventory stone-pickaxe goal PASSED in 683 ticks, zero planner turns. Cleanup overwrote pending success with cancellation; all 618 turns reproduced that race offline. Corrected cleanup to preserve terminal decisions while waiting for release. |
| `20260907-172709-641844-89225` | Final production verification PASSED in 690 elapsed ticks; harness OK, zero planner/LLM calls, runtime SUCCEEDED, motor fully released, client exited normally. All 628 decision turns replayed offline. There were 16 break commands, 7 crafts, 7 navigations, 1 placement, and zero survey turns; one occluded target recovered. |
| `20260907-174348-876499-93376` | First exposed-iron attempt FAILED before motor actuation: resource cost estimation preferred an unavailable diamond tool. Cyclic block-compression recipes divided a finite unavailability penalty into an apparently cheaper cost. The 8 recorded decision turns replay exactly with production policy v5. |
| `20260907-174947-087516-94884` | Tool progression correction allowed stone-pickaxe crafting and raw-iron harvesting. Furnace supply then exhausted excavation alternatives despite nearby previously observed stone. Production policy v6 records the failure and bounded cleanup; the next correction adds approaching observed stone before another excavation. |
| `20260907-175633-378080-96539` | Collected furnace stone but could not return: later excavation could remove footholds belonging to earlier tasks. Stopped after repeated return attempts. Added session-scoped foothold memory, recorded and replayed with observations. |
| `20260907-180207-162291-97887` | Routes and resource production progressed with no motor failures. All three exposed iron ores were mined, but pickup chose the player's lower height instead of the drop's floor. Search eventually exhausted its budget. Production policy v8 preserves this failure; drop-height recovery is next. |
| `20260907-181125-003484-138/01-system-one-iron` | Pickup correction collected raw iron. Furnace supply then took a two-block descent from Y196 to Y194, leaving no climbable return route. Failed in 1753 elapsed ticks, harness OK, zero planner calls, motor released. All 1677 turns replayed with production policy v9. |
| `20260907-181125-003484-138/02-system-one-smelting` | Focused fixture loaded raw iron and plank fuel into an observed furnace, waited without owning the motor, and collected an ingot. PASSED in 282 ticks, harness OK, zero planner calls; all 212 turns replayed. Cleanup still overwrote success while a child released the motor, leaving runtime CANCELLED. This fixture isolates furnace interaction and does not prove the iron-pickaxe chain. |
| `20260907-182610-738023-4574/02-system-one-smelting` | Root goal observation now ends the whole branch after motor release. PASSED in 293 elapsed ticks, runtime SUCCEEDED, zero planner calls, motor fully released. All 219 turns replayed with production policy v10. |
| `20260907-182610-738023-4574/01-system-one-iron` | One-block descent and intermediate mining stances allowed climbing from Y194 back to Y198, crafting a furnace, and smelting one ingot. Furnace placement at (-1,199,18) then occupied a used stair cell; subsequent surface navigation failed and raw-iron search exhausted. FAILED in 2621 elapsed ticks, zero planner calls; all 2545 turns replayed. Placement must preserve passage clearance as well as excavation preserving supports. |
| `20260907-183337-793482-6614/01-iron-pickaxe` | Original frozen scenario, no fixture supplies: harvested spruce, crafted a wooden pickaxe, excavated stone, and crafted a stone pickaxe. Exposed-iron search then exhausted; underground resource exploration remains missing. FAILED in 1827 elapsed ticks with zero planner calls. All 1805 decision turns replayed with production policy v11. |
| `20260907-183337-793482-6614/02-system-one-iron` | New placement avoided the used route, but wooden-pickaxe crafting failed at tick 429 with `crafting_cursor_occupied`. The adapter issued three predicted clicks per ingredient and treated an occupied cursor on the next tick as terminal. Stopped the ensuing alternative-wood searches through the isolated client-stop endpoint. No terminal fixture pass or complete replay claim. |
| `20260907-184153-125742-8728/01-system-one-iron` | Empty-inventory full production fixture PASSED in 2164 elapsed ticks, zero planner calls, harness OK, runtime SUCCEEDED, motor fully released. Crafted tools, excavated stone, harvested exposed ore, placed a furnace outside the used passage, smelted three ingots, and crafted the iron pickaxe. All 2086 decision turns replayed. Motor v7 waits for the cursor and recipe slots to settle between ingredient transfers. This is fixture proof, not a pass of the original terrain scenario. |
| `20260907-184153-125742-8728/02-system-one-production` | Concurrent stone-pickaxe regression PASSED in 819 elapsed ticks, zero planner calls, runtime SUCCEEDED. All 747 decision turns replayed. Both clients exercised crafting under concurrent live load without the earlier cursor failure. |

The failed recording is a concrete recorder/harness finding: an artifact existing on disk does not prove that recording continued through gameplay. Setup completion is now latched once, and post-outcome recording includes motor release before cleanup finishes. The final production Recorder Play was rendered successfully to 676 frames and `recording-render/fpv.mp4`. Sampled first-person frames were visually inspected through log harvesting, excavation, cobblestone collection, and return toward the table. The renderer covers its available server ticks 28–703, versus requested 28–724, and reports 314 ignored unsupported packets (310 chunk unloads, 4 player positions). Its images are supporting evidence; they do not independently prove final inventory or exact runtime-tick alignment.

The successful exposed-iron fixture's Recorder Play also rendered 2304 frames at 960×540 and 20 fps. Inspected frames 400, 900, 1350, 1600, 2200, and 2300 show stone/ore mining and furnace/table interactions. The final inventory is not shown in those frames; the evaluator and replayed inventory observation establish the iron-pickaxe result. Render coverage is global ticks 18–2321 versus requested 18–2341, with 238 skipped unsupported packets (234 chunk unloads, four player positions). These are explicit external playback limits, not proof of complete tick alignment.

Replay the recorded live decision inputs (no Minecraft client required):

```bash
./gradlew replaySystemOne \
  -Pairicraft.replayFile="$PWD/eval-output/20260907-172709-641844-89225/01-system-one-production/system-one-decisions.jsonl.gz"
```

This verifies the deterministic decision boundary; it does not replay physics or prove sensor fairness. The replay entry point reads both stone and production records. Policy revisions must match the recorded decisions; historical failures should be replayed from their implementation checkpoint.

Inspect any historical policy's recording without launching Minecraft:

```bash
scripts/inspect-system-one-recording \
  eval-output/20260907-171921-268801-87086/01-system-one-production/system-one-decisions.jsonl.gz
```

The compact report includes inventory changes, command counts, failure reasons, the first failed prerequisite chain, and the last failed command's parent chain. Input coverage is checked separately from decision replay; truncated compression, missing/reordered turns, missing footers, and trailing records report incomplete coverage. The inspector never claims policy replay.

## Outstanding plan gates

Underground search and a separate lighting policy are implemented experimentally in production policy v12. Search uses observed cave floors and individual exposed excavation surfaces; geological priors supply only a preferred depth and work bounds. Lighting has hysteresis, child-task supply/placement, return continuations, and a time/region allowance after repair failure. The perception lens is unchanged. Seventy-two System 1 tests and 33 evaluator tests pass, including repair ownership and preserved destinations. Evaluator event checks now support exact payload predicates, so the cave fixtures require supply/placement resumption evidence as well as raw-iron inventory.

The first live cave batch, `20260907-190840-236654-14858`, failed search in both fixtures. `system-one-lighting` entered the cave, requested supply at tick 152, crafted eight torches, placed one, observed restored working light, and resumed exploration at tick 172. The supplied-torch fixture also placed light and resumed. Both reached the cave's end before lateral exploration changed their heading and led excavation into side walls. Final results were FAILED at 960 and 945 elapsed ticks, with zero planner turns. These runs prove the local supply/placement cycle, not successful underground acquisition or the full away-from-cave resupply gate.

The v12 lighting and underground tapes replay all 886 and 865 turns. Policy v13 keeps the search heading across lateral movement; motor v8 requires grounded arrival in the requested destination block. Batch `20260907-191513-735706-16562` still failed at 974/964 elapsed ticks: preferring any unvisited floor caused backtracking from the cave's end to its entrance before excavation. Recorded positions isolate the remaining frontier-ranking defect; the supply/placement/resume cycle continued to work.

Policy v14 prevents backward floor selection from replacing forward search. Batch `20260907-192007-336591-17947` PASSED both cave fixtures: lighting supply in 303 elapsed ticks and supplied-torch exploration in 279, zero planner calls, runtime SUCCEEDED, and all motor control released. The lighting fixture's payload checks confirm supply suspension, acquisition of eight torches, and resumption after observed working light. Ore first becomes identified at ticks 288 and 256, after exploration opens the tunnel end. These are local-crafting and underground-discovery proofs; running out partway through a longer cave, away-from-cave supply, charcoal bootstrap, and hazard/changed-route cases remain separate gates.

1. Extend the verified live decision recording/replay to subsequent domain methods; expose accurate runtime/version diagnostics and finish lifecycle ownership on reload/world changes.
2. Finish stone variants, paired autonomous hidden-layout runs, and controlled travel/work/perception metrics. The source audit, adapter-level paired-world probe, and survey correction now have live evidence.
3. Exposed-iron production and passive smelting now have a complete live fixture pass. Extend quantity/fuel accounting beyond one-input batches, test charcoal alternatives, and add underground iron acquisition for the original scenario.
4. Implement exploration continuations, maintained lighting, resupply children, bounded relaxation, and survival preemption through the same motor owner.
5. Pass the original iron-pickaxe scenario and frozen development/held-out evaluations; extend injected-failure diagnosis and visual Recorder Play review to those behaviors.
6. Migrate remaining entry points and delete the old execution ownership and temporary selector.
7. Add and evaluate the capabilities needed for autonomous Minecraft completion across multiple seeds.

The new long-cave `system-one-lighting-exhaustion` fixture starts with one torch, two coal, and two sticks, with a 24-block passage. Batch `20260907-193145-189244-21203` consumed the carried torch before supply (placement at tick 157, supply at 200), crafted eight, and repeatedly restored light while preserving exploration task 2. It discovered and mined ore but failed pickup: the ore cavity had a solid ceiling, and pickup required standing in the exact drop column instead of an adjacent accessible cell. The fallback issued four explicit 90-degree survey commands, providing a concrete cause for spins in this run. Final FAILED at 1214 elapsed ticks; all 1134 turns replay with v14.

The concurrent original `iron-pickaxe` run descended from Y64 to Y61, observed and harvested coal, and crafted eight torches autonomously. Placement failed because the narrow stair's available floors were protected footholds. After 80 ticks of bounded reduced-light progress it returned to the search origin; fallback harvesting exhausted. FAILED at 1857 elapsed ticks, zero planner calls; all 1836 turns replay with v14. This establishes real-terrain supply and bounded retreat, not an original-scenario pass. Next corrections are adjacent drop pickup and lighting placement in narrow passages. The surface-floor ranking concern did not cause this failure and remains unmodified.

Policy v16 adds explicit placement faces and wall-torch identity checks; 75 System 1 and 33 evaluator tests pass. Batch `20260907-193856-359490-24712` exposed two remaining issues. The long cave approached the mined cavity but the drop could remain beyond pickup range under its solid headroom; FAILED at 1394 elapsed ticks, with all 1319 turns replayed. The original scenario's different initial view selected unavailable acacia prerequisites before observing nearby wood. Stopped that repetitive search through the isolated client-stop endpoint; harness reports CLIENT_EXITED before a terminal scenario report. No original-scenario replay or success claim for that interrupted run.

Policy v17 retains resource-drop identity while clearing observed, excavatable headroom over an observed safe floor. Batch `20260907-194646-603668-27548/01-system-one-lighting-exhaustion` PASSED in 423 elapsed ticks, zero planner calls, with all 347 turns replayed. Its recorded sequence consumed the carried torch, acquired eight replacements, placed further lights, and collected raw iron from the tunnel end. This run's randomized drop was collected from the adjacent cell; the headroom-clearance sequence has unit coverage but still needs direct live execution evidence.

The new one-block-wide `system-one-lighting-stairs` fixture in the same batch FAILED in 946 elapsed ticks; all 881 turns replay. Supply and light restoration succeeded, but choosing the next unused floor for a torch put a non-air voxel in the intended passage. The conservative terrain model then diverted search sideways. Wall placement must be preferred above passage clearance, and observed non-colliding block geometry remains a broader outstanding perception/domain improvement. These fixtures leave the original benchmark unchanged.

Policy v18 prefers observed wall supports above passage clearance before unused floors. Batch `20260907-195031-310300-28757` PASSED both cases with zero planner calls and runtime SUCCEEDED, with no remaining motor command, pathing process, or release pending:

- `01-system-one-lighting-exhaustion`: 418 elapsed ticks; all 346 decision turns replay. Task 2 placed its only carried torch at tick 167, resumed with zero torches at 170, suspended for supply at 216, observed eight crafted torches at 233, placed another wall torch, and resumed at 236. It then collected raw iron. This proves actual depletion and local resupply in a longer passage; it does not prove away-from-cave resupply.
- `02-system-one-lighting-stairs`: 248 elapsed ticks; all 173 decision turns replay. The one-block-wide passage used an EAST wall face at tick 172, observed restored light at 175, retained exploration task 2, and collected raw iron. This directly exercises wall placement without obstructing the narrow route.

All 75 System 1 tests and 33 evaluator tests pass. The original scenario still requires robust ingredient choice when initial observations omit nearby wood; the interrupted v16 run provides that regression. Non-colliding voxel geometry, away-from-cave supply/return, hazard preemption, larger production batches, original/held-out scenario gates, migration/deletion, and full-game capabilities remain outstanding.

Visual verification found an external playback failure for the successful v18 depletion Play. The renderer completed `recording-render/fpv.mp4` (494 frames, 960×540, 20 fps; global ticks 12–505 versus requested 12–506), but sampled frames 150, 210, 240, 350, and 430 all show the same view outside the cave. Only 29 distinct PNG byte hashes occur; the final frame change is ordinal 117. The MP4 is not valid visual proof of cave traversal. The captured `playerState` stream does contain 495 changing state samples through tick 506: positions descend from Y200 into the cave and progress from Z15 to Z40, with changing yaw/pitch and final raw-iron inventory. Thus the numerical capture and policy replay remain useful, while replay/camera presentation requires diagnosis. The renderer reports 160 ignored packets (156 chunk unloads, four player-position packets); that alone does not establish the cause. The external recorder checkout currently has unresolved merge conflicts, and this task has not modified it.

Policy v19 adds a generic ancestor-revision boundary to the task kernel. A domain can replace an obsolete method while preserving its parent task ID, run deadline, and command/task counters. An active descendant must acknowledge release before the kernel cancels that subtree and makes the parent ready with its new continuation. Cancellation overrides a pending revision; stale feedback cannot finish the replacement command. The Minecraft domain uses this only at completed look/travel boundaries of an unfulfilled, unobserved harvest, with a substantial recipe-cost improvement and no mined drops awaiting pickup. This addresses commitment to unavailable wood after another species becomes observed, without restarting the root goal or injecting a new controller. Seventy-nine System 1 and 33 evaluator tests pass. The new `system-one-production-discovery` fixture places wood behind the initial view and requires a revision event as well as a stone pickaxe; live verification is in progress.

Batch `20260907-200152-762628-32160` verified v19 recipe revision live. The discovery fixture revised wooden-pickaxe task 3 at tick 108 after observing oak, then PASSED in 892 elapsed ticks with zero planner calls; all 817 turns replay. The original scenario failed in 966 elapsed ticks at `crafting_cursor_occupied` while crafting the stone pickaxe; all 940 turns replay. The earlier two-tick client settling heuristic was insufficient.

Motor v10 replaces that heuristic with immutable full server-inventory receipts captured on `ScreenHandler.updateSlotStacks`. Each ingredient burst waits for a newer response for the same container containing the expected grid and an empty cursor. Minecraft 1.21.8 advances the server revision on a crafting-grid update, so the burst's following return click triggers a full resync. Tests reject old/wrong-container responses, occupied cursors, partial grids, and extra ingredients. Eighty-one System 1 and 33 evaluator tests pass.

Batch `20260907-201151-093354-35053` exercised the receipt motor with no crafting failures. Discovery PASSED in 745 elapsed ticks (682 replayed turns). The original run crafted tools, left its excavation, produced charcoal in a furnace, crafted eight torches, and returned, then failed lighting placement at tick 1911; FAILED in 2465 elapsed ticks (2443 replayed turns). The exposed-iron fixture failed ore acquisition in 1704 elapsed ticks (1624 replayed turns): remembered ore was close in straight-line distance but occluded by the excavation rim. All runs used zero planner calls. These failures identify two separate geometric issues: harvesting must approach from an observed visible stance, and footprint memory can retain a center cell observed as air while the player's body is supported by a neighboring block.

Policy v20 and observed footprint memory fix the two geometric failures. Footing memory now records observed support blocks under the body's contact area and invalidates cells observed as air; harvesting selects an observed visible stance instead of attempting a remembered ore through a rim. Eighty-four System 1 and 33 evaluator tests pass. Batch `20260907-202516-654171-39487` PASSED full iron production in 2157 elapsed ticks (2083 replayed turns) and narrow-passage lighting in 227 elapsed ticks (163 replayed turns), both with zero planner calls and all motor control released.

The concurrent original scenario completed charcoal resupply, returned, restored working light, and descended to Y32 before `underground_search_budget_exhausted`. FAILED in 4954 elapsed ticks; all 4933 turns replay. Its search issued only 44 navigation commands, so the 96-step budget was not the cause. Nine navigation commands reported `observed_route_unavailable` despite the player already occupying the requested destination cell; one recorded feet height was still 0.232 blocks above the floor. The motor checks for a stopped path against time since command start instead of allowing settling after path completion. These and other historical rejected candidates accumulated against the search's global rejection limit even while it made progress. Next corrections are bounded arrival settling and resetting local failed candidates after reaching a new exploration position.

Policy v21 / motor v11 gives navigation ten idle ticks after Baritone stops, allowing the player to land before declaring a missing route. Reaching a retained underground step clears obsolete local rejections without resetting the step or mission budgets. The focused suite passed 85 System 1 and 33 evaluator tests. Batch `20260907-203841-405763-43724` passed the narrow staircase in 254 elapsed ticks, with no failed navigation commands; all 176 turns replayed. The original iron-pickaxe scenario failed at 2646 elapsed ticks; all 2626 turns replayed, with zero planner calls. Its first underground failure was `no_observed_underground_step`: the three excavated descent columns had observed andesite floors, which the block-name whitelist rejected as standing support. Next: derive support from observed physical block properties instead of enumerating terrain names.

Policy v22 / stone method v8 records `fullSupport` alongside each observed voxel. Standing checks consume this physical fact rather than Minecraft material names. The Minecraft sensor accepts cached full collision cubes only for blocks without dynamic bounds, using no live neighbor queries for shape evaluation. Darkness masks support along with identity; the perception ray still stops at the first occupied voxel. Dynamic/partial support, non-air passage, and hazard policy remain separate work. Recordings from older method versions are explicitly incompatible with the changed observation contract.

The focused suite passes 86 System 1 and 33 evaluator tests, and both client source sets compile. The staircase now alternates andesite/deepslate underfoot. In batch `20260907-205241-751120-48247`, `02-system-one-lighting-stairs` PASSED in 263 elapsed ticks with zero planner calls, and all 176 decision turns replayed. Its tape records feet above five distinct observed deepslate supports and four andesite supports. This proves the Minecraft adapter supplies usable support facts for those materials; it is not visual playback proof or an original-scenario pass.

The v22 original run reached smelting and collected an ingot, but did not pass. A second one-input smelt tried to acquire more raw iron from the surface furnace; underground search failed at tick 4784 and harvesting exhausted at 5272. Fallback production then repeatedly unpacked the existing ingot into nine nuggets and repacked it, making no net progress toward three ingots. Stopped the owned client after at least 74 cycles. Harness outcome is CLIENT_EXITED before terminal report; this is an interrupted diagnostic run, with no full replay claim.

Policy v23 reserves existing goal stock while obtaining nested ingredients, tools, lighting supplies, stations, or smelting prerequisites. This prevents a partially satisfied goal from financing its own reversible conversion cycle. A generic bar/piece regression reproduced the old loop before the fix; it now chooses fresh production, terminates without consuming stock when no alternative exists, and still permits packing pieces already in inventory. All 87 System 1 and 33 evaluator tests pass. Live verification of v23 is pending. The preceding raw-iron failure also exposes one-batch ingredient acquisition: the next change will provision the missing output quantity before returning to a workstation.

Policies v24/v25 provision recipe inputs for all remaining output batches, accounting for yield and existing free stock, before returning to a workstation. Commands still cook or craft one batch at a time and observe each result. Smelting additionally reserves later batches' raw inputs so fuel acquisition cannot burn them. All 90 System 1 and 33 evaluator tests pass; separate regressions first reproduced the one-batch supply and later-input fuel defects. Residual furnace burn time is still not accounted for, so fuel use remains conservative.

Batch `20260907-210547-612131-52266` uses v24. Its exposed-iron fixture PASSED in 2238 elapsed ticks with zero planner calls; all 2158 turns replayed using saved v24 decision classes. The original scenario is still running at this entry: it recovered from a harvest failure, obtained three raw iron at tick 7506, returned to the furnace, and is acquiring more smelting fuel.

The new `system-one-charcoal` fixture starts with two logs and an exposed furnace, plus nearby harvestable wood. In v25 batch `20260907-211059-376790-53903`, it PASSED in 592 elapsed ticks with zero planner calls and a fully released motor; all 525 turns replayed. The decision tape records one extra log harvested, then three logs becoming two reserved logs plus four fuel planks. The first smelt at tick 177 uses a plank with both logs still present; the second at 396 uses another plank with one log and the first charcoal still present. The scenario inventory check is supplemented by this manual tape inspection of the reservation behavior; the YAML does not yet automatically assert that command order.

## First original iron-pickaxe pass

`20260907-210547-612131-52266/01-iron-pickaxe` PASSED under policy v24 / motor v11 in **9369 elapsed ticks**, with the unchanged original inventory check satisfied. Harness status is OK, the client exited normally, and the motor is fully released (no command, pathing, crafting, smelting, or pending release). The planner journal is empty and final LLM records are empty. The decision recording is complete, and **all 9346 turns replay exactly** using the saved matching v24 decision classes. The separate Recorder Play finalized; visual playback of this run has not yet been verified.

The tape contains 134 breaks, 152 navigations, 80 looks, 16 crafts, 13 placements, seven furnace starts, and seven collections. Summed successive recorded eye positions give approximately 678.59 blocks of travel; the lowest recorded feet position is Y36. These are observation-derived work/travel metrics, not a damage audit or proof of zero perception violations. The run recovered from a raw-iron harvest failure, obtained three raw iron at tick 7506, smelted them, acquired additional fuel, and crafted the final pickaxe at tick 9379; final release was acknowledged at 9393. Scenario elapsed ticks and runtime tick origins differ.

This is one successful original-world run, not the frozen held-out reliability gate. Current v25 still needs an original rerun after its later-input fuel reservation change. Remaining required work includes the other survival/lighting/route interruption fixtures, automatic behavioral metrics, visual replay diagnosis, frozen development and held-out evaluation, migration/deletion, and the game-completion stages.

The generic kernel now checks a domain interruption hook during passive waits as well as active commands. It preserves the ancestor branch, uses the domain-supplied continuation, and waits for motor release before starting urgent work. Interruption consumes a transition from the existing budget; cancellation still takes precedence. Three new synthetic-domain tests cover passive waiting, active handoff, late feedback, cancellation, and the transition limit. All 93 System 1 and 33 evaluator tests pass. Minecraft survival policy is not wired to the new hook yet; this is scheduler support, not a survival behavior pass.

The v25 rerun `20260907-211820-711360-56166` passed mid-cave torch depletion in 508 elapsed ticks, but the original scenario FAILED at 8376 elapsed ticks with zero planner calls. Its first failure at tick 7781 was a cave gap at feet (31,31,5): adjacent candidate cells were observed air with no floor for the search's one-block step. Neighboring solid faces could support explicit foothold placement, but that method did not exist. This failed rerun means the earlier v24 pass does not establish current-policy reliability. Next: bounded placement of a foothold from uncommitted carried material, using only observed empty space and an observed supporting face.

`scripts/inspect-system-one-recording --failure-context <tape>` now reconstructs the first failed task's local observed cells (a bounded radius-two cube), inventory, footholds, and recent motor commands. It works across policy versions and still labels decision replay as unchecked. Six inspector tests pass, including observation removals and preserving the first failure's state when later turns change inventory or terrain. Applying it to the v25 original reproduces tick 7781 at (31,31,5), with 81 local observed cells; applying it to the new gap fixture exposed the next policy failure without adding runtime logging.

Policies v26/v27 add explicit foothold placement. The Minecraft knowledge pack supplies stable material choices (cobblestone/dirt, with separate item and placed-block identities); the search consumes only uncommitted stock. Placement requires observed empty destination/headroom and a visible face of an observed full support block. Baritone still receives bounded navigation commands after support is observed. Placement destinations are checked against any active reduced-light region.

The first gap fixture in `20260907-213645-137141-60488/02-system-one-cave-gap` FAILED in 1695 elapsed ticks; all 1618 turns replayed. Placement worked, but the search chose lower footholds, dug beneath the target, and cycled through side cells until its step budget expired (180 looks and 121 navigations). v27 prefers a fillable gap at the existing floor height after already-standable choices, and limits the visited-floor exception to the exact retained destination. All 95 System 1 and 33 evaluator tests pass.

The v27 gap fixture `20260907-214423-086034-62121/01-system-one-cave-gap` PASSED in 250 elapsed ticks with zero planner calls and a fully released motor. All 181 turns replayed. It placed two cobblestone supports at Y195 across the gap, placed one wall torch, and reached raw iron with ten navigations, four breaks, and zero survey-look commands. The evaluator independently found both placed cobblestone blocks near the final position. This proves the targeted gap behavior, not generic bridging across unsupported open voids.

### Retain exploration routes through supply detours (v28)

The v26 original failure saved an exploration anchor at (-8,16,65), then finished resupply at (-5,70,19). Its detour exceeded the sensor's 48-block horizontal cache radius; no cells remained within two blocks of the saved anchor. Return failed without issuing a navigation command. Pinning just that endpoint would still leave missing intermediate terrain and an overlong single movement.

Exploration now records actual visited positions, retains already observed corridor cells while its continuation is active, and rejoins/follows that route in bounded waypoint legs. New observations can invalidate retained terrain, and completing the continuation releases its retention request. Partial failed movement also consumes the search step budget. Return and retreat share this navigation method; each command remains limited to 48 blocks and 400 ticks. This is active-continuation retention, not a permanent mission map.

All 101 System 1 tests and 33 evaluator tests pass. New tests cover long returns, joining after a detour, bounded failed stances, actual movement accounting, retention beyond the local cache, and invalidation without inventing unseen cells. Live batch `20260907-221231-647528-67875` runs the original scenario plus a new 60-block cave fixture that removes torches after 52 blocks of progress. The first fixture run failed before that intervention: at tick 197 the search chose to break the floor at (0,195,25), although (0,196,25) was already an observed standable position. A previously selected downward step outranked the newly observed cave floor. Consequently this run does not verify long-distance resupply or return; candidate reconsideration must be fixed before retrying that gate.

### Reconsider proposed excavation after observing a floor (v29)

The failed v28 return fixture finalized with harness OK; all 1686 decision turns replay exactly. The search now ranks observed standable terrain, then a fillable flat gap, ahead of a retained excavation proposal. A regression test reproduces the look-to-floor-discovery transition; all 102 System 1 tests pass. The v29 run `20260907-221644-583075-68861/01-system-one-return-route` traversed the existing floor, reached the 52-block intervention, and had 59 torches removed at tick 559. Exploration task 2 requested supply at tick 573. This establishes the candidate-selection correction, but the resupply/return gate is still pending: it remembers the entrance oak and furnace yet wastes work on unavailable wood variants. Existing ancestor reconsideration handles crafting recipes but excludes smelting alternatives.

The completed v29 recording establishes the return behavior: suspension saved (0,196,69); resupply took the player as far as Z14, 55 blocks from that anchor. Fifty observed cells near the anchor remained present beyond the local cache radius. At tick 6132, task 2 resumed with eight torches at (0,198,17), then navigated through Z32, Z48, Z63, and back to Z69. Each command retained its 48-block/400-tick bound. The same task continued exploration and the runtime obtained raw iron at tick 6674. All 6581 turns replay exactly, with no LLM calls and a fully released motor. However, the scenario report is FAILED at 6651 elapsed ticks: its earlier `lighting_supply` suspension event had been evicted from the bounded runtime event history, so the final `event_contains` check forgot an event it had already matched. The harness itself is OK and both clients exited normally. Do not relabel that existing report as a pass.

Evaluator historical event checks now retain the first matching tick for the current scenario. Inventory and other current-state assertions still reevaluate normally; starting another scenario clears the retained matches. A regression test covers eviction followed by later inventory success, and reset without a new matching event. All 34 evaluator tests pass. A fresh live rerun is required to validate the corrected report.

The v28 original `20260907-221231-647528-67875/01-iron-pickaxe` FAILED at 10532 elapsed ticks, with zero planner calls, harness OK, and a fully released motor. All 10512 turns replay. It completed two torch resupplies, but took repeated lava damage from tick 6798 and died at 6868. Automatic respawn occurred at 6869; System 1 continued the stale exploration task rather than responding to the death. Survival preemption and post-death continuation invalidation remain unimplemented acceptance requirements. The earlier v24 original pass does not cover them.

### Reconsider smelting alternatives and verify the long return (v30)

Crafting and smelting now use the same bounded method ranking for initial choice and reconsideration. At completed observation/travel boundaries during an unproductive input search, an appreciably cheaper observed alternative can replace the ancestor method. It preserves the ancestor goal identity and resource commitments, and does not interrupt productive harvesting or an active furnace operation. A regression test discovers an alternative smelting input while preserving already produced charcoal. All 103 System 1 tests and 34 evaluator tests pass.

Batch `20260907-222549-123594-70822` PASSED both scenarios with harness OK, empty planner journals, zero LLM records, fully released motors, and normally exited clients:

- `01-system-one-charcoal`: 608 elapsed ticks; all 527 decision turns replay exactly.
- `02-system-one-return-route`: 3080 elapsed ticks; all 3009 turns replay exactly. The fixture removed 57 torches at tick 488, exploration task 2 suspended at tick 508 with anchor (0,196,70), and resupply switched to observed oak smelting at tick 1831. Supply finished at 2574. The detour reached 53 horizontal blocks from the anchor, whose 50 nearby observed cells remained retained. Four bounded return legs targeted Z30, Z46, Z62, and Z70; actual feet reached the original anchor at 2839 before task 2 continued exploration. Final inventory contains raw iron, seven torches, and the stone pickaxe. The corrected evaluator retained the suspension and resume matches through completion. `return-verification.json` records the derived route evidence beside the raw tape.

This proves long-cave resupply and return, not general survival or current original-scenario reliability. The return run still issued 261 survey looks during unavailable-resource searches; method reconsideration reduces wasted work but does not solve the broader attention/frontier policy. The v28 fatal trace already identified lava beside the route at tick 6709, before further excavation and entry at Y27. The agent continued issuing break commands while taking lava damage. Next requirements: act on observed excavation hazards, wire survival preemption through the single motor lease, invalidate stale post-death continuations, and exercise hazard/changed-route fixtures before another original sanity run. Visual playback of these latest recordings has not been verified.

### Survival interruption and death recovery (v31)

The observation value and tape now include health, lava/fire contact, and a life identity derived from the client player instance. A Minecraft survival policy supplies explicit hazard IDs and bounded refuge selection over observed standing positions. Underground candidate eligibility and exposed harvesting avoid nearby observed hazards. The generic kernel remains free of Minecraft types; its existing interruption/release mechanism runs escape children, and task revision can now reset the active root as well as an ancestor.

Live missions wrap their inventory objective with life/recovery state. Death releases active control and discards stale descendants. Respawn preserves the mission ID and goal, rebuilds dependencies from current inventory, and retains the original command/time budgets; recovery is limited to two deaths. Escape continuations preserve passive furnace timing instead of collecting prematurely after an interruption. This does not implement food, combat, drowning, arbitrary dimension transitions, or every mid-inventory-transaction recovery case.

All 107 System 1 tests and 34 evaluator tests pass. Tests cover active mining release before escape, stale feedback, interruption of a passive furnace wait, post-death root revision without budget reset, and observed-only hazard eligibility.

Batch `20260907-225541-604125-78476` PASSED both live fixtures with harness OK, zero LLM calls, empty planner journals, fully released motors, and normally exited clients. Artifacts now reside on the external drive through the existing repository paths.

- `01-system-one-death-recovery`: 280 elapsed ticks; all 211 decision turns replay exactly. The fixture killed the player after the first log at tick 168. The runtime revised mission 1 with `player_died` at 172, then `life_changed:1` at 173, preserving the planks objective and rebuilding its descendants. Final inventory contains eight oak planks and final health is 20. The test uses a prepared world spawn; it does not prove recovery of a remote death inventory.
- `02-system-one-survival-wait`: 293 elapsed ticks; all 217 turns replay exactly. Furnace wait began at 112 with wake tick 312. The fixture introduced adjacent lava, triggering escape children at 113, 164, and 193 as its observed flow spread. Each returned to the same furnace task 3; the task retained wake tick 312 and collected its ingot afterward. Health remained 20 throughout. This is live evidence for passive-wait interruption and preventive escape, not yet active mining preemption or escape while already immersed in lava.

Next: verify an observed hazard interrupting active mining, then rerun the original iron-pickaxe scenario. The changed-return-route fixture, broader survival methods, visual playback, frozen reliability sets, execution cutover/deletion, and game-completion milestones remain open.

The concurrent v26 original run FAILED in 11271 elapsed ticks, zero planner calls; all 11247 turns replayed using saved matching v26 decision classes. It eventually replenished torches and rebuilt a worn-out stone pickaxe, but the saved exploration return failed at tick 10717 before issuing a return command. The anchor captured at tick 6100 was (-8,16,65); a detour reached 58 blocks away on one horizontal axis, beyond the sensor's 48-block cache radius. On return at (-5,70,19), zero remembered cells remained within two blocks of that anchor. Next: retain a bounded observed route for suspended exploration and navigate back through bounded waypoints. Merely retaining the final anchor would leave the corridor and travel budget unresolved.

### Active mining handoff and completion ordering (v32)

Preemption now preserves an eligible harvesting target instead of treating cancellation as evidence that the target failed. Immediate survival work precedes inventory-goal completion, including when the root is an escape continuation. The evaluator waits for System 1 success and acknowledged motor release before accepting its current-state checks. All 108 System 1 and 35 evaluator tests pass.

Batch `20260907-230616-432582-82772/02-system-one-survival-mining` reached eight oak planks in 861 elapsed ticks with a fully released motor and zero planner calls. The available tape proves active Break attempt 1 at tick 153, Stop at 160, release acknowledgement before escape Navigate at 161, and resumption of the same Gather task 4 and still-eligible target at 174. However, its recorder overflowed after 51 rows: harness status is DECISION_TRACE_INCOMPLETE. This is partial handoff evidence, not an accepted complete recording or replay pass. A fresh run is required after investigating external-drive write throughput.

The unchanged original scenario in the same batch FAILED in 5802 elapsed ticks, harness OK, zero planner calls, fully released motor. All 5751 decision turns replay exactly. Health remained 20 in the recorded observations. The first failed child was lighting placement at tick 4914; exploration later exhausted observed steps at 5335, followed by exhausted production alternatives. This run does not establish current-policy iron-pickaxe reliability. The saved local context contains observed tunnel walls beside retained footholds, so placement eligibility warrants inspection separately from the recorder overflow.

### Complete survival recordings on external storage

DecisionTraceWriter now coalesces gzip output into a bounded 64 KiB byte buffer before file writes. The 32-row nonblocking input queue and explicit incomplete footer remain unchanged. A four-trial external-storage probe round-tripped the 15 MB incomplete trace with and without buffering in 0.075–0.079 seconds; that does not establish small writes as the cause of the prior overflow. The overflow remains a transient throughput issue to monitor, not a proven fixed root cause.

The concurrent rerun `20260907-231835-359582-86651` PASSED both fixtures with complete recordings, harness OK, zero planner calls, fully released motors, and normal client exits. Active mining completed in 719 elapsed ticks and all 636 turns replay exactly; passive furnace waiting completed in 376 ticks and all 289 turns replay. In the mining run, the player contacted flowing lava at runtime tick 224 and health fell from 20 to 16. It left lava by 228, reached the refuge at (-5,200,16), waited while burning, and resumed after fire cleared at 551. No death occurred. This adds a bounded live contact-escape example; it does not establish reliable escape from deep lava or survival with depleted food.

### Light a two-block-high walking passage (v33/v34)

The v32 original tape showed tunnel wall placements rejected solely because their destination lay above a retained walking foothold. The observation value now distinguishes an empty voxel from observed body clearance. Minecraft supplies clearance only for static empty collision shapes without fluid, using the isolated empty block view rather than querying hidden neighbors. Unidentified occupied cells cannot grant clearance. Standing-position and excavation decisions can traverse an observed noncolliding fixture without breaking it. Wall-torch placement can consequently use the walking passage; ray occlusion remains a separate rule.

The first two-block-high fixture, `20260907-232158-405408-87706/02-system-one-lighting-low-ceiling`, FAILED in 720 elapsed ticks, harness OK; all 641 turns replay. A wall torch was placed at (0,197,22), but after the camera entered that same voxel the conservative sensor stopped every ray at its origin. The saved failure context established this before new instrumentation. v34 permits rays to leave an identified noncolliding fixture in their origin voxel. All later occupied voxels still stop rays, including other torches; solid and unidentified origin cells receive no exception. Tests cover that distinction, route clearance, placement in a two-block-high tunnel, and navigation without excavating the torch. All 112 System 1 and 35 evaluator tests pass. Production policy is v34; direct stone policy is v9.

The unchanged fixture rerun `20260907-232516-385340-88517/01-system-one-lighting-low-ceiling` PASSED in 315 elapsed ticks, harness OK, zero planner calls, fully released motor, normal client exit. All 246 turns replay exactly. It crafted eight torches, placed a wall torch at tick 239, entered its voxel at 248, moved beyond it at 255, and obtained raw iron with seven torches remaining. It issued no torch-breaking command. This verifies the low-ceiling lighting and traversal case; visual Recorder Play review is still pending.

The concurrent v33 original scenario FAILED in 3985 elapsed ticks with harness OK; all 3962 turns replay exactly. Its first failure was `no_observed_underground_step` at tick 3536, feet (15,66,-48), after a long near-surface excavation. This is a separate search-policy failure, not evidence that the origin-cell lighting correction solves original-world acquisition. Inspect its bounded search state and preference for shallow travel before another original rerun. Changed-route, broader survival, frozen reliability, cutover/deletion, and game-completion gates remain open.

### Preserve an excavation step through headroom clearing (v35)

Reconstructing the v33 original's pure task state showed a specific failure at tick 800: task 25 had selected (6,66,14) and broken its headroom at (6,67,14), but the next decision placed a block to walk at Y67 instead. The newly cleared intermediate position displaced the downward step. By tick 3536 the search had used 75 steps and reached the radius boundary, falling only from Y67 to Y66.

UndergroundSearch now distinguishes observing a proposed step from excavating it. Once a break starts opening a selected step, the step remains preferred through subsequent observation until it is reached, rejected, or invalidated. Hazard, foothold, work, and radius checks still apply. A proposal discovered only through looking can still yield to an existing cave floor. The regression test covers headroom clearing, an intervening look, discovery of a competing flat floor, completion of the downward step, and a newly observed hazard. All 113 System 1 and 35 evaluator tests pass.

The new evaluator-only `system-one-descent` fixture starts in a lit solid-rock chamber, with iron below its starting elevation and no target coordinates supplied to decisions. In batch `20260907-233333-659596-90308`, its first run FAILED in 1977 elapsed ticks, harness OK, zero planner calls; all 1904 turns replay exactly. Descending excavation reached and mined iron at (0,195,20), but the player was still at (0,197,19). Pickup navigation required a two-block descent, exceeding the current motor's one-block fall limit. The task had no method to prepare an alternate approach, then abandoned its known drop. This is an approach-preparation failure, not a discovery/tool failure; do not move the ore or loosen the check to pass the fixture.

The unchanged long-return fixture in that batch PASSED in 2724 elapsed ticks. Its decision replay is being checked, and the original iron-pickaxe run remains active at this entry. The original already reached Y43, demonstrating departure from the earlier near-surface tunnel but not yet goal completion.

The v35 long-return recording replays all 2658 turns exactly; harness OK, zero planner calls, fully released motor. The original scenario completed FAILED in 6041 elapsed ticks, harness OK, zero planner calls, fully released motor; all 6019 turns replay. Its first failed search reached (-6,38,-17) at tick 5473. The saved local context shows a lit cave gap, 47 cobblestone, and retained standing support below the player, but no eligible visible support face for the next foothold. Excavation retention solved the surface-tunnel behavior; this gap requires approach/placement work and remains a failed original sanity run.

The evaluator now also includes `system-one-return-blocked`, sharing the existing long-cave resupply setup. After the recorded eight-torch resupply completion, it inserts a two-block-high stone wall at a fixed point in the already traversed corridor. The intervention is server-side, its evidence is written separately, and its coordinates/state never feed System 1. Batch `20260907-233913-728451-91509` is active at this entry; no changed-route pass is claimed yet.

The blocked-return fixture finalized FAILED in 3146 elapsed ticks, harness OK, zero LLM records/planner calls, fully released motor, normal client exit; all 3079 turns replay exactly. The wall was inserted at runtime tick 2510 while the player was resupplying at (-1,199,17), and was first observed at 2571 from (0,196,27). Return task 3 navigated toward Z46, then tried Z45, Z47, and Z44 from Z42, all beyond the observed wall at Z43. It exhausted four leg attempts at 2695 and never issued a repair break. This is complete saved evidence for the planned changed-route failure injection, not a changed-route recovery pass.

The next implementation should address explicit preparation of a reachable approach to a known target or return waypoint. Current excavation preparation lives only in resource exploration; Gather and ReturnNavigation can select stances but cannot repair their access. Preserve the single motor lease, observed-only break/placement eligibility, one-block step/return constraints, and bounded work while sharing useful terrain preparation. The three recorded remaining cases are the two-block pickup drop, a blocked remembered corridor, and foothold placement without an accessible supporting face. Do not erase these distinctions by increasing navigation limits or treating a remembered destination as reachable.

### Shared local terrain preparation (v36)

`TerrainAccess` now prepares a bounded approach to a selected stance for pickup and remembered-route tasks. It searches a six-block local region, expands at most 512 proposals, and caps execution at 48 commands/1000 ticks. A proposal may include unknown space at higher cost, but only a real look can reveal it. Breaking requires an identified, eligible, reachable surface; movement requires observed body clearance and standing support. Each actual step is at most one block up or down. Protected footholds and observed hazards invalidate proposals, and unchanged looks/failures reject an edge rather than looping. The method does not place supports or solve the outstanding edge-placement case.

Pickup and return failures suspend into the same `Access` task under the existing kernel/motor lease. `AfterAccess` preserves the parent and route retention, reports success back as movement completion, and returns failed preparation to the parent's existing alternatives without recursively restarting it. Reach calculation now preserves observed body clearance for the sensor's existing origin-voxel exception. Production policy is v36; direct stone policy is v10. All 118 System 1 and 35 evaluator tests pass, covering wall preparation, an alternate stair for a two-block drop, observation-only unknown-space handling, budgets, hazards, protected footing, and parent continuation. Probing the saved v35 failure values produces a visible side-stair break for pickup and an upper-wall break for return; these probes establish selected commands, not physics.

Batch `20260907-235206-537121-97079/02-system-one-return-blocked` PASSED in 3427 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 3351 turns replay exactly. Return task 3 suspended at 2882 into access child 31. The child broke the upper wall block at (0,197,43), stepped onto the remaining lower block, descended on the far side, and reached (0,196,46). At 2938 the same return task resumed its longer waypoint legs and subsequently obtained raw iron. The fixture now additionally checks the access suspension and successful resume events; those events are present in this recording, but a fresh run will verify the strengthened YAML checks.

The broader descent fixture in that batch FAILED before exercising pickup repair; all 2286 turns replay. An early `observed_route_unavailable` changed its excavation route, and its first failed search ended at (-16,193,31), missing the buried ore. That scenario remains unchanged and remains a failed gate. The new focused `system-one-pickup-step` fixture reproduces the recorded exposed low ore and unprepared two-block approach. Its run `20260907-235545-649057-99014/01-system-one-pickup-step` PASSED in 238 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit; all 171 turns replay. Access child 5 cleared the next step at tick 213, navigated through (-1,196,16) and (0,195,16), then released control after inventory confirmed raw iron at 254–255. No enlarged fall limit was used. These are separate targeted repair passes, not an original-world or held-out reliability claim.

### Expose a support face by crouching to the edge (v37 / motor v12)

The v36 regression batch `20260908-000039-823114-750` passed the strengthened blocked-return assertions in 3169 elapsed ticks; all 3092 turns replay. Its original iron-pickaxe run FAILED in 10184 elapsed ticks; all 10163 turns replay. Both harnesses are OK. The original's first failed task was a completed lighting retreat at tick 3969, not a new success of the original sanity gate.

The previously recorded Y38 gap exposed a separate missing motor capability: the only useful neighboring support face can belong to the block beneath the player and remain unreachable from the center. A bounded `EdgePlace` command now approaches that face while crouched, validates it with a real ray hit, places the selected carried block, and waits to observe the placed block. Domain selection requires a known empty gap and headroom, identified full standing support confirmed by footing memory, an eligible horizontal neighbor, and uncommitted material. It does not identify new terrain through the edge or delegate target selection to Baritone.

The motor owns the movement keys throughout approach and release. Normal placement releases after observing the block; interruption before placement attempts to return toward the original support center before acknowledging release. World/player loss clears controls immediately. The return attempt is bounded at 40 ticks and does not promise recovery after footing itself disappears. Placement has a 160-tick deadline and a bounded support envelope. Lighting allowance checks include the affected destination. All 120 System 1 and 35 evaluator tests pass; geometry tests supplement, rather than replace, live physics evidence.

`20260908-001122-197567-8792/01-system-one-edge-bridge` PASSED in 283 elapsed ticks with harness OK, zero LLM calls, fully released motor (including edge placement), and normal client exit. All 210 turns replay exactly. Its three-block gap has no side-wall supports and requires a successful edge-placement event as well as raw iron and placed cobblestone.

The first edge approach began at tick 146. At 162 the player's Z was 21.020, just beyond the original support boundary at Z21, and its eye height showed crouching. Lighting maintenance requested Stop. The motor returned to Z20.657 and acknowledged release at 172, before the torch child started. The same exploration task 3 resumed, then completed edge placements at ticks 192 and 213 and a regular face placement to finish the crossing. During the entire edge sequence feet stayed at Y196; health remained 20 throughout the run, with no death. Final inventory included raw iron and seven torches. This is live evidence for successful bridging and interruption/release from the edge, not visual Recorder Play review or broad gap reliability.

### Automatic observation metrics and v37 regression

Batch `20260908-001545-688345-11282/02-system-one-descent` PASSED in 691 elapsed ticks with harness OK, zero LLM calls, fully released motor, and normal client exit. All 599 turns replay exactly. A failed pickup route suspended into shared access preparation; that child encountered another unavailable route but found an alternative and collected raw iron. Health remained 20. This run issued no EdgePlace command, so it supports access recovery rather than attributing the pass to edge placement.

The original iron-pickaxe run in the same batch FAILED in 9934 elapsed ticks with harness OK and normal client exit; all 9901 turns replay exactly. It issued 215 Break attempts and traveled 327.356 observed horizontal blocks. Recorded health stayed 20. The first failed child was a bounded coal-search lighting retreat at tick 4207; production subsequently selected charcoal and resumed. Later the pickaxes wore out, and six Break commands failed `suitable_hotbar_tool_missing`. Exploration checks tools before entry but does not maintain them while running. Final inventory contained 108 cobblestone, two planks, two sticks, and seven torches, but no pickaxe. This is the next repair case; the original sanity gate remains failed for the current policy.

The recording inspector now reports observed horizontal travel, Break attempts, minimum health, adjacent same-life net health decreases, lives observed dead, burning/lava samples, and task suspension/resumption/outcome counts. Definitions explicitly exclude sampling gaps and respawn displacement from travel; health decreases are not total damage, and command attempts do not prove blocks destroyed. Missing vital/pose evidence yields unavailable values. Dark-allowance consumption and perception violations remain explicitly unavailable rather than falsely reporting zero.

Future `--system-one` batches automatically write `system-one-inspection.json` after client/recording finalization, with a link and coverage status in `harness-summary.json`. A saved trace that fails coverage inspection cannot retain harness OK. The inspector remains independent of exact decision replay and scenario success. All 33 Python runner/inspector tests pass, including life changes, missing samples, repeated dead samples, older recordings, durable export, and missing-tape handling. Applying the inspector to the live descent tape reproduces 11.599 horizontal blocks, 25 Break attempts, and 599 health-20 observations. Automatic generation by a fresh batch remains to be exercised.

### Maintain the search tool through wear (v38)

The v37 recording loses its stone pickaxe at tick 7437 and its wooden pickaxe at 9311. The search task remains active throughout; the first tool rejection arrives only at 9945 after lighting resupply. Search now maintains the declared harvesting capability during execution. If none of its domain-declared tools remains, it suspends into replacement production while retaining the same exploration state, material commitments, and actual return route. The existing return continuation distinguishes lighting repair from tool repair. Failed replacement tries the next declared tool once, excludes ancestor cycles, and terminates when alternatives are exhausted. It does not reset search progress or global work budgets.

All 122 System 1 and 35 evaluator tests pass. New lifecycle tests verify Stop/release before replacement, reserved material preservation, travel back from a repair detour, resumption of the same task/destination, and bounded alternatives. `system-one-tool-wear` starts in the descent chamber with a stone pickaxe that has two uses remaining, a crafting table, three cobblestone, and two sticks. Its checks require the replacement interruption and successful production resume in addition to raw iron. Live results are pending; this change currently maintains tools for Explore, not every excavation or harvesting task.

The first worn-tool run, `20260908-003234-343612-17271/02-system-one-tool-wear`, FAILED in 2357 elapsed ticks with harness OK and normal client exit. All 2260 turns replay exactly. Replacement suspended correctly after two breaks, but Station immediately requested manufacturing a table without a local survey. The dependent log searches then exhausted alternatives in the stone chamber. Automatic `system-one-inspection.json` generation succeeded with complete coverage and explicitly unchecked replay; matching Java replay was checked separately.

v39 surveys locally at workstation height before manufacturing an uncarried, unobserved station. The four-look bound and existing observed-station/reach validation remain. A new test covers discovery before manufacturing and reservation preservation when the bounded survey fails. The tool-wear fixture also had stone directly above its supplied table, making the intended exposed station occluded under the conservative sensor. That one cover block is now air; the ore, two-use tool, replacement inputs, time budget, and behavioral checks are unchanged. This is a corrected supporting-fixture layout, not a pass of the original covered-table layout. Both changes require a fresh live run.

The first v39 test run exposed an existing production simulator that accepted only Craft and Place commands. Its motor now applies Look poses as well, and the production test asserts four survey commands before manufacturing the absent station. All 123 System 1 and 35 evaluator tests pass after that update.

The first v39 launch, `20260908-003834-956989-19230`, stalled while joining the world. Repeated client-thread bridge timeouts caused capture failure, and the client exited via SIGINT (130). There are zero decision turns and no finalized capture; this is a harness failure, not a scenario result. No stack dump established the startup stall's cause.

The fresh retry `20260908-004153-992251-20547/01-system-one-tool-wear` PASSED in 608 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 521 turns replay exactly. At tick 178 tool loss requested Stop; release was acknowledged at 179 before crafting replacement stone pickaxe. Inventory confirmed it at 202, and the same Explore task 3 resumed before eventually obtaining raw iron. Health remained 20 throughout, with 7.488 observed horizontal blocks of travel. The corrected table was already observed by the replacement time, so this proves tool repair/release/resumption, not the new Station survey branch in live execution. The automatic inspection artifact is complete and correctly leaves perception auditing and allowance usage unavailable.

### Prepare access before abandoning an observed resource (v40)

The earlier v37 original recording contains another failure before tool wear: Gather task 60 selected an approach to observed iron at tick 6137, but navigation failed at 6150. It never mined the ore; subsequent generic harvest frontiers led back to the surface, where the task exhausted at 6761. Shared access preparation was restricted to mined drops and remembered return routes. Applying that preparation method to the saved tick-6150 observation selects an eligible stone headroom break at (-2,33,-14), showing a concrete alternative to abandoning the target.

Gather now also requests the bounded Access child when its failed stance can reach a still-observed, eligible unmined resource. Unknown targets and unrelated frontier travel do not authorize preparation. The existing observed-only break checks, one-block steps, preparation budget, parent continuation, and failure limits are unchanged. All 124 System 1 and 35 evaluator tests pass, including a regression that distinguishes observed ore from an unidentified cell. The new `system-one-harvest-approach` fixture puts visible ore across a two-block descent and requires successful access preparation/resumption as well as raw iron. Live validation is pending.

`20260908-004602-806699-22107/01-system-one-harvest-approach` PASSED in 297 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 222 turns replay exactly; its automatic inspection is complete. The first approach failed at tick 128, suspending Gather task 3 into Access child 4. That child observed and cleared a side stair, handled one further unavailable route at 260, and reached its stance at 285. The same Gather task then mined the ore and collected raw iron. Health remained 20, with 9.473 observed horizontal blocks of travel. No larger fall limit was used.

The v38 original run in `20260908-003234-343612-17271/01-iron-pickaxe` remains active at this entry (last verified report: 16632 elapsed ticks). It replaced its stone pickaxe, obtained three raw iron, and smelted two, but subsequent wood/fuel production is spending substantial work on unproductive harvest searches underground. This is ongoing evidence, not a terminal failure or an original sanity pass. Preserve the matching v38 replay classes when it finalizes; current policy is v40. Current targeted repair passes do not satisfy the held-out, migration/deletion, broader survival, or game-completion gates.

### Distinguish supply discovery from estimated work (v41)

The v38 original's tick-12439 observation still contains many remembered birch logs, including (9,67,3), while the player is at (42,31,-4). Fuel selection nevertheless requests acacia planks. The scalar estimate assigns an unseen harvest source a flat cost of 30, making an unobserved species appear cheaper than known wood farther away. This is a source-selection error; the evidence does not support blaming lost tree memory for this choice.

Production estimates now carry source evidence separately from work: known supply, discovery required, or unavailable within the bounded dependency search. Recipe composition preserves the least-supported input status, including through output yields. Selection first prefers a known supply chain, then compares estimated work. Failed methods still fall back through existing bounded alternatives. Meaningful reconsideration can replace discovery with a newly observed source at completed observation/travel boundaries. This is a conservative deterministic policy, not proof that the known source is reachable or always globally cheaper.

Applying the old and new pure decisions to the same saved tick-12439 observation selects acacia planks under v38 and birch planks under v41, respectively. These are offline choice probes, not physics replay. All 126 System 1 and 35 evaluator tests pass. New tests cover distant remembered fuel, unidentified-source exclusion, fallback after a known route fails, reservations, and preventing large yields from hiding a discovery prerequisite.

`system-one-remote-fuel` exposes oak at a forty-block cave entrance and a furnace near its underground ore. No fuel is supplied. It checks an iron ingot, the oak-plank fuel dependency, and completed oak harvesting. No fixture coordinates are passed to the agent. Live navigation, harvesting, and smelting verification are pending.

The v38 original finalized FAILED in 19949 elapsed ticks (`command_budget_exhausted`), harness OK, zero LLM calls, fully released motor, normal client exit. Its complete tape contains 19914 turns; the matching decision replay is still running at this entry, so exact replay is not yet claimed. Final inventory retains two iron ingots, one raw iron, a replacement stone pickaxe, and seven torches. Health stayed 20 throughout. It accumulated 1302.256 observed horizontal blocks of travel, much of it during unproductive wood-source searches. The first failed child at tick 8118 was spruce-wood acquisition for tool replacement; subsequent alternatives did obtain the replacement. The later acacia fuel search consumed the remaining global command budget. The original sanity gate remains failed pending a fresh current-policy run.

`20260908-005750-375582-25500/01-system-one-remote-fuel` PASSED in 1131 elapsed ticks, harness OK, zero LLM calls, fully released motor. All 1034 turns replay exactly, and the automatic inspection is complete. The first fuel dependency at tick 496 was oak planks while the player stood at (0,196,56), near the far furnace. Oak harvesting completed at tick 760 near the entrance, followed by fuel production and travel back to the furnace to obtain an iron ingot. Recorded travel was 112.474 horizontal blocks; health stayed 20. The Recorder Play finalized, but the harness needed SIGINT after its shutdown wait and the client exited 130. This proves the resource trip and recording; it does not prove graceful client shutdown or visual playback.

The long v38 original replay has now completed successfully: all 19914 decisions match, ending in `command_budget_exhausted`. The earlier pending qualification is resolved. The v41 original rerun is active in `20260908-005901-434777-25993/01-iron-pickaxe`.

### Persist policy state while a command remains active (v42)

A delayed-feedback regression found a lifecycle gap in lighting maintenance. If an active movement first enters reduced light while resupply is forbidden by its dependency ancestry, the policy creates a scoped allowance but returns Keep without retaining it. Every subsequent dark tick can consequently create a fresh deadline. The new test reproduced this: at the original tick-82 expiry, v41 issued no Stop.

The generic Keep decision can now carry updated domain state while preserving the command token, phase, feedback, task identity, and global budgets. Explore uses it to retain the lighting assessment during active work. No new actuator or scheduler is introduced. The regression now stops the same command at tick 82, and a synthetic kernel test verifies state refresh without changed ownership or lost feedback. All 128 System 1 and 35 evaluator tests pass. This is a source/unit proof with delayed feedback, not yet a live injected-delay result.

The v41 regression batch `20260908-010342-791622-27450` finalized with charcoal PASSED (659 elapsed ticks), blocked return PASSED (2642), and tool wear FAILED (2197). All three harnesses are OK with normal client exits. Matching replays and the new tool-wear failure diagnosis are pending; the earlier v39 tool-wear pass does not establish current reliability.

### Repair a failed exploration step before choosing another direction (v43)

The v41 tool-wear tape replays all 2121 turns exactly. Replacement succeeded and the same Explore task resumed. Its prepared downward move then failed `observed_route_unavailable` at tick 246. Search abandoned that descent and moved horizontally, shifting the tunnel above the fixture ore before eventually exhausting alternatives. The charcoal and blocked-return runs in that batch also replay exactly (580 and 2566 turns respectively).

Explore now suspends a failed movement into the same bounded Access child used by harvesting and return travel. It preserves the destination, task continuation, reservations, and remembered route. Access does not immediately repeat an unchanged failed direct move, but can still observe or clear an eligible obstruction on that edge before retrying it. A failed Access child returns to bounded search alternatives rather than recursively requesting itself. All 130 System 1 and 35 evaluator tests pass, including obstruction preparation and unchanged-movement rejection. Live regression is pending; Access still lacks the full maintained-lighting coverage required by the final design.

The v41 original run `20260908-005901-434777-25993/01-iron-pickaxe` finalized FAILED in 19516 elapsed ticks with `command_budget_exhausted`, harness OK, released motor, and normal client exit. All 19492 decision turns now replay exactly (the earlier row count also included the footer). Later wood searches again consumed substantial work; their initiating failure still requires diagnosis. The original sanity gate remains unproven for the current policy.

The v43 regression batch `20260908-012314-371229-31961` PASSED descent (539 elapsed ticks, 474 replayed turns), lighting exhaustion (411, 345), and tool wear (555, 487). All three have harness OK, zero LLM calls, released motors, normal client exits, and complete automatic inspection. Exact replay was verified separately. The tool-replacement child starts at tick 121; the access repair later in that run belongs to Gather. These runs establish current integration passes, but do not independently reproduce the specific failed Explore movement seen in v41 or inject delayed lighting feedback.

### Keep distant approach goals while planning local work (v44)

The v41 original exposed iron at tick 5628, but its selected approach was roughly ten horizontal blocks away. Two navigation failures immediately produced `access_goal_out_of_range`; Gather then left the ore and later exhausted its harvest search. The six-block local search radius had inadvertently become a maximum distance for the entire access task.

Access now retains its ultimate destination and searches a six-block horizon around the current observed position. For a distant destination, it can select a reached horizon frontier that makes geometric progress, ranked by the same deterministic path estimate. Every next step still passes the existing observation, clearable-block, protected-footing, and hazard checks. The 512-expansion per-decision limit, 48-command total, and original 1000-tick deadline remain. There is no larger Baritone fall limit or hidden target scan. This is bounded local heuristic planning; it can still fail when no useful horizon frontier is found.

All 131 System 1 and 35 evaluator tests pass. A twelve-block corridor regression requires clearing an observed head obstruction and reaching the original destination across multiple horizons; the original deadline still expires after progress. A pure probe against the recorded tick-5641 observation now selects a neighboring navigation step instead of rejecting the distant goal. This probe is not live execution. The new `system-one-distant-approach` fixture extends the existing lit lower-chamber geometry with ore twelve blocks away and requires completed access preparation plus raw iron. The existing near fixture and original iron-pickaxe scenario retain their geometry and checks. Live validation is pending.

`20260908-012935-310142-33536/01-system-one-distant-approach` PASSED in 332 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 265 turns replay exactly and automatic inspection is complete. At tick 104, failed navigation from (0,200,15) suspended Gather into Access. It observed and prepared the ledge, then advanced to the original approach (0,198,26) at tick 306. The same Gather task resumed and broke ore at (0,198,27). The trace issued six Break commands, traveled 16.247 observed horizontal blocks, and maintained health 20. This proves a live approach beyond one six-block planning horizon. It does not prove broad terrain reliability or visual Recorder Play playback. The unchanged original v44 run is active in `20260908-012939-903912-33602/01-iron-pickaxe`.

### Preserve a resource trip across productive motor legs (v45)

The v41 original's birch supply trip starts at tick 11753 from (5,16,-48) toward the observed approach (9,67,-11). At 11862 the motor reaches its travel budget after moving to (5,25,-32). Gather marked the destination visited and switched to another stance despite substantial progress. Repeated stance rejection can exhaust a long supply trip even when the route is making progress.

Gather now records the observed origin of each issued command. After `navigation_budget_exhausted`, it can continue the same destination if the observed position is at least half a block closer, the destination remains a safe observed stance, and a still-identified resource remains reachable from it. A stalled/regressing leg, unavailable route, vanished resource, or absent command-origin evidence does not qualify. Motor and root budgets are unchanged; this is not unlimited navigation or a retry of every failure. Drop-only pickup and generic survey travel retain their existing recovery.

All 133 System 1 and 35 evaluator tests pass. New tests cover productive continuation and refusal after stalled/regressing movement, route unavailability, and disappearing resource evidence. A pure probe on the saved tick-11862 observation now retains (9,67,-11), while the recording selected (9,67,-10). This is decision-level evidence; the live remote-fuel regression is pending. The running v44 original retains its loaded policy and matching saved classes.

`20260908-013638-202020-35127/01-system-one-remote-fuel` PASSED under v45 in 1076 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 1010 turns replay exactly; automatic inspection is complete. At tick 572, a budget-limited leg ends at (0,196,32), and the same Gather task retains its destination (0,197,18). It completes wood supply and returns to smelt an ingot. This exercises the new continuation in live execution. Observed horizontal travel is 112.034 blocks and health stays 20. A read-only live screenshot (`live-lighting.png` in that run directory) was visually inspected: placed wall torches illuminate the tunnel walls and floor. This is one live frame, not full Recorder Play playback or all-run lighting validation.

The unchanged v44 original `20260908-012939-903912-33602/01-iron-pickaxe` FAILED in 7185 elapsed ticks with harness OK, zero LLM calls, released motor, and normal client exit. All 7157 turns replay exactly. It retains three raw iron, a stone pickaxe, one spruce plank, and four torches. The first failed child is `smelt_start_failed:furnace_not_observed` at tick 7213; recipe alternatives then exhaust. At 7210 Station requests completion while navigation is active and the player is jumping. Release is acknowledged at 7211 and StartSmelt targets the remembered furnace (-16,70,1); its live ray check fails next tick. Station currently tests transient reach before `view.acting()`, so it can end an approach mid-jump. That premature completion is a concrete next repair candidate; the recording alone does not establish which exact ray was occluded, and no fix or fresh live proof for it is claimed here. All evaluation processes from this checkpoint are terminal.

### Finish workstation approach before testing reach (v46)

A focused regression reproduces Station returning Complete from a reachable furnace while its navigation command is active. The task now waits for the active command to finish before testing workstation reach. This prevents transient visibility during a jump from ending the approach early. Command ownership and motor arrival rules are unchanged; this does not claim to reconcile arbitrary furnace contents or recover all interaction failures.

All 134 System 1 and 35 evaluator tests pass. The new `system-one-station-stairs` fixture supplies raw iron and coal, with a furnace above four stair rises. Its checks require an observed ingot and completed navigation. Live validation and a fresh original run are pending. The existing original scenario remains unchanged.

The initial stair-fixture launch `20260908-014332-662858-36883` did not exercise the intended approach: the furnace was hidden behind the stair crest and the fixture inherited the default downward starting view. It began manufacturing another furnace instead. The supporting fixture now exposes the furnace beside the upper landing and starts looking toward it; inventory, goal, navigation-completion check, and budget remain unchanged. This is a correction to isolate the approach case, not a claimed pass of workstation discovery in the earlier occluded layout. Evaluator compilation and all 35 evaluator tests pass after the correction. The original scenario is unmodified.

### Use access preparation for known workstations (v47)

The corrected v46 fixture `20260908-014700-938488-37827` identifies the furnace in its first observation. Its known upper approaches all fail `observed_route_unavailable`, because lower stair geometry has not yet been observed. Station rejects eight stances and starts manufacturing another furnace. Unlike Gather and Explore, it did not invoke shared local access preparation.

Station now suspends into Access after a failed approach that can reach a still-identified workstation. It retains the original Station task, reservations, and rejected stances. Access uses the domain's declared excavation materials and existing observation/footing/hazard rules; unknown workstations do not authorize preparation. A failed child returns to the existing bounded alternatives. All 135 System 1 and 35 evaluator tests pass, including task preservation, unknown-target exclusion, and no recursive re-entry after a failed Access child.

The two v46 stair attempts were deliberately interrupted by the agent after their diagnostic failure paths were established, rather than waiting through irrelevant wood searches. Neither is a terminal scenario failure or pass. Preserve their harness interruption/finalization evidence separately; the v46 original was not stopped. A v47 run of the corrected fixture is pending.
