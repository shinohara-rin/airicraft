# System 1 implementation evidence

Tracks the [implementation plan](superpowers/plans/2026-09-06-system-one-redesign.md). The full redesign remains in progress. A passing stone fixture does not establish completion of the iron-pickaxe or game-completion gates.

## Implemented so far

- `TaskKernel`: immutable inputs/results and bounded transitions; child tasks retain parent continuations; commands have session/run/task/attempt identity; replacement work waits for acknowledged release. Synthetic-domain tests cover repair/resumption, stale feedback, cancellation, alternate-method recovery, budgets, and replay.
- Opt-in replacement entrance: `scripts/run-evaluation-scenarios --system-one` implies no-LLM mode. Its tick path bypasses the legacy execution/reflex loop. Structured inventory goals enter reactive production tasks.
- Geometric perception: bounded rays stop at the first occupied voxel, do not cross occluding corners, hide unlit identities, and restrict dark geometry to the near field. This is a geometric sensor, not screenshot recognition or proof of human-equivalent visibility.
- Baritone terrain boundary: block-state interfaces capture immutable observed terrain; hidden live/cached cells are replaced by a non-traversable, non-supporting sentinel. Execution-time direct reads also use observations. Navigation cannot issue attack/use input, perform mining/placement preparation, or select buckets. Scoped settings disable independent avoidance, inventory/tool selection, parkour, sprint, and unloaded-goal simplification. See the [source audit and live probe](system-one-baritone-boundary.md); paired autonomous playthroughs and broader terrain variants remain required.
- Exact breaking aims at and ray-checks the target face, verifies reach/tool eligibility, and avoids breaking the player's own footing. Soil excavation prioritizes lower exposed surfaces and preserves tools when an empty hand is equally effective.
- Excavation surveys on demand rather than performing four quarter-turns after every move. This correction follows the user's live observation of repeated spins.
- Decision capture records an initial mission plus every tick's observation delta, motor feedback, cancellation, effects, and lifecycle events. A bounded background writer reports overflow/storage failure as incomplete; evaluator cleanup waits for motor release and writer completion. Offline replay rejects missing turns, incompatible versions, altered decisions, and missing/incomplete footers.

- Production tasks load crafting patterns from the active recipe catalog, preserve parent ingredient reservations, account for recipe yields, and terminate prerequisite cycles. Observed resource sources affect recipe choice. Child tasks harvest logs, bootstrap tools, acquire/place tables, and return toward remembered workstations. Crafting, exact placement, and inventory selection stay under the motor owner; crafting release drains the cursor and input grid. Production recordings include the complete immutable recipe/prior catalog and support offline replay.

Current development revision (2026-09-08, policy v83 / motor v15 / knowledge v13): 223 System 1, 38 evaluator and 50 Python tests pass. Identified observations now carry immutable registry block-state properties. Timestamp refreshes preserve them; dark observations strip them. Navigation reconstructs state solely from those recorded values and the active registry; missing, extra or invalid properties and unknown identities become barriers. The raw sampled-state map and separate sensor terrain cache are deleted. Production wire format 5 / stone format 4 require property objects and reject numeric/boolean coercion. Historical tapes remain inspectable; exact replay requires their saved classes. Historical decision-only fixtures explicitly omit properties and do not claim navigation replay. Live registry round-trip, immutable-view and Baritone consumption checks are prepared in the terrain fixture; verification is pending. This closes a recording boundary gap, not the full hidden-read or human-equivalent-vision audit. The original iron-pickaxe run `20260908-153346-786024-54130` remains on its already-loaded v82 build.

Historical checkpoint (2026-09-08, policy v82 / motor v14 / knowledge v13): 220 System 1, 38 evaluator, and 49 Python tests pass. A recorded repair result now transitions to ordinary `ReturnWork`, which preserves the interrupted work and observed route. Completed repairs no longer carry their timer or health baseline through return travel. Return movement remains bounded by leg limits, route progress/rejection limits and the mission budget; normal lighting maintenance may interrupt it, with release acknowledgement before resumption. The original 2,400-tick repair budget still applies to unfinished repair work. The first 220-block fixture exhausted its three search areas before reaching depletion (`20260908-150921-161186-48530`, FAILED, 2,187 exact turns). It does not exercise the repaired lifecycle. The revised fixture uses a 160-block corridor, removes torches after 128 blocks, and applies Slowness IV after supply completion to extend return travel past the old deadline. Agent search and repair budgets remain unchanged; the revised fixture passes as recorded below.

The [v82 regression batch](evaluations/system-one-v82-regressions.json) `20260908-151030-752657-48897` completed: injury terminated as intended, transient-light recovery and furnace waiting passed. The blocked-return case acquired raw iron but FAILED its lifecycle assertions in 623 ticks: depletion eight blocks before the ore allowed completion without resupply, so no wall was injected. Its retry removes torches after 32 of 60 blocks to leave a meaningful unlit stretch. These fixture failures are retained; inventory success is not return-repair evidence. All four tapes replay exactly (injury 6 turns, region 176, wait 211, blocked 555), with complete capture, harness OK, zero planner/LLM calls and normal client exits. Injury stopped at tick 113 and released at 114 without subsequent Starts. Region recovery preserved task 3, moved from Z22 to Z18, stopped at 161 and released/resumed at 162. Furnace waiting resumed task 3 at 98 with its original deadline 489.

[The v82 blocked-return retry](evaluations/system-one-v82-blocked-return.json) `20260908-152426-956497-52133/01-system-one-return-blocked` PASSED in 2,588 elapsed ticks with 2,518 exact decisions, complete capture, harness OK, zero LLM/planner calls and client exit 0. The injected wall appeared after supply completed at 2126. Its upper block was observed at 2272, broken at 2291, and observed empty at 2308; player feet crossed beyond the wall at 2325. Access returned to exploration task 3, which resumed its original search at 2452. This proves changed-route recovery for this fixture; it did not cross the former repair deadline. [The slowed-return case](evaluations/system-one-v82-long-return.json) PASSED in 4,444 elapsed ticks with 4,376 exact replay turns. Return started at 2416, remained Acting across the old repair deadline 3494, and resumed exploration task 3 at 3932. Capture completed, harness OK and zero planner/LLM calls. The shorter return is rejected by the deadline-crossing audit as a negative control. Client shutdown required SIGINT (130), so this is not graceful-shutdown evidence. This verifies lifetime separation in the controlled fixture; original iron-pickaxe integration still needs a fresh run.

Historical checkpoint (2026-09-08, policy v81 / motor v14 / knowledge v13): 218 System 1, 38 evaluator, and 49 Python tests pass. Pickup can request an Access child for a nearby item stance with observed floor support and blocked or unresolved headroom. The child retains pickup's deadline and respects hazard/material/support exclusions. It requires actual inventory evidence after access completes. [Live pickup/access evidence](evaluations/system-one-v81-pickup-access.json): the headroom fixture PASSED in 105 ticks with 37 exact replay turns, and the long-return rerun PASSED in 2,600 ticks with 2,531 exact replay turns. Both have harness OK, zero planner/LLM calls, released motors, complete captures and normal client exits. In the focused fixture, pickup task 3 requested access task 4, which broke the observed ceiling, approached the item and ended only after raw iron appeared in inventory. The long-return audit preserves exploration task 3 across resupply and resumption at tick 2504.

[The v80 regression run](evaluations/system-one-v80-regressions.json) verified gathering, furnace-wait and injury behavior. Its long-return scenario completed resupply and resumed exploration, then FAILED at pickup (4,102 elapsed ticks): the observed raw-iron drop rested beneath a known stone ceiling. Pickup twice inspected the already-known floor and reported `item_pickup_stance_unobserved`; subsequent search abandoned the drop. v81 addresses and live-verifies that seam. The v79 original iron-pickaxe run FAILED in 21,394 ticks with `lighting_repair_budget_or_health:retreated` in its search branch. [Exact replay of the original run](evaluations/system-one-v79-original.json) verifies 21,374 turns: the fixed 2,400-tick lighting repair deadline expired on the return leg at observed light 14, after torch production. Repair work and return travel need separate lifetime handling; v80 prospective-region recovery does not establish a fix for that timeout.

Historical checkpoint (2026-09-08, policy v80 / motor v14 / knowledge v13): 217 System 1, 38 evaluator, and 49 Python tests pass. A prospective ordinary dim-region rejection now retains the interrupted task in a bounded `RegainLight` continuation, retraces the observed mission route, and resumes only after observing working light. Resumption and timeout wait for motor release. A stale refuge is a failure, and completed child failure evidence is preserved at the mission boundary. Existing explicit health revocation and supply-repair region limits remain unchanged. [The transient-light corridor fixture](evaluations/system-one-v80-light-region.json) PASSED in 249 elapsed ticks with harness OK, zero planner/LLM calls, a released motor and normal client exit. Exact replay verifies 179 turns: gathering task 3 requested recovery at tick 134, moved from Z22 back to Z18, stopped retreat at 153, then released and resumed at 154. The evaluator restores the failed light only after the agent regains observed working light. No screenshot review is claimed for this fixture. The separate v79 original iron-pickaxe run subsequently failed as recorded above.

Historical checkpoint (2026-09-08, policy v79 / motor v14 / knowledge v13): 213 System 1, 38 evaluator, and 49 Python tests pass. Attachment support is now an immutable observed value separate from collision support. Minecraft derives full-face and upper-center attachment shape without querying neighboring world cells; unidentified observations carry no attachment capability. Torch selection requires the corresponding face capability. Production tape format 4 and stone tape format 3 preserve it through timestamp refreshes; historical replay needs matching saved classes. [Live attachment evidence](evaluations/system-one-v79-attachment.json): the clean leaf-wall rerun PASSED in 268 ticks with harness OK and 200 exact replay turns; task 3 interrupted at tick 93, released at 94 and resumed at 99 after placing one floor torch on bedrock. All 33 identified leaf cells carried no attachment capability. The terrain fixture PASSED in 424 ticks with harness OK and 352 exact replay turns; its adapter probe distinguishes leaf, stone and bottom-slab attachment shapes while retaining paired hidden-layout navigation checks. Both runs had zero planner/LLM calls and released motors. The leaf-wall client exited normally (0); terrain gameplay and recording completed, but client shutdown required SIGINT (130), so graceful process shutdown is not proven for that run. An earlier gameplay screenshot shows the floor torch between leaves. The first attempt's harness rejected the new tape format; the clean rerun follows the inspector correction.

Original v78 run `20260908-135744-599076-30254/01-iron-pickaxe` FAILED in 16,120 elapsed ticks, with zero planner turns. All 16,095 decision turns replay exactly using saved v78 classes. Near its end, torch attempts 733–736 chose spruce-leaf walls and timed out; Minecraft leaves have full collision but an empty attachment shape. A focused regression reproduced that choice before the v79 selection fix. At tick 16147 the resumed search's return command was rejected by the reduced-light region check, producing `lighting_repair_region_exhausted` and exhausting the raw-iron method. Correcting support selection does not yet prove recovery from that separate prospective-region rejection. Full navigation-state recording remains open as well.

Historical checkpoint (2026-09-08, policy v78 / motor v13 / knowledge v13): 211 System 1, 38 evaluator, and 48 Python tests pass. Lighting state and repair priority have moved from Explore to the mission, with one persistent allowance across ordinary child tasks and a separately bounded light-restoration window. v77 live gathering, furnace-wait and dim-bootstrap fixtures pass and replay; [mission-lighting evidence](evaluations/system-one-v77-mission-lighting.json) verifies stable task identity, release ordering and the original cooking deadline. v78 retains the observed supply route across lit waypoints and avoids attaching torches to blocks needed by suspended work. Its [live regression evidence](evaluations/system-one-v78-mission-lighting.json) verifies bootstrap, gathering, furnace waiting, long cave resupply and prompt injury termination. Original and development-02/03 successes remain v72, development-01 v75. Full perception audit, broader maintained-condition validation, frozen held-out release, cutover and game completion remain open.

The generic lifecycle, fair observation/motor boundary, production, exploration, lighting repair, tool replacement, local access preparation, and limited lava/death recovery have working implementations and specific evidence below. Maintained conditions across every task type, broader survival, remaining injected-failure and visual-recording gates, held-out evaluation, cutover/deletion, and multi-seed autonomous game completion remain unfinished. The opt-in replacement is still a temporary migration entrance.

## Tests and live evidence

Focused command:

```bash
source .envrc
./gradlew :test --tests 'ai.moeru.airicraft.systemone.*'
```

Initial 2026-09-07 checkpoint (historical): 56 tests passed: 11 kernel, 6 geometric sensing, 13 acquisition and 1 observed-reach, 9 production, 6 decision replay, 3 recording tests, and 7 smelting-production tests. The 32 evaluator tests and evaluation-launcher Python suite also pass (24 launcher tests). Five recording-inspector tests cover old-policy diagnosis and incomplete input detection.

Live fixture command:

```bash
source .envrc
scripts/run-evaluation-scenarios --system-one --scenario system-one-stone \
  --recorder-jar /absolute/path/to/recorder-profile.jar \
  --stop-client-after-scenario --bridge-timeout-seconds 240
```

The evaluator prepares a disposable platform: stone underneath three soil layers, with one wooden pickaxe supplied. Fixture construction executes on the integrated-server thread before goal submission. Setup state and coordinates are not supplied to the agent. This fixture deliberately isolates acquisition; it does not replace the original iron-pickaxe scenario. Use `--scenario system-one-production` for the empty-inventory version with an observed log column and a stone-pickaxe goal. That fixture supplies no tools or ingredients directly to the player.

| Run directory under `eval-output/` | Evidence |
| --- | --- |
| `20260906-195642-628152-51720` | First look/break succeeded; first navigation crashed because one Baritone constructor overload had not captured its terrain view. Corrected the constructor selector. |
| `20260906-195834-754333-53801` | Interrupted rerun exited before a terminal scenario report. No gameplay success claim. |
| `20260907-154110-841283-61691` | Navigation worked, but excavation widened the surface and wore out the pickaxe. The fixture incorrectly gated evaluator ticks on remaining at the starting height, causing recording to stop after descent. Recovered 485 remaining System 1 events from the live event buffer; earlier events were truncated. Gracefully stopped the client. |
| `20260907-154739-926189-63272` | Obtained three cobblestone in 446 elapsed ticks; scenario PASSED and harness OK. Recorder Play finalized. This run still performed routine panorama scans; the follow-up run tests their removal. |
| `20260907-155134-120434-64304` | Obtained three cobblestone in 376 elapsed ticks with zero survey-look commands. Scenario PASSED; harness OK; Recorder Play finalized. `system-one-final.json` confirms SUCCEEDED and no remaining motor command, pathing process, or pending release. Planner journal is empty and final LLM records are empty. |
| `20260907-160438-513978-67297` | Obtained three cobblestone in 376 elapsed ticks; scenario PASSED and harness OK. Decision recording completed with 312 input rows. Offline replay verified all 311 turns, matching effects, lifecycle events, and SUCCEEDED inventory evidence. |
| `20260907-162016-195995-72174` | Live startup caught an incorrect overload selected when remapping the passability hook. Client failed before gameplay; corrected the selector to an explicit descriptor. |
| `20260907-162237-150390-72866` | Live adapter probe held its authorized corridor fixed while changing 102 hidden cells. All 132 movement results, the A* route, and cost matched. Subsequent autonomous stone goal PASSED in 388 ticks with zero planner turns. |
| `20260907-162605-529498-73805` | Final probe also checks placement rejection and bucket-selection isolation. Paired route/cost checks passed; autonomous stone goal PASSED in 382 ticks, harness OK, zero planner calls, and motor fully released. All 312 decision turns replayed offline. |
| `20260907-164921-022470-78966` | First production attempt chose acacia ingredients despite observed oak. Saved events identified the incorrect cost estimate; stopped the run and added an observed-resource preference regression. No terminal success claim. |
| `20260907-165313-281422-80092` | Harvested logs, crafted the table and wooden pickaxe, and obtained three cobblestone. Failed because a nearby remembered table was occluded. Replay reproduced all 693 decision turns and the failure. Added station visibility/return logic and placement body-clearance checks. |
| `20260907-170343-610028-83181` | Return attempts failed: excavation had removed earlier footholds. The fallback created another table but could not place it from the shaft. Scenario FAILED in 824 elapsed ticks; zero planner turns. This exposes an acquisition defect that the stone-only fixture did not exercise. |
| `20260907-171617-714595-86198` | Protecting visited footholds alone caused surface widening: walking onto each cleared cell immediately protected the floor that needed excavation. Stopped this repetitive run and corrected the step sequence. |
| `20260907-171921-268801-87086` | The corrected sequence descended two steps and preserved footholds. Later descent failed because the destination ceiling blocked entry despite two-block standing clearance. Recorded observations identified the obstructing grass block. All 967 turns replayed with policy v3 at checkpoint `dc70f25`. Added a retained step destination and overhead preparation. |
| `20260907-172346-877489-88255` | Empty-inventory stone-pickaxe goal PASSED in 683 ticks, zero planner turns. Cleanup overwrote pending success with cancellation; all 618 turns reproduced that race offline. Corrected cleanup to preserve terminal decisions while waiting for release. |
| `20260907-172709-641844-89225` | Final production verification PASSED in 690 elapsed ticks; harness OK, zero planner/LLM calls, runtime SUCCEEDED, motor fully released, client exited normally. All 628 decision turns replayed offline. There were 16 break commands, 7 crafts, 7 navigations, 1 placement, and zero survey turns; one occluded target recovered. |
| `20260907-174348-876499-93376` | First exposed-iron attempt FAILED before motor actuation: resource cost estimation preferred an unavailable diamond tool. Cyclic block-compression recipes divided a finite unavailability penalty into an apparently cheaper cost. The 8 recorded decision turns replay exactly with production policy v5. |
| `20260907-174947-087516-94884` | Tool progression correction allowed stone-pickaxe crafting and raw-iron harvesting. Furnace supply then exhausted excavation alternatives despite nearby previously observed stone. Production policy v6 records the failure and bounded cleanup; the next correction adds approaching observed stone before another excavation. |
| `20260907-175633-378080-96539` | Collected furnace stone but could not return: later excavation could remove footholds belonging to earlier tasks. Stopped after repeated return attempts. Added session-scoped foothold memory, recorded and replayed with observations. |
| `20260907-180207-162291-97887` | Routes and resource production progressed with no motor failures. All three exposed iron ores were mined, but pickup chose the player's lower height instead of the drop's floor. Search eventually exhausted its budget. Production policy v8 preserves this failure; drop-height recovery is next. |
| `20260907-181125-003484-138/01-system-one-iron` | Pickup correction collected raw iron. Furnace supply then took a two-block descent from Y196 to Y194, leaving no climbable return route. Failed in 1753 elapsed ticks, harness OK, zero planner calls, motor released. All 1677 turns replayed with production policy v9. |
| `20260907-181125-003484-138/02-system-one-smelting` | Focused fixture loaded raw iron and plank fuel into an observed furnace, waited without owning the motor, and collected an ingot. PASSED in 282 ticks, harness OK, zero planner calls; all 212 turns replayed. Cleanup still overwrote success while a child released the motor, leaving runtime CANCELLED. This fixture isolates furnace interaction and does not prove the iron-pickaxe chain. |
| `20260907-182610-738023-4574/02-system-one-smelting` | Root goal observation now ends the whole branch after motor release. PASSED in 293 elapsed ticks, runtime SUCCEEDED, zero planner calls, motor fully released. All 219 turns replayed with production policy v10. |
| `20260907-182610-738023-4574/01-system-one-iron` | One-block descent and intermediate mining stances allowed climbing from Y194 back to Y198, crafting a furnace, and smelting one ingot. Furnace placement at (-1,199,18) then occupied a used stair cell; subsequent surface navigation failed and raw-iron search exhausted. FAILED in 2621 elapsed ticks, zero planner calls; all 2545 turns replayed. Placement must preserve passage clearance as well as excavation preserving supports. |
| `20260907-183337-793482-6614/01-iron-pickaxe` | Original frozen scenario, no fixture supplies: harvested spruce, crafted a wooden pickaxe, excavated stone, and crafted a stone pickaxe. Exposed-iron search then exhausted; underground resource exploration remains missing. FAILED in 1827 elapsed ticks with zero planner calls. All 1805 decision turns replayed with production policy v11. |
| `20260907-183337-793482-6614/02-system-one-iron` | New placement avoided the used route, but wooden-pickaxe crafting failed at tick 429 with `crafting_cursor_occupied`. The adapter issued three predicted clicks per ingredient and treated an occupied cursor on the next tick as terminal. Stopped the ensuing alternative-wood searches through the isolated client-stop endpoint. No terminal fixture pass or complete replay claim. |
| `20260907-184153-125742-8728/01-system-one-iron` | Empty-inventory full production fixture PASSED in 2164 elapsed ticks, zero planner calls, harness OK, runtime SUCCEEDED, motor fully released. Crafted tools, excavated stone, harvested exposed ore, placed a furnace outside the used passage, smelted three ingots, and crafted the iron pickaxe. All 2086 decision turns replayed. Motor v7 waits for the cursor and recipe slots to settle between ingredient transfers. This is fixture proof, not a pass of the original terrain scenario. |
| `20260907-184153-125742-8728/02-system-one-production` | Concurrent stone-pickaxe regression PASSED in 819 elapsed ticks, zero planner calls, runtime SUCCEEDED. All 747 decision turns replayed. Both clients exercised crafting under concurrent live load without the earlier cursor failure. |

The failed recording is a concrete recorder/harness finding: an artifact existing on disk does not prove that recording continued through gameplay. Setup completion is now latched once, and post-outcome recording includes motor release before cleanup finishes. The final production Recorder Play was rendered successfully to 676 frames and `recording-render/fpv.mp4`. Sampled first-person frames were visually inspected through log harvesting, excavation, cobblestone collection, and return toward the table. The renderer covers its available server ticks 28–703, versus requested 28–724, and reports 314 ignored unsupported packets (310 chunk unloads, 4 player positions). Its images are supporting evidence; they do not independently prove final inventory or exact runtime-tick alignment.

The successful exposed-iron fixture's Recorder Play also rendered 2304 frames at 960×540 and 20 fps. Inspected frames 400, 900, 1350, 1600, 2200, and 2300 show stone/ore mining and furnace/table interactions. The final inventory is not shown in those frames; the evaluator and replayed inventory observation establish the iron-pickaxe result. Render coverage is global ticks 18–2321 versus requested 18–2341, with 238 skipped unsupported packets (234 chunk unloads, four player positions). These are explicit external playback limits, not proof of complete tick alignment.

Replay the recorded live decision inputs (no Minecraft client required):

```bash
./gradlew replaySystemOne \
  -Pairicraft.replayFile="$PWD/eval-output/20260907-172709-641844-89225/01-system-one-production/system-one-decisions.jsonl.gz"
```

This verifies the deterministic decision boundary; it does not replay physics or prove sensor fairness. The replay entry point reads both stone and production records. Policy revisions must match the recorded decisions; historical failures should be replayed from their implementation checkpoint.

Inspect any historical policy's recording without launching Minecraft:

```bash
scripts/inspect-system-one-recording \
  eval-output/20260907-171921-268801-87086/01-system-one-production/system-one-decisions.jsonl.gz
```

The compact report includes inventory changes, command counts, failure reasons, the first failed prerequisite chain, and the last failed command's parent chain. Input coverage is checked separately from decision replay; truncated compression, missing/reordered turns, missing footers, and trailing records report incomplete coverage. The inspector never claims policy replay.

## Outstanding plan gates

Underground search and a separate lighting policy are implemented experimentally in production policy v12. Search uses observed cave floors and individual exposed excavation surfaces; geological priors supply only a preferred depth and work bounds. Lighting has hysteresis, child-task supply/placement, return continuations, and a time/region allowance after repair failure. The perception lens is unchanged. Seventy-two System 1 tests and 33 evaluator tests pass, including repair ownership and preserved destinations. Evaluator event checks now support exact payload predicates, so the cave fixtures require supply/placement resumption evidence as well as raw-iron inventory.

The first live cave batch, `20260907-190840-236654-14858`, failed search in both fixtures. `system-one-lighting` entered the cave, requested supply at tick 152, crafted eight torches, placed one, observed restored working light, and resumed exploration at tick 172. The supplied-torch fixture also placed light and resumed. Both reached the cave's end before lateral exploration changed their heading and led excavation into side walls. Final results were FAILED at 960 and 945 elapsed ticks, with zero planner turns. These runs prove the local supply/placement cycle, not successful underground acquisition or the full away-from-cave resupply gate.

The v12 lighting and underground tapes replay all 886 and 865 turns. Policy v13 keeps the search heading across lateral movement; motor v8 requires grounded arrival in the requested destination block. Batch `20260907-191513-735706-16562` still failed at 974/964 elapsed ticks: preferring any unvisited floor caused backtracking from the cave's end to its entrance before excavation. Recorded positions isolate the remaining frontier-ranking defect; the supply/placement/resume cycle continued to work.

Policy v14 prevents backward floor selection from replacing forward search. Batch `20260907-192007-336591-17947` PASSED both cave fixtures: lighting supply in 303 elapsed ticks and supplied-torch exploration in 279, zero planner calls, runtime SUCCEEDED, and all motor control released. The lighting fixture's payload checks confirm supply suspension, acquisition of eight torches, and resumption after observed working light. Ore first becomes identified at ticks 288 and 256, after exploration opens the tunnel end. These are local-crafting and underground-discovery proofs; running out partway through a longer cave, away-from-cave supply, charcoal bootstrap, and hazard/changed-route cases remain separate gates.

1. Extend the verified live decision recording/replay to subsequent domain methods; expose accurate runtime/version diagnostics and finish lifecycle ownership on reload/world changes.
2. Finish stone variants, paired autonomous hidden-layout runs, and controlled travel/work/perception metrics. The source audit, adapter-level paired-world probe, and survey correction now have live evidence.
3. Exposed-iron production and passive smelting now have a complete live fixture pass. Extend quantity/fuel accounting beyond one-input batches, test charcoal alternatives, and add underground iron acquisition for the original scenario.
4. Implement exploration continuations, maintained lighting, resupply children, bounded relaxation, and survival preemption through the same motor owner.
5. Pass the original iron-pickaxe scenario and frozen development/held-out evaluations; extend injected-failure diagnosis and visual Recorder Play review to those behaviors.
6. Migrate remaining entry points and delete the old execution ownership and temporary selector.
7. Add and evaluate the capabilities needed for autonomous Minecraft completion across multiple seeds.

The new long-cave `system-one-lighting-exhaustion` fixture starts with one torch, two coal, and two sticks, with a 24-block passage. Batch `20260907-193145-189244-21203` consumed the carried torch before supply (placement at tick 157, supply at 200), crafted eight, and repeatedly restored light while preserving exploration task 2. It discovered and mined ore but failed pickup: the ore cavity had a solid ceiling, and pickup required standing in the exact drop column instead of an adjacent accessible cell. The fallback issued four explicit 90-degree survey commands, providing a concrete cause for spins in this run. Final FAILED at 1214 elapsed ticks; all 1134 turns replay with v14.

The concurrent original `iron-pickaxe` run descended from Y64 to Y61, observed and harvested coal, and crafted eight torches autonomously. Placement failed because the narrow stair's available floors were protected footholds. After 80 ticks of bounded reduced-light progress it returned to the search origin; fallback harvesting exhausted. FAILED at 1857 elapsed ticks, zero planner calls; all 1836 turns replay with v14. This establishes real-terrain supply and bounded retreat, not an original-scenario pass. Next corrections are adjacent drop pickup and lighting placement in narrow passages. The surface-floor ranking concern did not cause this failure and remains unmodified.

Policy v16 adds explicit placement faces and wall-torch identity checks; 75 System 1 and 33 evaluator tests pass. Batch `20260907-193856-359490-24712` exposed two remaining issues. The long cave approached the mined cavity but the drop could remain beyond pickup range under its solid headroom; FAILED at 1394 elapsed ticks, with all 1319 turns replayed. The original scenario's different initial view selected unavailable acacia prerequisites before observing nearby wood. Stopped that repetitive search through the isolated client-stop endpoint; harness reports CLIENT_EXITED before a terminal scenario report. No original-scenario replay or success claim for that interrupted run.

Policy v17 retains resource-drop identity while clearing observed, excavatable headroom over an observed safe floor. Batch `20260907-194646-603668-27548/01-system-one-lighting-exhaustion` PASSED in 423 elapsed ticks, zero planner calls, with all 347 turns replayed. Its recorded sequence consumed the carried torch, acquired eight replacements, placed further lights, and collected raw iron from the tunnel end. This run's randomized drop was collected from the adjacent cell; the headroom-clearance sequence has unit coverage but still needs direct live execution evidence.

The new one-block-wide `system-one-lighting-stairs` fixture in the same batch FAILED in 946 elapsed ticks; all 881 turns replay. Supply and light restoration succeeded, but choosing the next unused floor for a torch put a non-air voxel in the intended passage. The conservative terrain model then diverted search sideways. Wall placement must be preferred above passage clearance, and observed non-colliding block geometry remains a broader outstanding perception/domain improvement. These fixtures leave the original benchmark unchanged.

Policy v18 prefers observed wall supports above passage clearance before unused floors. Batch `20260907-195031-310300-28757` PASSED both cases with zero planner calls and runtime SUCCEEDED, with no remaining motor command, pathing process, or release pending:

- `01-system-one-lighting-exhaustion`: 418 elapsed ticks; all 346 decision turns replay. Task 2 placed its only carried torch at tick 167, resumed with zero torches at 170, suspended for supply at 216, observed eight crafted torches at 233, placed another wall torch, and resumed at 236. It then collected raw iron. This proves actual depletion and local resupply in a longer passage; it does not prove away-from-cave resupply.
- `02-system-one-lighting-stairs`: 248 elapsed ticks; all 173 decision turns replay. The one-block-wide passage used an EAST wall face at tick 172, observed restored light at 175, retained exploration task 2, and collected raw iron. This directly exercises wall placement without obstructing the narrow route.

All 75 System 1 tests and 33 evaluator tests pass. The original scenario still requires robust ingredient choice when initial observations omit nearby wood; the interrupted v16 run provides that regression. Non-colliding voxel geometry, away-from-cave supply/return, hazard preemption, larger production batches, original/held-out scenario gates, migration/deletion, and full-game capabilities remain outstanding.

Visual verification found an external playback failure for the successful v18 depletion Play. The renderer completed `recording-render/fpv.mp4` (494 frames, 960×540, 20 fps; global ticks 12–505 versus requested 12–506), but sampled frames 150, 210, 240, 350, and 430 all show the same view outside the cave. Only 29 distinct PNG byte hashes occur; the final frame change is ordinal 117. The MP4 is not valid visual proof of cave traversal. The captured `playerState` stream does contain 495 changing state samples through tick 506: positions descend from Y200 into the cave and progress from Z15 to Z40, with changing yaw/pitch and final raw-iron inventory. Thus the numerical capture and policy replay remain useful, while replay/camera presentation requires diagnosis. The renderer reports 160 ignored packets (156 chunk unloads, four player-position packets); that alone does not establish the cause. The external recorder checkout currently has unresolved merge conflicts, and this task has not modified it.

Policy v19 adds a generic ancestor-revision boundary to the task kernel. A domain can replace an obsolete method while preserving its parent task ID, run deadline, and command/task counters. An active descendant must acknowledge release before the kernel cancels that subtree and makes the parent ready with its new continuation. Cancellation overrides a pending revision; stale feedback cannot finish the replacement command. The Minecraft domain uses this only at completed look/travel boundaries of an unfulfilled, unobserved harvest, with a substantial recipe-cost improvement and no mined drops awaiting pickup. This addresses commitment to unavailable wood after another species becomes observed, without restarting the root goal or injecting a new controller. Seventy-nine System 1 and 33 evaluator tests pass. The new `system-one-production-discovery` fixture places wood behind the initial view and requires a revision event as well as a stone pickaxe; live verification is in progress.

Batch `20260907-200152-762628-32160` verified v19 recipe revision live. The discovery fixture revised wooden-pickaxe task 3 at tick 108 after observing oak, then PASSED in 892 elapsed ticks with zero planner calls; all 817 turns replay. The original scenario failed in 966 elapsed ticks at `crafting_cursor_occupied` while crafting the stone pickaxe; all 940 turns replay. The earlier two-tick client settling heuristic was insufficient.

Motor v10 replaces that heuristic with immutable full server-inventory receipts captured on `ScreenHandler.updateSlotStacks`. Each ingredient burst waits for a newer response for the same container containing the expected grid and an empty cursor. Minecraft 1.21.8 advances the server revision on a crafting-grid update, so the burst's following return click triggers a full resync. Tests reject old/wrong-container responses, occupied cursors, partial grids, and extra ingredients. Eighty-one System 1 and 33 evaluator tests pass.

Batch `20260907-201151-093354-35053` exercised the receipt motor with no crafting failures. Discovery PASSED in 745 elapsed ticks (682 replayed turns). The original run crafted tools, left its excavation, produced charcoal in a furnace, crafted eight torches, and returned, then failed lighting placement at tick 1911; FAILED in 2465 elapsed ticks (2443 replayed turns). The exposed-iron fixture failed ore acquisition in 1704 elapsed ticks (1624 replayed turns): remembered ore was close in straight-line distance but occluded by the excavation rim. All runs used zero planner calls. These failures identify two separate geometric issues: harvesting must approach from an observed visible stance, and footprint memory can retain a center cell observed as air while the player's body is supported by a neighboring block.

Policy v20 and observed footprint memory fix the two geometric failures. Footing memory now records observed support blocks under the body's contact area and invalidates cells observed as air; harvesting selects an observed visible stance instead of attempting a remembered ore through a rim. Eighty-four System 1 and 33 evaluator tests pass. Batch `20260907-202516-654171-39487` PASSED full iron production in 2157 elapsed ticks (2083 replayed turns) and narrow-passage lighting in 227 elapsed ticks (163 replayed turns), both with zero planner calls and all motor control released.

The concurrent original scenario completed charcoal resupply, returned, restored working light, and descended to Y32 before `underground_search_budget_exhausted`. FAILED in 4954 elapsed ticks; all 4933 turns replay. Its search issued only 44 navigation commands, so the 96-step budget was not the cause. Nine navigation commands reported `observed_route_unavailable` despite the player already occupying the requested destination cell; one recorded feet height was still 0.232 blocks above the floor. The motor checks for a stopped path against time since command start instead of allowing settling after path completion. These and other historical rejected candidates accumulated against the search's global rejection limit even while it made progress. Next corrections are bounded arrival settling and resetting local failed candidates after reaching a new exploration position.

Policy v21 / motor v11 gives navigation ten idle ticks after Baritone stops, allowing the player to land before declaring a missing route. Reaching a retained underground step clears obsolete local rejections without resetting the step or mission budgets. The focused suite passed 85 System 1 and 33 evaluator tests. Batch `20260907-203841-405763-43724` passed the narrow staircase in 254 elapsed ticks, with no failed navigation commands; all 176 turns replayed. The original iron-pickaxe scenario failed at 2646 elapsed ticks; all 2626 turns replayed, with zero planner calls. Its first underground failure was `no_observed_underground_step`: the three excavated descent columns had observed andesite floors, which the block-name whitelist rejected as standing support. Next: derive support from observed physical block properties instead of enumerating terrain names.

Policy v22 / stone method v8 records `fullSupport` alongside each observed voxel. Standing checks consume this physical fact rather than Minecraft material names. The Minecraft sensor accepts cached full collision cubes only for blocks without dynamic bounds, using no live neighbor queries for shape evaluation. Darkness masks support along with identity; the perception ray still stops at the first occupied voxel. Dynamic/partial support, non-air passage, and hazard policy remain separate work. Recordings from older method versions are explicitly incompatible with the changed observation contract.

The focused suite passes 86 System 1 and 33 evaluator tests, and both client source sets compile. The staircase now alternates andesite/deepslate underfoot. In batch `20260907-205241-751120-48247`, `02-system-one-lighting-stairs` PASSED in 263 elapsed ticks with zero planner calls, and all 176 decision turns replayed. Its tape records feet above five distinct observed deepslate supports and four andesite supports. This proves the Minecraft adapter supplies usable support facts for those materials; it is not visual playback proof or an original-scenario pass.

The v22 original run reached smelting and collected an ingot, but did not pass. A second one-input smelt tried to acquire more raw iron from the surface furnace; underground search failed at tick 4784 and harvesting exhausted at 5272. Fallback production then repeatedly unpacked the existing ingot into nine nuggets and repacked it, making no net progress toward three ingots. Stopped the owned client after at least 74 cycles. Harness outcome is CLIENT_EXITED before terminal report; this is an interrupted diagnostic run, with no full replay claim.

Policy v23 reserves existing goal stock while obtaining nested ingredients, tools, lighting supplies, stations, or smelting prerequisites. This prevents a partially satisfied goal from financing its own reversible conversion cycle. A generic bar/piece regression reproduced the old loop before the fix; it now chooses fresh production, terminates without consuming stock when no alternative exists, and still permits packing pieces already in inventory. All 87 System 1 and 33 evaluator tests pass. Live verification of v23 is pending. The preceding raw-iron failure also exposes one-batch ingredient acquisition: the next change will provision the missing output quantity before returning to a workstation.

Policies v24/v25 provision recipe inputs for all remaining output batches, accounting for yield and existing free stock, before returning to a workstation. Commands still cook or craft one batch at a time and observe each result. Smelting additionally reserves later batches' raw inputs so fuel acquisition cannot burn them. All 90 System 1 and 33 evaluator tests pass; separate regressions first reproduced the one-batch supply and later-input fuel defects. Residual furnace burn time is still not accounted for, so fuel use remains conservative.

Batch `20260907-210547-612131-52266` uses v24. Its exposed-iron fixture PASSED in 2238 elapsed ticks with zero planner calls; all 2158 turns replayed using saved v24 decision classes. The original scenario is still running at this entry: it recovered from a harvest failure, obtained three raw iron at tick 7506, returned to the furnace, and is acquiring more smelting fuel.

The new `system-one-charcoal` fixture starts with two logs and an exposed furnace, plus nearby harvestable wood. In v25 batch `20260907-211059-376790-53903`, it PASSED in 592 elapsed ticks with zero planner calls and a fully released motor; all 525 turns replayed. The decision tape records one extra log harvested, then three logs becoming two reserved logs plus four fuel planks. The first smelt at tick 177 uses a plank with both logs still present; the second at 396 uses another plank with one log and the first charcoal still present. The scenario inventory check is supplemented by this manual tape inspection of the reservation behavior; the YAML does not yet automatically assert that command order.

## First original iron-pickaxe pass

`20260907-210547-612131-52266/01-iron-pickaxe` PASSED under policy v24 / motor v11 in **9369 elapsed ticks**, with the unchanged original inventory check satisfied. Harness status is OK, the client exited normally, and the motor is fully released (no command, pathing, crafting, smelting, or pending release). The planner journal is empty and final LLM records are empty. The decision recording is complete, and **all 9346 turns replay exactly** using the saved matching v24 decision classes. The separate Recorder Play finalized; visual playback of this run has not yet been verified.

The tape contains 134 breaks, 152 navigations, 80 looks, 16 crafts, 13 placements, seven furnace starts, and seven collections. Summed successive recorded eye positions give approximately 678.59 blocks of travel; the lowest recorded feet position is Y36. These are observation-derived work/travel metrics, not a damage audit or proof of zero perception violations. The run recovered from a raw-iron harvest failure, obtained three raw iron at tick 7506, smelted them, acquired additional fuel, and crafted the final pickaxe at tick 9379; final release was acknowledged at 9393. Scenario elapsed ticks and runtime tick origins differ.

This is one successful original-world run, not the frozen held-out reliability gate. Current v25 still needs an original rerun after its later-input fuel reservation change. Remaining required work includes the other survival/lighting/route interruption fixtures, automatic behavioral metrics, visual replay diagnosis, frozen development and held-out evaluation, migration/deletion, and the game-completion stages.

The generic kernel now checks a domain interruption hook during passive waits as well as active commands. It preserves the ancestor branch, uses the domain-supplied continuation, and waits for motor release before starting urgent work. Interruption consumes a transition from the existing budget; cancellation still takes precedence. Three new synthetic-domain tests cover passive waiting, active handoff, late feedback, cancellation, and the transition limit. All 93 System 1 and 33 evaluator tests pass. Minecraft survival policy is not wired to the new hook yet; this is scheduler support, not a survival behavior pass.

The v25 rerun `20260907-211820-711360-56166` passed mid-cave torch depletion in 508 elapsed ticks, but the original scenario FAILED at 8376 elapsed ticks with zero planner calls. Its first failure at tick 7781 was a cave gap at feet (31,31,5): adjacent candidate cells were observed air with no floor for the search's one-block step. Neighboring solid faces could support explicit foothold placement, but that method did not exist. This failed rerun means the earlier v24 pass does not establish current-policy reliability. Next: bounded placement of a foothold from uncommitted carried material, using only observed empty space and an observed supporting face.

`scripts/inspect-system-one-recording --failure-context <tape>` now reconstructs the first failed task's local observed cells (a bounded radius-two cube), inventory, footholds, and recent motor commands. It works across policy versions and still labels decision replay as unchecked. Six inspector tests pass, including observation removals and preserving the first failure's state when later turns change inventory or terrain. Applying it to the v25 original reproduces tick 7781 at (31,31,5), with 81 local observed cells; applying it to the new gap fixture exposed the next policy failure without adding runtime logging.

Policies v26/v27 add explicit foothold placement. The Minecraft knowledge pack supplies stable material choices (cobblestone/dirt, with separate item and placed-block identities); the search consumes only uncommitted stock. Placement requires observed empty destination/headroom and a visible face of an observed full support block. Baritone still receives bounded navigation commands after support is observed. Placement destinations are checked against any active reduced-light region.

The first gap fixture in `20260907-213645-137141-60488/02-system-one-cave-gap` FAILED in 1695 elapsed ticks; all 1618 turns replayed. Placement worked, but the search chose lower footholds, dug beneath the target, and cycled through side cells until its step budget expired (180 looks and 121 navigations). v27 prefers a fillable gap at the existing floor height after already-standable choices, and limits the visited-floor exception to the exact retained destination. All 95 System 1 and 33 evaluator tests pass.

The v27 gap fixture `20260907-214423-086034-62121/01-system-one-cave-gap` PASSED in 250 elapsed ticks with zero planner calls and a fully released motor. All 181 turns replayed. It placed two cobblestone supports at Y195 across the gap, placed one wall torch, and reached raw iron with ten navigations, four breaks, and zero survey-look commands. The evaluator independently found both placed cobblestone blocks near the final position. This proves the targeted gap behavior, not generic bridging across unsupported open voids.

### Retain exploration routes through supply detours (v28)

The v26 original failure saved an exploration anchor at (-8,16,65), then finished resupply at (-5,70,19). Its detour exceeded the sensor's 48-block horizontal cache radius; no cells remained within two blocks of the saved anchor. Return failed without issuing a navigation command. Pinning just that endpoint would still leave missing intermediate terrain and an overlong single movement.

Exploration now records actual visited positions, retains already observed corridor cells while its continuation is active, and rejoins/follows that route in bounded waypoint legs. New observations can invalidate retained terrain, and completing the continuation releases its retention request. Partial failed movement also consumes the search step budget. Return and retreat share this navigation method; each command remains limited to 48 blocks and 400 ticks. This is active-continuation retention, not a permanent mission map.

All 101 System 1 tests and 33 evaluator tests pass. New tests cover long returns, joining after a detour, bounded failed stances, actual movement accounting, retention beyond the local cache, and invalidation without inventing unseen cells. Live batch `20260907-221231-647528-67875` runs the original scenario plus a new 60-block cave fixture that removes torches after 52 blocks of progress. The first fixture run failed before that intervention: at tick 197 the search chose to break the floor at (0,195,25), although (0,196,25) was already an observed standable position. A previously selected downward step outranked the newly observed cave floor. Consequently this run does not verify long-distance resupply or return; candidate reconsideration must be fixed before retrying that gate.

### Reconsider proposed excavation after observing a floor (v29)

The failed v28 return fixture finalized with harness OK; all 1686 decision turns replay exactly. The search now ranks observed standable terrain, then a fillable flat gap, ahead of a retained excavation proposal. A regression test reproduces the look-to-floor-discovery transition; all 102 System 1 tests pass. The v29 run `20260907-221644-583075-68861/01-system-one-return-route` traversed the existing floor, reached the 52-block intervention, and had 59 torches removed at tick 559. Exploration task 2 requested supply at tick 573. This establishes the candidate-selection correction, but the resupply/return gate is still pending: it remembers the entrance oak and furnace yet wastes work on unavailable wood variants. Existing ancestor reconsideration handles crafting recipes but excludes smelting alternatives.

The completed v29 recording establishes the return behavior: suspension saved (0,196,69); resupply took the player as far as Z14, 55 blocks from that anchor. Fifty observed cells near the anchor remained present beyond the local cache radius. At tick 6132, task 2 resumed with eight torches at (0,198,17), then navigated through Z32, Z48, Z63, and back to Z69. Each command retained its 48-block/400-tick bound. The same task continued exploration and the runtime obtained raw iron at tick 6674. All 6581 turns replay exactly, with no LLM calls and a fully released motor. However, the scenario report is FAILED at 6651 elapsed ticks: its earlier `lighting_supply` suspension event had been evicted from the bounded runtime event history, so the final `event_contains` check forgot an event it had already matched. The harness itself is OK and both clients exited normally. Do not relabel that existing report as a pass.

Evaluator historical event checks now retain the first matching tick for the current scenario. Inventory and other current-state assertions still reevaluate normally; starting another scenario clears the retained matches. A regression test covers eviction followed by later inventory success, and reset without a new matching event. All 34 evaluator tests pass. A fresh live rerun is required to validate the corrected report.

The v28 original `20260907-221231-647528-67875/01-iron-pickaxe` FAILED at 10532 elapsed ticks, with zero planner calls, harness OK, and a fully released motor. All 10512 turns replay. It completed two torch resupplies, but took repeated lava damage from tick 6798 and died at 6868. Automatic respawn occurred at 6869; System 1 continued the stale exploration task rather than responding to the death. Survival preemption and post-death continuation invalidation remain unimplemented acceptance requirements. The earlier v24 original pass does not cover them.

### Reconsider smelting alternatives and verify the long return (v30)

Crafting and smelting now use the same bounded method ranking for initial choice and reconsideration. At completed observation/travel boundaries during an unproductive input search, an appreciably cheaper observed alternative can replace the ancestor method. It preserves the ancestor goal identity and resource commitments, and does not interrupt productive harvesting or an active furnace operation. A regression test discovers an alternative smelting input while preserving already produced charcoal. All 103 System 1 tests and 34 evaluator tests pass.

Batch `20260907-222549-123594-70822` PASSED both scenarios with harness OK, empty planner journals, zero LLM records, fully released motors, and normally exited clients:

- `01-system-one-charcoal`: 608 elapsed ticks; all 527 decision turns replay exactly.
- `02-system-one-return-route`: 3080 elapsed ticks; all 3009 turns replay exactly. The fixture removed 57 torches at tick 488, exploration task 2 suspended at tick 508 with anchor (0,196,70), and resupply switched to observed oak smelting at tick 1831. Supply finished at 2574. The detour reached 53 horizontal blocks from the anchor, whose 50 nearby observed cells remained retained. Four bounded return legs targeted Z30, Z46, Z62, and Z70; actual feet reached the original anchor at 2839 before task 2 continued exploration. Final inventory contains raw iron, seven torches, and the stone pickaxe. The corrected evaluator retained the suspension and resume matches through completion. `return-verification.json` records the derived route evidence beside the raw tape.

This proves long-cave resupply and return, not general survival or current original-scenario reliability. The return run still issued 261 survey looks during unavailable-resource searches; method reconsideration reduces wasted work but does not solve the broader attention/frontier policy. The v28 fatal trace already identified lava beside the route at tick 6709, before further excavation and entry at Y27. The agent continued issuing break commands while taking lava damage. Next requirements: act on observed excavation hazards, wire survival preemption through the single motor lease, invalidate stale post-death continuations, and exercise hazard/changed-route fixtures before another original sanity run. Visual playback of these latest recordings has not been verified.

### Survival interruption and death recovery (v31)

The observation value and tape now include health, lava/fire contact, and a life identity derived from the client player instance. A Minecraft survival policy supplies explicit hazard IDs and bounded refuge selection over observed standing positions. Underground candidate eligibility and exposed harvesting avoid nearby observed hazards. The generic kernel remains free of Minecraft types; its existing interruption/release mechanism runs escape children, and task revision can now reset the active root as well as an ancestor.

Live missions wrap their inventory objective with life/recovery state. Death releases active control and discards stale descendants. Respawn preserves the mission ID and goal, rebuilds dependencies from current inventory, and retains the original command/time budgets; recovery is limited to two deaths. Escape continuations preserve passive furnace timing instead of collecting prematurely after an interruption. This does not implement food, combat, drowning, arbitrary dimension transitions, or every mid-inventory-transaction recovery case.

All 107 System 1 tests and 34 evaluator tests pass. Tests cover active mining release before escape, stale feedback, interruption of a passive furnace wait, post-death root revision without budget reset, and observed-only hazard eligibility.

Batch `20260907-225541-604125-78476` PASSED both live fixtures with harness OK, zero LLM calls, empty planner journals, fully released motors, and normally exited clients. Artifacts now reside on the external drive through the existing repository paths.

- `01-system-one-death-recovery`: 280 elapsed ticks; all 211 decision turns replay exactly. The fixture killed the player after the first log at tick 168. The runtime revised mission 1 with `player_died` at 172, then `life_changed:1` at 173, preserving the planks objective and rebuilding its descendants. Final inventory contains eight oak planks and final health is 20. The test uses a prepared world spawn; it does not prove recovery of a remote death inventory.
- `02-system-one-survival-wait`: 293 elapsed ticks; all 217 turns replay exactly. Furnace wait began at 112 with wake tick 312. The fixture introduced adjacent lava, triggering escape children at 113, 164, and 193 as its observed flow spread. Each returned to the same furnace task 3; the task retained wake tick 312 and collected its ingot afterward. Health remained 20 throughout. This is live evidence for passive-wait interruption and preventive escape, not yet active mining preemption or escape while already immersed in lava.

Next: verify an observed hazard interrupting active mining, then rerun the original iron-pickaxe scenario. The changed-return-route fixture, broader survival methods, visual playback, frozen reliability sets, execution cutover/deletion, and game-completion milestones remain open.

The concurrent v26 original run FAILED in 11271 elapsed ticks, zero planner calls; all 11247 turns replayed using saved matching v26 decision classes. It eventually replenished torches and rebuilt a worn-out stone pickaxe, but the saved exploration return failed at tick 10717 before issuing a return command. The anchor captured at tick 6100 was (-8,16,65); a detour reached 58 blocks away on one horizontal axis, beyond the sensor's 48-block cache radius. On return at (-5,70,19), zero remembered cells remained within two blocks of that anchor. Next: retain a bounded observed route for suspended exploration and navigate back through bounded waypoints. Merely retaining the final anchor would leave the corridor and travel budget unresolved.

### Active mining handoff and completion ordering (v32)

Preemption now preserves an eligible harvesting target instead of treating cancellation as evidence that the target failed. Immediate survival work precedes inventory-goal completion, including when the root is an escape continuation. The evaluator waits for System 1 success and acknowledged motor release before accepting its current-state checks. All 108 System 1 and 35 evaluator tests pass.

Batch `20260907-230616-432582-82772/02-system-one-survival-mining` reached eight oak planks in 861 elapsed ticks with a fully released motor and zero planner calls. The available tape proves active Break attempt 1 at tick 153, Stop at 160, release acknowledgement before escape Navigate at 161, and resumption of the same Gather task 4 and still-eligible target at 174. However, its recorder overflowed after 51 rows: harness status is DECISION_TRACE_INCOMPLETE. This is partial handoff evidence, not an accepted complete recording or replay pass. A fresh run is required after investigating external-drive write throughput.

The unchanged original scenario in the same batch FAILED in 5802 elapsed ticks, harness OK, zero planner calls, fully released motor. All 5751 decision turns replay exactly. Health remained 20 in the recorded observations. The first failed child was lighting placement at tick 4914; exploration later exhausted observed steps at 5335, followed by exhausted production alternatives. This run does not establish current-policy iron-pickaxe reliability. The saved local context contains observed tunnel walls beside retained footholds, so placement eligibility warrants inspection separately from the recorder overflow.

### Complete survival recordings on external storage

DecisionTraceWriter now coalesces gzip output into a bounded 64 KiB byte buffer before file writes. The 32-row nonblocking input queue and explicit incomplete footer remain unchanged. A four-trial external-storage probe round-tripped the 15 MB incomplete trace with and without buffering in 0.075–0.079 seconds; that does not establish small writes as the cause of the prior overflow. The overflow remains a transient throughput issue to monitor, not a proven fixed root cause.

The concurrent rerun `20260907-231835-359582-86651` PASSED both fixtures with complete recordings, harness OK, zero planner calls, fully released motors, and normal client exits. Active mining completed in 719 elapsed ticks and all 636 turns replay exactly; passive furnace waiting completed in 376 ticks and all 289 turns replay. In the mining run, the player contacted flowing lava at runtime tick 224 and health fell from 20 to 16. It left lava by 228, reached the refuge at (-5,200,16), waited while burning, and resumed after fire cleared at 551. No death occurred. This adds a bounded live contact-escape example; it does not establish reliable escape from deep lava or survival with depleted food.

### Light a two-block-high walking passage (v33/v34)

The v32 original tape showed tunnel wall placements rejected solely because their destination lay above a retained walking foothold. The observation value now distinguishes an empty voxel from observed body clearance. Minecraft supplies clearance only for static empty collision shapes without fluid, using the isolated empty block view rather than querying hidden neighbors. Unidentified occupied cells cannot grant clearance. Standing-position and excavation decisions can traverse an observed noncolliding fixture without breaking it. Wall-torch placement can consequently use the walking passage; ray occlusion remains a separate rule.

The first two-block-high fixture, `20260907-232158-405408-87706/02-system-one-lighting-low-ceiling`, FAILED in 720 elapsed ticks, harness OK; all 641 turns replay. A wall torch was placed at (0,197,22), but after the camera entered that same voxel the conservative sensor stopped every ray at its origin. The saved failure context established this before new instrumentation. v34 permits rays to leave an identified noncolliding fixture in their origin voxel. All later occupied voxels still stop rays, including other torches; solid and unidentified origin cells receive no exception. Tests cover that distinction, route clearance, placement in a two-block-high tunnel, and navigation without excavating the torch. All 112 System 1 and 35 evaluator tests pass. Production policy is v34; direct stone policy is v9.

The unchanged fixture rerun `20260907-232516-385340-88517/01-system-one-lighting-low-ceiling` PASSED in 315 elapsed ticks, harness OK, zero planner calls, fully released motor, normal client exit. All 246 turns replay exactly. It crafted eight torches, placed a wall torch at tick 239, entered its voxel at 248, moved beyond it at 255, and obtained raw iron with seven torches remaining. It issued no torch-breaking command. This verifies the low-ceiling lighting and traversal case; visual Recorder Play review is still pending.

The concurrent v33 original scenario FAILED in 3985 elapsed ticks with harness OK; all 3962 turns replay exactly. Its first failure was `no_observed_underground_step` at tick 3536, feet (15,66,-48), after a long near-surface excavation. This is a separate search-policy failure, not evidence that the origin-cell lighting correction solves original-world acquisition. Inspect its bounded search state and preference for shallow travel before another original rerun. Changed-route, broader survival, frozen reliability, cutover/deletion, and game-completion gates remain open.

### Preserve an excavation step through headroom clearing (v35)

Reconstructing the v33 original's pure task state showed a specific failure at tick 800: task 25 had selected (6,66,14) and broken its headroom at (6,67,14), but the next decision placed a block to walk at Y67 instead. The newly cleared intermediate position displaced the downward step. By tick 3536 the search had used 75 steps and reached the radius boundary, falling only from Y67 to Y66.

UndergroundSearch now distinguishes observing a proposed step from excavating it. Once a break starts opening a selected step, the step remains preferred through subsequent observation until it is reached, rejected, or invalidated. Hazard, foothold, work, and radius checks still apply. A proposal discovered only through looking can still yield to an existing cave floor. The regression test covers headroom clearing, an intervening look, discovery of a competing flat floor, completion of the downward step, and a newly observed hazard. All 113 System 1 and 35 evaluator tests pass.

The new evaluator-only `system-one-descent` fixture starts in a lit solid-rock chamber, with iron below its starting elevation and no target coordinates supplied to decisions. In batch `20260907-233333-659596-90308`, its first run FAILED in 1977 elapsed ticks, harness OK, zero planner calls; all 1904 turns replay exactly. Descending excavation reached and mined iron at (0,195,20), but the player was still at (0,197,19). Pickup navigation required a two-block descent, exceeding the current motor's one-block fall limit. The task had no method to prepare an alternate approach, then abandoned its known drop. This is an approach-preparation failure, not a discovery/tool failure; do not move the ore or loosen the check to pass the fixture.

The unchanged long-return fixture in that batch PASSED in 2724 elapsed ticks. Its decision replay is being checked, and the original iron-pickaxe run remains active at this entry. The original already reached Y43, demonstrating departure from the earlier near-surface tunnel but not yet goal completion.

The v35 long-return recording replays all 2658 turns exactly; harness OK, zero planner calls, fully released motor. The original scenario completed FAILED in 6041 elapsed ticks, harness OK, zero planner calls, fully released motor; all 6019 turns replay. Its first failed search reached (-6,38,-17) at tick 5473. The saved local context shows a lit cave gap, 47 cobblestone, and retained standing support below the player, but no eligible visible support face for the next foothold. Excavation retention solved the surface-tunnel behavior; this gap requires approach/placement work and remains a failed original sanity run.

The evaluator now also includes `system-one-return-blocked`, sharing the existing long-cave resupply setup. After the recorded eight-torch resupply completion, it inserts a two-block-high stone wall at a fixed point in the already traversed corridor. The intervention is server-side, its evidence is written separately, and its coordinates/state never feed System 1. Batch `20260907-233913-728451-91509` is active at this entry; no changed-route pass is claimed yet.

The blocked-return fixture finalized FAILED in 3146 elapsed ticks, harness OK, zero LLM records/planner calls, fully released motor, normal client exit; all 3079 turns replay exactly. The wall was inserted at runtime tick 2510 while the player was resupplying at (-1,199,17), and was first observed at 2571 from (0,196,27). Return task 3 navigated toward Z46, then tried Z45, Z47, and Z44 from Z42, all beyond the observed wall at Z43. It exhausted four leg attempts at 2695 and never issued a repair break. This is complete saved evidence for the planned changed-route failure injection, not a changed-route recovery pass.

The next implementation should address explicit preparation of a reachable approach to a known target or return waypoint. Current excavation preparation lives only in resource exploration; Gather and ReturnNavigation can select stances but cannot repair their access. Preserve the single motor lease, observed-only break/placement eligibility, one-block step/return constraints, and bounded work while sharing useful terrain preparation. The three recorded remaining cases are the two-block pickup drop, a blocked remembered corridor, and foothold placement without an accessible supporting face. Do not erase these distinctions by increasing navigation limits or treating a remembered destination as reachable.

### Shared local terrain preparation (v36)

`TerrainAccess` now prepares a bounded approach to a selected stance for pickup and remembered-route tasks. It searches a six-block local region, expands at most 512 proposals, and caps execution at 48 commands/1000 ticks. A proposal may include unknown space at higher cost, but only a real look can reveal it. Breaking requires an identified, eligible, reachable surface; movement requires observed body clearance and standing support. Each actual step is at most one block up or down. Protected footholds and observed hazards invalidate proposals, and unchanged looks/failures reject an edge rather than looping. The method does not place supports or solve the outstanding edge-placement case.

Pickup and return failures suspend into the same `Access` task under the existing kernel/motor lease. `AfterAccess` preserves the parent and route retention, reports success back as movement completion, and returns failed preparation to the parent's existing alternatives without recursively restarting it. Reach calculation now preserves observed body clearance for the sensor's existing origin-voxel exception. Production policy is v36; direct stone policy is v10. All 118 System 1 and 35 evaluator tests pass, covering wall preparation, an alternate stair for a two-block drop, observation-only unknown-space handling, budgets, hazards, protected footing, and parent continuation. Probing the saved v35 failure values produces a visible side-stair break for pickup and an upper-wall break for return; these probes establish selected commands, not physics.

Batch `20260907-235206-537121-97079/02-system-one-return-blocked` PASSED in 3427 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 3351 turns replay exactly. Return task 3 suspended at 2882 into access child 31. The child broke the upper wall block at (0,197,43), stepped onto the remaining lower block, descended on the far side, and reached (0,196,46). At 2938 the same return task resumed its longer waypoint legs and subsequently obtained raw iron. The fixture now additionally checks the access suspension and successful resume events; those events are present in this recording, but a fresh run will verify the strengthened YAML checks.

The broader descent fixture in that batch FAILED before exercising pickup repair; all 2286 turns replay. An early `observed_route_unavailable` changed its excavation route, and its first failed search ended at (-16,193,31), missing the buried ore. That scenario remains unchanged and remains a failed gate. The new focused `system-one-pickup-step` fixture reproduces the recorded exposed low ore and unprepared two-block approach. Its run `20260907-235545-649057-99014/01-system-one-pickup-step` PASSED in 238 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit; all 171 turns replay. Access child 5 cleared the next step at tick 213, navigated through (-1,196,16) and (0,195,16), then released control after inventory confirmed raw iron at 254–255. No enlarged fall limit was used. These are separate targeted repair passes, not an original-world or held-out reliability claim.

### Expose a support face by crouching to the edge (v37 / motor v12)

The v36 regression batch `20260908-000039-823114-750` passed the strengthened blocked-return assertions in 3169 elapsed ticks; all 3092 turns replay. Its original iron-pickaxe run FAILED in 10184 elapsed ticks; all 10163 turns replay. Both harnesses are OK. The original's first failed task was a completed lighting retreat at tick 3969, not a new success of the original sanity gate.

The previously recorded Y38 gap exposed a separate missing motor capability: the only useful neighboring support face can belong to the block beneath the player and remain unreachable from the center. A bounded `EdgePlace` command now approaches that face while crouched, validates it with a real ray hit, places the selected carried block, and waits to observe the placed block. Domain selection requires a known empty gap and headroom, identified full standing support confirmed by footing memory, an eligible horizontal neighbor, and uncommitted material. It does not identify new terrain through the edge or delegate target selection to Baritone.

The motor owns the movement keys throughout approach and release. Normal placement releases after observing the block; interruption before placement attempts to return toward the original support center before acknowledging release. World/player loss clears controls immediately. The return attempt is bounded at 40 ticks and does not promise recovery after footing itself disappears. Placement has a 160-tick deadline and a bounded support envelope. Lighting allowance checks include the affected destination. All 120 System 1 and 35 evaluator tests pass; geometry tests supplement, rather than replace, live physics evidence.

`20260908-001122-197567-8792/01-system-one-edge-bridge` PASSED in 283 elapsed ticks with harness OK, zero LLM calls, fully released motor (including edge placement), and normal client exit. All 210 turns replay exactly. Its three-block gap has no side-wall supports and requires a successful edge-placement event as well as raw iron and placed cobblestone.

The first edge approach began at tick 146. At 162 the player's Z was 21.020, just beyond the original support boundary at Z21, and its eye height showed crouching. Lighting maintenance requested Stop. The motor returned to Z20.657 and acknowledged release at 172, before the torch child started. The same exploration task 3 resumed, then completed edge placements at ticks 192 and 213 and a regular face placement to finish the crossing. During the entire edge sequence feet stayed at Y196; health remained 20 throughout the run, with no death. Final inventory included raw iron and seven torches. This is live evidence for successful bridging and interruption/release from the edge, not visual Recorder Play review or broad gap reliability.

### Automatic observation metrics and v37 regression

Batch `20260908-001545-688345-11282/02-system-one-descent` PASSED in 691 elapsed ticks with harness OK, zero LLM calls, fully released motor, and normal client exit. All 599 turns replay exactly. A failed pickup route suspended into shared access preparation; that child encountered another unavailable route but found an alternative and collected raw iron. Health remained 20. This run issued no EdgePlace command, so it supports access recovery rather than attributing the pass to edge placement.

The original iron-pickaxe run in the same batch FAILED in 9934 elapsed ticks with harness OK and normal client exit; all 9901 turns replay exactly. It issued 215 Break attempts and traveled 327.356 observed horizontal blocks. Recorded health stayed 20. The first failed child was a bounded coal-search lighting retreat at tick 4207; production subsequently selected charcoal and resumed. Later the pickaxes wore out, and six Break commands failed `suitable_hotbar_tool_missing`. Exploration checks tools before entry but does not maintain them while running. Final inventory contained 108 cobblestone, two planks, two sticks, and seven torches, but no pickaxe. This is the next repair case; the original sanity gate remains failed for the current policy.

The recording inspector now reports observed horizontal travel, Break attempts, minimum health, adjacent same-life net health decreases, lives observed dead, burning/lava samples, and task suspension/resumption/outcome counts. Definitions explicitly exclude sampling gaps and respawn displacement from travel; health decreases are not total damage, and command attempts do not prove blocks destroyed. Missing vital/pose evidence yields unavailable values. Dark-allowance consumption and perception violations remain explicitly unavailable rather than falsely reporting zero.

Future `--system-one` batches automatically write `system-one-inspection.json` after client/recording finalization, with a link and coverage status in `harness-summary.json`. A saved trace that fails coverage inspection cannot retain harness OK. The inspector remains independent of exact decision replay and scenario success. All 33 Python runner/inspector tests pass, including life changes, missing samples, repeated dead samples, older recordings, durable export, and missing-tape handling. Applying the inspector to the live descent tape reproduces 11.599 horizontal blocks, 25 Break attempts, and 599 health-20 observations. Automatic generation by a fresh batch remains to be exercised.

### Maintain the search tool through wear (v38)

The v37 recording loses its stone pickaxe at tick 7437 and its wooden pickaxe at 9311. The search task remains active throughout; the first tool rejection arrives only at 9945 after lighting resupply. Search now maintains the declared harvesting capability during execution. If none of its domain-declared tools remains, it suspends into replacement production while retaining the same exploration state, material commitments, and actual return route. The existing return continuation distinguishes lighting repair from tool repair. Failed replacement tries the next declared tool once, excludes ancestor cycles, and terminates when alternatives are exhausted. It does not reset search progress or global work budgets.

All 122 System 1 and 35 evaluator tests pass. New lifecycle tests verify Stop/release before replacement, reserved material preservation, travel back from a repair detour, resumption of the same task/destination, and bounded alternatives. `system-one-tool-wear` starts in the descent chamber with a stone pickaxe that has two uses remaining, a crafting table, three cobblestone, and two sticks. Its checks require the replacement interruption and successful production resume in addition to raw iron. Live results are pending; this change currently maintains tools for Explore, not every excavation or harvesting task.

The first worn-tool run, `20260908-003234-343612-17271/02-system-one-tool-wear`, FAILED in 2357 elapsed ticks with harness OK and normal client exit. All 2260 turns replay exactly. Replacement suspended correctly after two breaks, but Station immediately requested manufacturing a table without a local survey. The dependent log searches then exhausted alternatives in the stone chamber. Automatic `system-one-inspection.json` generation succeeded with complete coverage and explicitly unchecked replay; matching Java replay was checked separately.

v39 surveys locally at workstation height before manufacturing an uncarried, unobserved station. The four-look bound and existing observed-station/reach validation remain. A new test covers discovery before manufacturing and reservation preservation when the bounded survey fails. The tool-wear fixture also had stone directly above its supplied table, making the intended exposed station occluded under the conservative sensor. That one cover block is now air; the ore, two-use tool, replacement inputs, time budget, and behavioral checks are unchanged. This is a corrected supporting-fixture layout, not a pass of the original covered-table layout. Both changes require a fresh live run.

The first v39 test run exposed an existing production simulator that accepted only Craft and Place commands. Its motor now applies Look poses as well, and the production test asserts four survey commands before manufacturing the absent station. All 123 System 1 and 35 evaluator tests pass after that update.

The first v39 launch, `20260908-003834-956989-19230`, stalled while joining the world. Repeated client-thread bridge timeouts caused capture failure, and the client exited via SIGINT (130). There are zero decision turns and no finalized capture; this is a harness failure, not a scenario result. No stack dump established the startup stall's cause.

The fresh retry `20260908-004153-992251-20547/01-system-one-tool-wear` PASSED in 608 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 521 turns replay exactly. At tick 178 tool loss requested Stop; release was acknowledged at 179 before crafting replacement stone pickaxe. Inventory confirmed it at 202, and the same Explore task 3 resumed before eventually obtaining raw iron. Health remained 20 throughout, with 7.488 observed horizontal blocks of travel. The corrected table was already observed by the replacement time, so this proves tool repair/release/resumption, not the new Station survey branch in live execution. The automatic inspection artifact is complete and correctly leaves perception auditing and allowance usage unavailable.

### Prepare access before abandoning an observed resource (v40)

The earlier v37 original recording contains another failure before tool wear: Gather task 60 selected an approach to observed iron at tick 6137, but navigation failed at 6150. It never mined the ore; subsequent generic harvest frontiers led back to the surface, where the task exhausted at 6761. Shared access preparation was restricted to mined drops and remembered return routes. Applying that preparation method to the saved tick-6150 observation selects an eligible stone headroom break at (-2,33,-14), showing a concrete alternative to abandoning the target.

Gather now also requests the bounded Access child when its failed stance can reach a still-observed, eligible unmined resource. Unknown targets and unrelated frontier travel do not authorize preparation. The existing observed-only break checks, one-block steps, preparation budget, parent continuation, and failure limits are unchanged. All 124 System 1 and 35 evaluator tests pass, including a regression that distinguishes observed ore from an unidentified cell. The new `system-one-harvest-approach` fixture puts visible ore across a two-block descent and requires successful access preparation/resumption as well as raw iron. Live validation is pending.

`20260908-004602-806699-22107/01-system-one-harvest-approach` PASSED in 297 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 222 turns replay exactly; its automatic inspection is complete. The first approach failed at tick 128, suspending Gather task 3 into Access child 4. That child observed and cleared a side stair, handled one further unavailable route at 260, and reached its stance at 285. The same Gather task then mined the ore and collected raw iron. Health remained 20, with 9.473 observed horizontal blocks of travel. No larger fall limit was used.

The v38 original run in `20260908-003234-343612-17271/01-iron-pickaxe` remains active at this entry (last verified report: 16632 elapsed ticks). It replaced its stone pickaxe, obtained three raw iron, and smelted two, but subsequent wood/fuel production is spending substantial work on unproductive harvest searches underground. This is ongoing evidence, not a terminal failure or an original sanity pass. Preserve the matching v38 replay classes when it finalizes; current policy is v40. Current targeted repair passes do not satisfy the held-out, migration/deletion, broader survival, or game-completion gates.

### Distinguish supply discovery from estimated work (v41)

The v38 original's tick-12439 observation still contains many remembered birch logs, including (9,67,3), while the player is at (42,31,-4). Fuel selection nevertheless requests acacia planks. The scalar estimate assigns an unseen harvest source a flat cost of 30, making an unobserved species appear cheaper than known wood farther away. This is a source-selection error; the evidence does not support blaming lost tree memory for this choice.

Production estimates now carry source evidence separately from work: known supply, discovery required, or unavailable within the bounded dependency search. Recipe composition preserves the least-supported input status, including through output yields. Selection first prefers a known supply chain, then compares estimated work. Failed methods still fall back through existing bounded alternatives. Meaningful reconsideration can replace discovery with a newly observed source at completed observation/travel boundaries. This is a conservative deterministic policy, not proof that the known source is reachable or always globally cheaper.

Applying the old and new pure decisions to the same saved tick-12439 observation selects acacia planks under v38 and birch planks under v41, respectively. These are offline choice probes, not physics replay. All 126 System 1 and 35 evaluator tests pass. New tests cover distant remembered fuel, unidentified-source exclusion, fallback after a known route fails, reservations, and preventing large yields from hiding a discovery prerequisite.

`system-one-remote-fuel` exposes oak at a forty-block cave entrance and a furnace near its underground ore. No fuel is supplied. It checks an iron ingot, the oak-plank fuel dependency, and completed oak harvesting. No fixture coordinates are passed to the agent. Live navigation, harvesting, and smelting verification are pending.

The v38 original finalized FAILED in 19949 elapsed ticks (`command_budget_exhausted`), harness OK, zero LLM calls, fully released motor, normal client exit. Its complete tape contains 19914 turns; the matching decision replay is still running at this entry, so exact replay is not yet claimed. Final inventory retains two iron ingots, one raw iron, a replacement stone pickaxe, and seven torches. Health stayed 20 throughout. It accumulated 1302.256 observed horizontal blocks of travel, much of it during unproductive wood-source searches. The first failed child at tick 8118 was spruce-wood acquisition for tool replacement; subsequent alternatives did obtain the replacement. The later acacia fuel search consumed the remaining global command budget. The original sanity gate remains failed pending a fresh current-policy run.

`20260908-005750-375582-25500/01-system-one-remote-fuel` PASSED in 1131 elapsed ticks, harness OK, zero LLM calls, fully released motor. All 1034 turns replay exactly, and the automatic inspection is complete. The first fuel dependency at tick 496 was oak planks while the player stood at (0,196,56), near the far furnace. Oak harvesting completed at tick 760 near the entrance, followed by fuel production and travel back to the furnace to obtain an iron ingot. Recorded travel was 112.474 horizontal blocks; health stayed 20. The Recorder Play finalized, but the harness needed SIGINT after its shutdown wait and the client exited 130. This proves the resource trip and recording; it does not prove graceful client shutdown or visual playback.

The long v38 original replay has now completed successfully: all 19914 decisions match, ending in `command_budget_exhausted`. The earlier pending qualification is resolved. The v41 original rerun is active in `20260908-005901-434777-25993/01-iron-pickaxe`.

### Persist policy state while a command remains active (v42)

A delayed-feedback regression found a lifecycle gap in lighting maintenance. If an active movement first enters reduced light while resupply is forbidden by its dependency ancestry, the policy creates a scoped allowance but returns Keep without retaining it. Every subsequent dark tick can consequently create a fresh deadline. The new test reproduced this: at the original tick-82 expiry, v41 issued no Stop.

The generic Keep decision can now carry updated domain state while preserving the command token, phase, feedback, task identity, and global budgets. Explore uses it to retain the lighting assessment during active work. No new actuator or scheduler is introduced. The regression now stops the same command at tick 82, and a synthetic kernel test verifies state refresh without changed ownership or lost feedback. All 128 System 1 and 35 evaluator tests pass. This is a source/unit proof with delayed feedback, not yet a live injected-delay result.

The v41 regression batch `20260908-010342-791622-27450` finalized with charcoal PASSED (659 elapsed ticks), blocked return PASSED (2642), and tool wear FAILED (2197). All three harnesses are OK with normal client exits. Matching replays and the new tool-wear failure diagnosis are pending; the earlier v39 tool-wear pass does not establish current reliability.

### Repair a failed exploration step before choosing another direction (v43)

The v41 tool-wear tape replays all 2121 turns exactly. Replacement succeeded and the same Explore task resumed. Its prepared downward move then failed `observed_route_unavailable` at tick 246. Search abandoned that descent and moved horizontally, shifting the tunnel above the fixture ore before eventually exhausting alternatives. The charcoal and blocked-return runs in that batch also replay exactly (580 and 2566 turns respectively).

Explore now suspends a failed movement into the same bounded Access child used by harvesting and return travel. It preserves the destination, task continuation, reservations, and remembered route. Access does not immediately repeat an unchanged failed direct move, but can still observe or clear an eligible obstruction on that edge before retrying it. A failed Access child returns to bounded search alternatives rather than recursively requesting itself. All 130 System 1 and 35 evaluator tests pass, including obstruction preparation and unchanged-movement rejection. Live regression is pending; Access still lacks the full maintained-lighting coverage required by the final design.

The v41 original run `20260908-005901-434777-25993/01-iron-pickaxe` finalized FAILED in 19516 elapsed ticks with `command_budget_exhausted`, harness OK, released motor, and normal client exit. All 19492 decision turns now replay exactly (the earlier row count also included the footer). Later wood searches again consumed substantial work; their initiating failure still requires diagnosis. The original sanity gate remains unproven for the current policy.

The v43 regression batch `20260908-012314-371229-31961` PASSED descent (539 elapsed ticks, 474 replayed turns), lighting exhaustion (411, 345), and tool wear (555, 487). All three have harness OK, zero LLM calls, released motors, normal client exits, and complete automatic inspection. Exact replay was verified separately. The tool-replacement child starts at tick 121; the access repair later in that run belongs to Gather. These runs establish current integration passes, but do not independently reproduce the specific failed Explore movement seen in v41 or inject delayed lighting feedback.

### Keep distant approach goals while planning local work (v44)

The v41 original exposed iron at tick 5628, but its selected approach was roughly ten horizontal blocks away. Two navigation failures immediately produced `access_goal_out_of_range`; Gather then left the ore and later exhausted its harvest search. The six-block local search radius had inadvertently become a maximum distance for the entire access task.

Access now retains its ultimate destination and searches a six-block horizon around the current observed position. For a distant destination, it can select a reached horizon frontier that makes geometric progress, ranked by the same deterministic path estimate. Every next step still passes the existing observation, clearable-block, protected-footing, and hazard checks. The 512-expansion per-decision limit, 48-command total, and original 1000-tick deadline remain. There is no larger Baritone fall limit or hidden target scan. This is bounded local heuristic planning; it can still fail when no useful horizon frontier is found.

All 131 System 1 and 35 evaluator tests pass. A twelve-block corridor regression requires clearing an observed head obstruction and reaching the original destination across multiple horizons; the original deadline still expires after progress. A pure probe against the recorded tick-5641 observation now selects a neighboring navigation step instead of rejecting the distant goal. This probe is not live execution. The new `system-one-distant-approach` fixture extends the existing lit lower-chamber geometry with ore twelve blocks away and requires completed access preparation plus raw iron. The existing near fixture and original iron-pickaxe scenario retain their geometry and checks. Live validation is pending.

`20260908-012935-310142-33536/01-system-one-distant-approach` PASSED in 332 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 265 turns replay exactly and automatic inspection is complete. At tick 104, failed navigation from (0,200,15) suspended Gather into Access. It observed and prepared the ledge, then advanced to the original approach (0,198,26) at tick 306. The same Gather task resumed and broke ore at (0,198,27). The trace issued six Break commands, traveled 16.247 observed horizontal blocks, and maintained health 20. This proves a live approach beyond one six-block planning horizon. It does not prove broad terrain reliability or visual Recorder Play playback. The unchanged original v44 run is active in `20260908-012939-903912-33602/01-iron-pickaxe`.

### Preserve a resource trip across productive motor legs (v45)

The v41 original's birch supply trip starts at tick 11753 from (5,16,-48) toward the observed approach (9,67,-11). At 11862 the motor reaches its travel budget after moving to (5,25,-32). Gather marked the destination visited and switched to another stance despite substantial progress. Repeated stance rejection can exhaust a long supply trip even when the route is making progress.

Gather now records the observed origin of each issued command. After `navigation_budget_exhausted`, it can continue the same destination if the observed position is at least half a block closer, the destination remains a safe observed stance, and a still-identified resource remains reachable from it. A stalled/regressing leg, unavailable route, vanished resource, or absent command-origin evidence does not qualify. Motor and root budgets are unchanged; this is not unlimited navigation or a retry of every failure. Drop-only pickup and generic survey travel retain their existing recovery.

All 133 System 1 and 35 evaluator tests pass. New tests cover productive continuation and refusal after stalled/regressing movement, route unavailability, and disappearing resource evidence. A pure probe on the saved tick-11862 observation now retains (9,67,-11), while the recording selected (9,67,-10). This is decision-level evidence; the live remote-fuel regression is pending. The running v44 original retains its loaded policy and matching saved classes.

`20260908-013638-202020-35127/01-system-one-remote-fuel` PASSED under v45 in 1076 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. All 1010 turns replay exactly; automatic inspection is complete. At tick 572, a budget-limited leg ends at (0,196,32), and the same Gather task retains its destination (0,197,18). It completes wood supply and returns to smelt an ingot. This exercises the new continuation in live execution. Observed horizontal travel is 112.034 blocks and health stays 20. A read-only live screenshot (`live-lighting.png` in that run directory) was visually inspected: placed wall torches illuminate the tunnel walls and floor. This is one live frame, not full Recorder Play playback or all-run lighting validation.

The unchanged v44 original `20260908-012939-903912-33602/01-iron-pickaxe` FAILED in 7185 elapsed ticks with harness OK, zero LLM calls, released motor, and normal client exit. All 7157 turns replay exactly. It retains three raw iron, a stone pickaxe, one spruce plank, and four torches. The first failed child is `smelt_start_failed:furnace_not_observed` at tick 7213; recipe alternatives then exhaust. At 7210 Station requests completion while navigation is active and the player is jumping. Release is acknowledged at 7211 and StartSmelt targets the remembered furnace (-16,70,1); its live ray check fails next tick. Station currently tests transient reach before `view.acting()`, so it can end an approach mid-jump. That premature completion is a concrete next repair candidate; the recording alone does not establish which exact ray was occluded, and no fix or fresh live proof for it is claimed here. All evaluation processes from this checkpoint are terminal.

### Finish workstation approach before testing reach (v46)

A focused regression reproduces Station returning Complete from a reachable furnace while its navigation command is active. The task now waits for the active command to finish before testing workstation reach. This prevents transient visibility during a jump from ending the approach early. Command ownership and motor arrival rules are unchanged; this does not claim to reconcile arbitrary furnace contents or recover all interaction failures.

All 134 System 1 and 35 evaluator tests pass. The new `system-one-station-stairs` fixture supplies raw iron and coal, with a furnace above four stair rises. Its checks require an observed ingot and completed navigation. Live validation and a fresh original run are pending. The existing original scenario remains unchanged.

The initial stair-fixture launch `20260908-014332-662858-36883` did not exercise the intended approach: the furnace was hidden behind the stair crest and the fixture inherited the default downward starting view. It began manufacturing another furnace instead. The supporting fixture now exposes the furnace beside the upper landing and starts looking toward it; inventory, goal, navigation-completion check, and budget remain unchanged. This is a correction to isolate the approach case, not a claimed pass of workstation discovery in the earlier occluded layout. Evaluator compilation and all 35 evaluator tests pass after the correction. The original scenario is unmodified.

### Use access preparation for known workstations (v47)

The corrected v46 fixture `20260908-014700-938488-37827` identifies the furnace in its first observation. Its known upper approaches all fail `observed_route_unavailable`, because lower stair geometry has not yet been observed. Station rejects eight stances and starts manufacturing another furnace. Unlike Gather and Explore, it did not invoke shared local access preparation.

Station now suspends into Access after a failed approach that can reach a still-identified workstation. It retains the original Station task, reservations, and rejected stances. Access uses the domain's declared excavation materials and existing observation/footing/hazard rules; unknown workstations do not authorize preparation. A failed child returns to the existing bounded alternatives. All 135 System 1 and 35 evaluator tests pass, including task preservation, unknown-target exclusion, and no recursive re-entry after a failed Access child.

The two v46 stair attempts were deliberately interrupted by the agent after their diagnostic failure paths were established, rather than waiting through irrelevant wood searches. Neither is a terminal scenario failure or pass. Preserve their harness interruption/finalization evidence separately; the v46 original was not stopped. A v47 run of the corrected fixture is pending.

`20260908-015224-900782-39030/01-system-one-station-stairs` PASSED under v47 in 336 elapsed ticks with harness OK, zero LLM calls, and fully released motor. All 271 turns replay exactly; automatic inspection is complete. At tick 101 Station suspends into Access. Navigation completes and Access reaches its stance at 136; Station then completes and smelting obtains the ingot. Observed travel is 3.010 horizontal blocks; health stays 20. The capture finalized, but the harness needed SIGINT after its shutdown wait and the client exited 130. This proves the live approach/repair/smelting sequence, not graceful shutdown or visual playback.

Both deliberately stopped v46 stair attempts finalized with harness INTERRUPTED and normal client exits (0). Their decision coverage is correctly marked incomplete; no exact complete replay or scenario outcome is claimed. The original v46 run remains active in `20260908-014337-449876-36955/01-iron-pickaxe` (last verified report exceeded 11436 elapsed ticks). Its matching classes are saved separately from v47. Workstation access recovery, broader maintained conditions, held-out evaluation, migration/deletion, and game-completion gates still need their full required evidence; this single supporting fixture does not close those gates.

### Record lighting allowance state before and after decisions (v48)

Production recording format 2 adds structured lighting snapshots for both sides of each decision. Each entry identifies the task, lifecycle phase, leaf status, continuation context, maintenance state, failed repairs, and allowance origin/deadline. The trace also records the light value supplied to the policy (including its existing zero fallback for an unobserved eye cell). Nested survival/access/resupply continuations remain distinguishable from active exploration. Serialization remains on the recording writer; the client tick constructs immutable values rather than JSON text.

Exact replay now verifies these snapshots as well as decisions and rejects missing or altered lighting evidence. The inspector counts observed allowance instances and post-decision states, distinguishing acting Explore leaves from suspended/returning states. Expired active-state samples are reported separately. These are policy-state measurements, not physical dark-exposure ticks or perception audits; those unproven quantities remain unavailable. Old recordings remain inspectable without invented zero allowance counts, and missing lighting data in format 2 makes coverage incomplete.

All 137 System 1, 35 evaluator, and 35 Python tests pass. Tests cover retained deadlines during delayed feedback, nested suspended context, altered/missing trace evidence, expired active versus suspended samples, and recorded empty state. Live capture/replay of the new format is pending.

The unchanged v47 original `20260908-015650-203149-40123/01-iron-pickaxe` PASSED in 4233 elapsed ticks, harness OK, zero LLM calls, fully released motor, normal client exit. Automatic inspection is complete. Matching replay is queued after the v46 original replay and is not yet claimed. This is a current original sanity pass, not held-out reliability, migration/deletion, or game-completion proof. The concurrent v46 original finalized FAILED in 18866 elapsed ticks (`command_budget_exhausted`), harness OK, released motor, normal client exit; its repeated workstation replacement attempts led into unproductive wood searches.

The v46 original replay completed: all 18837 turns match its failure. The v47 original replay completed: all 4214 turns match inventory success. The original sanity pass now has exact replay proof in addition to the previously recorded harness, no-LLM, and motor evidence. Recorded travel was 105.529 horizontal blocks, with 94 Break attempts and health 20 throughout. These measurements do not establish blocks destroyed, actual total damage, or perception violations.

`20260908-020552-699741-42041` PASSED v48 lighting exhaustion (408 elapsed ticks, 345 exact replay turns) and low ceiling (331, 268). Both harnesses are OK with normal exits, zero LLM calls, and released motors. New-format lighting state is present for every turn and verifies during replay. Neither run used an allowance: zero instances and active samples are recorded rather than inferred from missing data. Nonzero allowance deadline/continuation evidence remains unit-tested; a live relaxation/injected-feedback case is still required.

`docs/evaluations/system-one-development-v48.json` freezes the 26 supporting fixtures plus the original scenario, their scenario/world hashes, source commit, concurrency, and acceptance requirements before running the development regression. It uses existing scenario budgets without overrides. This is not the ten-world held-out release set, and unavailable perception auditing cannot satisfy its separate zero-violation requirement. Keep the compiled/runtime source fixed while this batch runs.

## v48 fixed regression and v49 retained Access routes

The frozen development batch `20260908-021115-699700-43287` completed with all 26 supporting fixtures PASSED and the original `iron-pickaxe` FAILED after 18464 elapsed ticks (`command_budget_exhausted`). All 27 clients exited normally, their harnesses reported OK, all had zero LLM calls and released motors, and all decision recordings have complete coverage and exact matching-v48 replay. The failed original contains 18439 replayed turns. Per-scenario evidence is in [the result manifest](evaluations/system-one-development-v48-results.json). This suite fails its acceptance condition despite all supporting fixtures passing.

The original recording exposed repeated Access attempts returning from underground toward a known surface destination. At ticks 15140–15354, Access alternated between (-1,20,-21) and (-2,20,-21) until its 48-command work budget expired. A frozen-observation simulation reproduced the same cycle for twelve commands. The local search recomputed its horizon frontier after every successful step, making a different frontier preferable as the search origin moved.

Policy v49 retains the full selected local route until traversed or invalidated. Every next edge still passes current eligibility, clearance, support, and observed interaction checks. Failed execution rejects that edge and replans; unchanged looks remain bounded. The original destination, 1000-tick deadline, and 48-command budget are preserved. A small two-frontier uphill counterexample fails under v48 and passes under v49; additional coverage checks new hazards and failed edges. The System 1 and evaluator suites and 35 Python tests pass. Live validation of v49 is pending. This is useful flight-recorder diagnosis evidence, not a claim of game completion or reliable original-world success.

The v49 live batch `20260908-023751-924146-50371` runs the original world, `iron-pickaxe-development-01`, distant approach, and workstation stairs with two clients. Its runtime revision is `e561e8b6`; no runtime/build sources have changed during the run. Development seed 01 loaded and its first recorded observation (tick 39) has an empty inventory at (-72,77,-550). Its configuration-only archive contains the frozen seed -8922710515911108288. Reading the saved level.dat during play still showed the pre-save `initialized=false` configuration; persisted generated spawn metadata has not yet been verified. A screenshot request timed out and produced no image, so no visual proof is claimed.

The development world exposes additional issues for the next iteration: logs were broken but not collected, and the source restricts Gather's Access repair to items with an underground search prior. Dark-oak harvesting has no such prior. The client continues advancing; a read-only thread dump located substantial render-thread work in repeated recipe cost estimation. A separate probe over 25428 recorded remembered cells measured roughly 1244 ms for wooden-pickaxe method ranking and 933 ms for stone-pickaxe ranking (single cold measurements, not a benchmark suite). Each recursive harvest estimate rescans the same observations. A per-decision index of observed harvest costs can remove that repeated work without caching across changing observations. These diagnoses are pending repairs; the current run remains unchanged.

### v49 completed live checks and recipe-cost indexing

The original run `20260908-023751-924146-50371/01-iron-pickaxe` PASSED in 7940 elapsed ticks; all 7917 decisions replay exactly. Distant approach PASSED in 328 ticks / 261 replayed turns, and workstation stairs PASSED in 338 ticks / 272 replayed turns. All three harnesses report OK and normal client exit 0. The remaining development run was deliberately stopped through runner SIGINT after the collection and performance failures were diagnosed; its harness is INTERRUPTED, client exit 0, and its gameplay outcome remains RUNNING, not PASSED or FAILED. This is a diagnostic checkpoint, not a release result.

After shutdown, the development world saved `initialized=true`, seed -8922710515911108288, spawn (-64,79,-544), Survival game type and Peaceful difficulty. Its first observed player position is within the configured natural spawn radius and its inventory is empty. This verifies the configuration-only world's live initialization; gameplay competence remains unproven.

Recipe and fuel ranking now build one local index of nearest identified block costs per decision and share it across recursive estimates. The index is discarded after ranking; observations, reservations, recursion budgets, and method ordering retain their previous meaning. The 25428-cell recorded input gave the same five sets of method rankings. Cold pickaxe-ranking probes fell from 1832/1001 ms to 16/6 ms; these timings include JVM warm-up and concurrent activity and are only diagnostic. All three completed v49 recordings replay exactly through the optimized code, providing stronger behavior-equivalence evidence than the timing probe. All 139 System 1 and 35 evaluator tests pass. Live timing under the optimized runtime is pending.

### v50 surface acquisition access

The interrupted independent seed showed a domain-boundary mistake: surface log harvesting could request navigation and breaking, but its failed approaches could only create Access children if the harvested item also had a geological search prior. Policy v50 removes that condition. Failed movement to an observed harvest target or a remembered drop now requests the same bounded Access child as ore collection. Pickup repair is limited to actual pickup-approach candidates rather than any failed movement after mining.

The knowledge catalog separately records approach-clearance materials; Minecraft supplies ordinary excavation materials and registered leaves. This permits surface canopy clearance without inventing an underground prior for logs. Unknown blocks, current footing, hazards, reach, and tool eligibility retain their existing guards. Existing constructors can derive the minimal materials from declared excavation priors; the Minecraft adapter supplies the explicit independent list. Catalog version is v6 and decision policy v50.

A regression with log harvesting and no underground priors fails before the repair and passes afterward. The new `system-one-log-pickup` live fixture starts empty-handed on a dirt ledge above a visible log and requires inventory acquisition plus Access interruption and successful resumption. Original fixtures are unchanged. All 140 System 1, 35 evaluator, and 35 Python tests pass. Live proof of v50 is pending. The completed v49 original and both Access fixtures separately confirm zero LLM calls, released motors, complete recording coverage, normal client exits, and exact replay.

The v50 log fixture `20260908-025609-314937-54321/02-system-one-log-pickup` PASSED in 416 elapsed ticks. All 346 decisions replay exactly; harness OK, client exit 0, zero LLM calls, complete coverage, and released motor. Access ran at ticks 106–281, preparing dirt stairs before log mining; the log was then mined and collected. Thus the fixture proves shared surface approach plus collection, while repair specifically after a failed mined-drop approach remains unit-tested rather than isolated live proof.

The same batch's independent development seed is still running. By recorded tick 2818 it has collected logs, made a table and wooden pickaxe, and begun stone acquisition; the previous interrupted run never collected a log. A read-only screenshot succeeded promptly and was visually inspected (`/private/tmp/airicraft-development-01-v50-live.png`): it shows close tree bark and foliage. This is live rendering evidence, not Recorder Play playback or full-run lighting proof. A separate v50 original-world regression was launched after the log fixture finalized. Runtime sources remain at `ceb4aa75` during both active runs.

## v50 independent failure and v51 stone-search repair

Development seed 01 (`20260908-025609-314937-54321/01-iron-pickaxe-development-01`) reached a wooden pickaxe but FAILED in 8310 elapsed ticks at the command budget. All 8286 decisions replay exactly. The stone task repeatedly moved between (-76,77,-546) and (-76,77,-548), with four survey looks after each arrival. Its retained route already included both positions; frontier selection ignored that route. Several remembered grass-supported standing positions existed below the canopy, and an earlier failed ground approach had been abandoned without Access repair.

The v50 original (`20260908-025938-009874-55209/01-iron-pickaxe`) separately FAILED in 11406 elapsed ticks. All 11389 decisions replay exactly. It exhausted the underground method at tick 10824 (`no_observed_underground_step`) and exposed harvesting at 11430, then failed the production chain. Its final position was (-9,35,52), with no iron obtained. Both failed runs have harness OK, normal client exit 0, released motors, complete recording coverage, and zero LLM calls. The earlier v47/v49 original passes do not establish repeatability.

Policy v51 excludes already visited exploration destinations from local stone search and gives observed soil/stone support priority over nearer canopy support. Resource interactions and return paths can still use visited positions; this only limits exploration destination selection. Failed navigation to a safe, observed stone-search stance uses the shared Access child, preserving the stone task's origin, route, goal, and failure budget. Failed Access returns to bounded alternatives rather than recursively retrying. Three isolated regressions fail before and pass after; the full 143 System 1, 35 evaluator, and 35 Python tests pass. The stone recording method is v11; the production method is v51.

The new `system-one-stone-canopy` fixture starts with a wooden pickaxe on persistent leaves above visible ground. It requires three cobblestone plus successful Access interruption/resumption. No runtime code changed while the v50 clients ran; the candidate repair was compiled separately under `/private/tmp` before promotion after both clients exited. Live validation of v51 is pending. The original's later underground dead end remains a separate open diagnosis.

### v51 live outcomes and next failure boundaries

Batch `20260908-031515-491325-59308` completed: existing stone PASSED in 427 elapsed ticks / 351 replayed decisions; canopy stone FAILED in 370 ticks / 304 decisions; development seed 01 FAILED in 3692 ticks / 3671 decisions. All three have harness OK, normal client exit 0, released motors, zero LLM calls, complete coverage, and exact matching-v51 replay. The new canopy fixture remains failing; it is not an accepted success case.

The independent seed did progress through the earlier defect: it collected cobblestone at ticks 2930, 2971, and 3001 and crafted a stone pickaxe by tick 3107. Its underground search failed at tick 3126. Recorded neighbor cells show walkable, identified leaf litter above unknown support. The sensor conservatively stops rays at that occupied voxel, so looking at the soil below does not expose it. The search treats the litter as traversable and never clears it to obtain the missing support observation. The next repair should explicitly remove an eligible observed obstruction before requiring a new observation, keeping the hidden-ground contract intact. There is no evidence of pickaxe loss in this run; counts omitted during crafting were transient recipe transfers.

The canopy fixture's first Access attempt navigated around the top, cleared leaves into a lower step, then failed movement to (-1,198,14). At tick 246 the body was grounded partly over the supporting leaf at (-1,197,14), while its feet voxel was (-2,198,14) over air. Subsequent Access requests returned `no_local_access_proposal`; the stone task exhausted its alternatives. This needs a separate local-access diagnosis involving actual support contact, retained footing, and feasible future clearance. No root cause beyond those recorded facts is claimed yet.

The v50 original's underground failure is now localized more precisely: before tick 10824 the search had reached preferred Y16, with 73 of 96 steps used, origin (-6,69,18), and an approach to (-17,16,81) near its 64-block horizontal boundary. Forward/outer moves exceeded the region, the inward side was previously visited footing, and the fixed-heading rule unconditionally rejected the remaining backward direction. The prior heading should rank exploration choices rather than forbid an otherwise eligible unvisited inward step. This diagnosis does not justify increasing the frozen work or travel budgets.

## v52 observation preparation and inward exploration

The recorded litter and region-boundary failures have failing-before/passing-after regressions. The remembered-ray module can identify an already observed obstruction between the eye and an inspection location. Access and underground exploration may clear that obstruction only when its identity is known, the domain permits clearing it, it passes hazard/footing checks, and a current interaction is possible. They then wait for an actual observation of the previously hidden support. Unknown cells never authorize a break, and removing a plant is not treated as proof of the block underneath.

The Minecraft knowledge adapter derives additional clearance materials from replaceable, non-air, fluid-free block states, excluding fire. Leaf litter's replaceable/no-collision registration was verified in the current Yarn Minecraft bytecode. The standalone raw-Minecraft bootstrap probe failed at an access-widener-dependent registry method, so it does not count as a live catalog proof. The catalog is v7 and the decision policy v52.

Search heading remains a preference: backward candidates are considered after viable forward/lateral alternatives, while an excavation already being prepared remains retained. The unconditional backward prohibition is removed; region, steps, hazards, and previously used footing still bound candidates. The exact original-region counterexample can select an unvisited inward step without resetting its 73-step progress or increasing its radius.

New fixtures `system-one-obscured-support` and `system-one-cave-turn` require raw-iron inventory and structured block-break evidence. Host motor-effect events now include command type and the expected block ID for breaks; existing effect text is retained. This is issued-command evidence, not a new perception-violation audit. All 146 System 1, 35 evaluator, and 35 Python tests pass. Live validation of v52 is pending; the v51 canopy-access failure remains open.

### v52 targeted live verification

The first fixture batch `20260908-033757-475093-64049` FAILED both cave-turn (118 elapsed ticks / 47 replayed decisions) and obscured-support (87 ticks / 13 decisions). Both failures replay exactly with v52. Recorded observations localize a different obstruction: a floor torch occupies the conservative sensor voxel between the eye and the floor being inspected. In the litter fixture it was the torch under the player's eye, before the litter; in the turning corridor it was the next floor torch. Both have harness OK, normal exit 0, released motors, and zero LLM calls. General inspection around floor torches remains unresolved; these failures are preserved.

The fixtures now use wall torches at Y202 to isolate their intended litter-clearing and backward-exploration behaviors. Runtime policy and the visibility contract are unchanged. Batch `20260908-034812-885849-66472` PASSED both: cave-turn in 186 elapsed ticks / 111 exactly replayed decisions; obscured-support in 219 ticks / 146 decisions. Both have harness OK, normal client exit 0, SUCCEEDED inventory evidence, released motors, complete recording coverage, and zero LLM calls.

The cave recording issues the inward exploration step from (-2,200,21) toward (-2,200,20) before observing iron and transferring to harvesting. The litter recording first issues a leaf-litter break, then observes/excavates stone and descends to the ore. The recorded live catalog contains leaf litter in its excavation materials. These runs prove the targeted behaviors, not the floor-torch case, canopy access, original-scenario reliability, or the still-unavailable perception-violation audit. Recorder Plays finalized, but visual review of these runs is still pending.

### v52 broader validation in progress

Batch `20260908-035018-737987-67378` runs the unchanged original iron-pickaxe world and development seed 01 under the same v52 runtime. Both are still active; no outcome is claimed. Development seed 01 crafted a stone pickaxe by tick 2783 and continued beyond its v51 failure boundary. A read-only live screenshot at `/private/tmp/airicraft-development-01-v52-live.png` was inspected; it shows nearby tree bark. It is a single rendered frame, not full-run lighting or Recorder Play validation.

An offline canopy counterfactual using the v51 recording at tick 259 retains every observed cell and protected foothold, but centers the player on the actual contact support (-1,197,14). Access still returns `no_local_access_proposal`. Thus recentering alone does not solve the recorded canopy failure. Dropping historical protections changes the result, but that does not justify removing them without preserving a valid return route. No runtime change was made from this probe.

### v52 original and development search budgets

Batch `20260908-035018-737987-67378` finalized with both FAILED: original 13302 elapsed ticks / 13281 exactly replayed decisions; development seed 01 15113 ticks / 15083 decisions. Both have harness OK, normal client exit 0, complete recording coverage, zero LLM calls, and released motors. Original search failed at tick 12668 and development search at 14482, both with `underground_search_budget_exhausted`; the exposed-harvesting fallback then failed. No iron-pickaxe success is claimed for v52.

The original's exact pre-failure task state has 95 steps and a pending navigation from (-33,16,76) to (-33,16,75); arrival consumes step 96. Its route reached Y16 and continued inward around the 64-block boundary. This confirms the v52 heading repair progresses past the old boundary dead end, but the single bounded search still did not expose iron. The result calls for better resource-search alternatives, not a claim that the original check passed. Original lighting supply at tick 4265 returned to the surface, produced eight torches by 5371, and the same exploration task resumed placement at 5644. A second screenshot (`/private/tmp/airicraft-original-v52-live.png`) shows surface workstations during a repair trip; it does not show underground illumination.

### Live feedback-delivery fault fixture

A per-mission host delivery seam defaults to immediate delivery. Only the evaluator's `system-one-feedback-delivery` fixture selects a fault injector: the first `Finished` is held for 20 ticks and duplicated five ticks after its delayed delivery. Other feedback, including release acknowledgements, passes through. Raw motor receipt events are recorded before delivery; the decision tape records the actual delivered inputs. The injector never changes motor execution or command tokens, and normal mission starts restore immediate delivery.

Two focused tests exercise delayed ownership progression, a stale duplicate while a later command is acting, and unaffected release/subsequent feedback. The existing 146 System 1 and 35 evaluator tests plus both new evaluator tests pass; all 35 Python tests pass. The live fixture requires three cobblestone and a recorded `feedback_ignored` event. Its exact delay, token identity, command ordering, and replay must still be checked live; implementation alone does not close that gate.

The live feedback fixture `20260908-041003-013917-73253/01-system-one-feedback-delivery` PASSED in 436 elapsed ticks, with all 371 decisions replayed exactly. Raw completion for attempt 1 arrived at tick 106, but the kernel received it at 126. No new command started during that 20-tick delay. Attempt 2 started at 126; the old completion arrived again at 131 and produced `feedback_ignored` while attempt 2 was still active. The later command's actual motor receipt occurred after the duplicate. The goal obtained three cobblestone, harness status is OK, client exit is 0, the motor is fully released, trace coverage is complete, and LLM calls are zero. The saved receipt events and decision inputs establish the cause without adding logging after the run. Structured evidence is in `docs/evaluations/system-one-feedback-delivery-v52.json`. Recorder Play finalized; visual playback remains pending. This closes the targeted live delayed/duplicate feedback proof, not the full recorder or redesign acceptance gates.

## v53 bounded follow-on search areas

The v52 failure was a completed local search with no observed iron, not proof that the mission had no remaining acquisition method. The domain search prior now states a finite area allowance. Existing constructors retain one area; the Minecraft raw-iron prior explicitly permits three, each with the existing 64-block radius and 96-step limit. Coal remains one area. The global 72000-tick / 2000-command runtime limits and frozen evaluator travel/time thresholds are unchanged. This increases the domain's permitted search coverage; it is not a claim that v52 passed its original budget.

A new area can begin only after successful arrival at the previous area's step limit, on observed eligible standing support, at least half a radius away from every earlier area center. Its center is the player's actual observed position. It does not fabricate an ore coordinate or clear previous observations/foothold protection. The full return route survives, including lighting/tool interruptions. Failed motion, unconfirmed support, circling near a previous center, and the exhausted area allowance terminate rather than restart. Search-area progress is included in issued motor-effect evidence.

Two reduced-recording regressions fail before and pass after: continuing from the v52 original's last stance, and carrying the continuation through the production wrapper into a lighting repair. A third test covers unproductive/unsafe reset attempts and the finite allowance. The production wrapper now preserves `Keep` continuations from underground search. All 149 System 1, 37 evaluator, and 35 Python tests pass. Decision policy is v53 and the captured knowledge catalog v8.

`system-one-search-areas` is a lit, bent passage within the first area's spatial radius, long enough to use 96 exploration steps before the ore becomes visible. It requires raw-iron inventory plus an issued command in search area 2. The original and development worlds are unchanged. Live validation is pending.

V53 targeted batch `20260908-042322-353525-76431` PASSED both fixtures: cave-turn 185 elapsed ticks / 115 exactly replayed decisions; search-areas 913 ticks / 828 decisions. Both have harness OK, normal client exit 0, SUCCEEDED inventory evidence, released motors, complete trace coverage, and zero LLM calls. Search area 2's first command is at tick 746, anchored at the observed stance (-20,200,59), with its local step count 0. Iron is first observed at tick 803, after the transition; nine search commands use area 2 before harvesting takes over. This verifies live continuation beyond one search budget without supplying ore coordinates. Original/development validation of v53 is still pending, and this fixture does not establish broader resource-search reliability.

### Fresh Recorder Play visual review

The completed feedback-delivery Play was rendered with the external recorder CLI in `--no-gui` mode. Airicraft's Gradle 9.3 wrapper could not load the renderer's Loom plugin; retrying with the existing Gradle 9.5.1 distribution succeeded. No recorder source or capture input was changed, and the unrelated recorder dashboard merge conflicts remain untouched.

`20260908-041003-013917-73253/01-system-one-feedback-delivery/recording-render-no-gui/fpv.mp4` contains 469 frames at 640×360 / 20 fps. Frames 90, 180, 300, and 450 were visually inspected: a surface opening, descent among dirt faces, and later stone excavation are visible. There are 276 distinct PNG hashes, with the last image change at ordinal 424. The capture first records three cobblestone at recorder tick 450 and settles at its final pose by 457; unchanged images at the end therefore agree with the captured stationary post-goal state rather than the earlier v18 premature camera freeze.

The renderer covers recorder ticks 34–502 out of requested 34–522. The last 20 ticks are omitted; capture pose and inventory remain unchanged through that tail. This is a useful visually reviewed gameplay sequence through the goal, not proof of a complete-capture export or a fix for the earlier GUI-rendering failure. The no-GUI video was opened in Codex. Structured evidence is appended to `docs/evaluations/system-one-feedback-delivery-v52.json`. These recorder ticks are not asserted to align with runtime decision ticks.


### V53 original and first independent-world outcome

Batch `20260908-042744-585063-77491` finalized. Development seed 01 PASSED in 17887 elapsed ticks / 17865 exactly replayed decisions, obtaining an iron pickaxe with 896.125 observed horizontal blocks of travel. Original FAILED in 19975 elapsed ticks / 19954 exactly replayed decisions with `command_budget_exhausted`. Both have harness OK, client exit 0, released motors, complete recording coverage, and zero LLM calls. Perception-violation counting remains unavailable. This is one development-world success, not held-out reliability proof.

The original entered area 2, then requested another torch supply at tick 14538 from (-34,16,85). Its inventory had two sticks, one spruce plank, a stone pickaxe, and excavation materials but no coal, charcoal, torches, or logs. Memory still contained three oak-log cells and one birch-log cell. The supply task tried successive log/wood production alternatives from the underground passage rather than using the saved route to leave it. By command 2000 it had exhausted the mission budget. Thus retained source identities alone did not provide an executable approach to supplies.

### V54 explicit outbound resupply

Lighting resupply now has an outbound task carrying the reversed, observed exploration route and the requested acquisition. It follows bounded return-navigation legs, invokes the existing local Access repair on a failed leg, and only begins ingredient acquisition at the exploration entrance. The suspended Explore keeps its complete forward route and original target for the later return. Route cells remain retained while outbound. No source coordinate is fabricated.

If a recipe can make the complete required torch batch from unreserved inventory, crafting still begins in place. Otherwise the initial heuristic returns to the exploration entrance; it does not yet optimize partial crafting chains or select among multiple supply locations. A failed outbound route becomes a failed lighting repair and proceeds through the existing relaxation/retreat policy.

A regression fails before the change because acquisition starts at the cave position; it passes after the change by verifying the outbound navigation, retained return continuation, and acquisition beginning only after arrival. Existing in-place torch crafting and placement/resumption tests remain passing. All 150 System 1, 37 evaluator, and 35 Python tests pass. Policy is v54; knowledge remains v8. Live lighting, long resupply, and blocked-return fixtures are running.


V54 targeted batch `20260908-044906-800979-82538` PASSED all three fixtures: in-place lighting 295 elapsed ticks / 222 exactly replayed decisions; blocked return 1867 / 1795; ordinary return 1826 / 1758. All have harness OK, client exit 0, inventory success, released motors, complete recording coverage, and zero LLM calls.

In the blocked-return run, torches were removed at runtime tick 476 after reaching Z67. Supply task 11 began at 496 and followed the saved route through Z54, Z38, Z22, then entrance (0,200,15). Only then did resource production proceed. Eight torches were observed at tick 1455; the fixture then blocked the remembered passage at (0,196,43). The same exploration task requested Access at 1593, completed it at 1662, resumed lighting placement at 1768, and observed iron at 1844 before inventory success. This proves outbound supply, blocked inbound repair, and resumed purpose in the fixture, not original-world reliability. Recorder Plays finalized; these particular Plays have not been visually reviewed.

The unchanged original scenario and all three frozen development seeds are now scheduled under v54, with two isolated clients at a time. No held-out seed has been generated or inspected. Their outcomes remain pending; policy and compiled classes remain fixed while the batch runs.


### V54 complete fixed-development outcomes

Evaluation run `20260908-045254-219031-83590` finalized all four scenarios. Original FAILED in 1476 elapsed ticks / 1452 exactly replayed decisions. Development 01 PASSED in 8081 / 8055. Development 02 FAILED in 2988 / 2969; development 03 FAILED in 7365 / 7346. Every scenario has harness OK, client exit 0, complete recording coverage, released motors, and zero LLM calls. All development seeds have now run; no held-out seed has been generated or inspected.

Original crafted a stone pickaxe at tick 1030, then failed underground exploration at 1036 (`no_observed_underground_step`). Development 02 reached a stone pickaxe at 2232, completed Access at 2477, then failed exploration at 2479 with the same code. Development 03 supplied torches and replaced its stone pickaxe before exploration failed at 6697. These are distinct from the v53 command-budget failure during underground wood discovery; v54's outbound resupply is not a proven cure for all exploration failures.

### Canopy protection counterfactuals

The v51 canopy recording at tick 259 still reproduces `no_local_access_proposal` against v54's local-access code. Increasing the planning expansion cap from 512 to 8192 in an isolated `/private/tmp` copy does not change that result. None of the other observed lower standing goals produces a route with all historical footing protected. Removing a single historical support from protection does produce a proposal: independently (-1,199,15), (0,198,14), or (1,199,16). Actual current contact (-1,197,14) stays protected in every probe. The suspended stone task's explicit route contains only its original stance (0,200,15).

These are counterfactual proposals, not evidence that clearing any of those cells preserves a return path. The remaining design problem is to separate visited footing from support reserved for current contact and viable return connectivity. Do not solve it by dropping every historical protection or merely increasing search budgets. Probe sources/logs remain under `/private/tmp/CanopyProtectionProbe.java`, `/private/tmp/canopy-capacity-src/`, and `/private/tmp/airicraft-canopy-protection-probe.log`; no experimental planning-cap change entered production.

### V55 real client-chunk availability

Original v54 sensing recorded 3840 `minecraft:void_air` cells at tick 54, including below the apparent ground. Current Yarn bytecode confirms that `ClientWorld.isChunkLoaded(int,int)` unconditionally returns true, while missing client chunks use an `EmptyChunk` returning void air. The sensor had called the former guard and then the fallback-producing world block accessor. This is an unavailable-data error, independent of legitimate geology.

The engine sampling boundary is extracted into `MinecraftScene`, used by the existing geometric lens. It obtains the actual client chunk with `getChunk(..., ChunkStatus.FULL, false)` and returns unknown obstructing geometry when absent or outside the world's height range. Block identity, light, and static collision reads occur only after that admission, against the loaded chunk; sampled engine states remain scoped to one observation pass.

The existing live terrain probe now first exercises the production sampler at a probe-only position outside the client chunk cache. Before the guard fix, run `20260908-050440-020932-86343` fails the assertion: the chunk manager reports false, the world convenience method reports true, and the sample is walkable void air at light 15. This happens before autonomous goal submission, so the harness reports `DECISION_TRACE_INCOMPLETE`; it is a reproduced adapter assertion, not a completed autonomous recording.

After the fix, `20260908-050717-759906-87039` PASSED in 425 elapsed ticks / 356 exactly replayed decisions. The unavailable sample is unknown, nonempty, nonsupporting, and not traversable. The existing paired Baritone probe remains equal across 102 changed hidden cells. Autonomous stone acquisition succeeds, harness is OK, client exit 0, motors released, recording coverage complete, and LLM calls zero. All 150 System 1, 37 evaluator, and 35 Python tests pass. Structured evidence is in `docs/evaluations/system-one-sensor-availability-v55.json`.

A separate counterfactual of the original v54 failure at tick 1036 removes every remembered void-air sample but still yields `no_observed_underground_step`. Removing historical footprint restrictions instead produces a grass-block excavation proposal. Thus the sensor bug is fixed and live-verified, but the original dead end still requires the support-reservation work described above. No broader iron-pickaxe or hidden-read audit gate is claimed closed by this patch.


### V56 return-support reservations

`SupportReservations` builds a walking graph entirely from remembered standable cells and bidirectional observed clearance. It keeps current body contact (including partial contact beside the feet voxel), active return waypoints, and observed connector paths to those waypoints. An old historical foothold can be released only when its neighbors remain connected by an observed bypass excluding that stance. Unknown clearance does not establish a bypass. Connectivity searches have a 2048-visit bound; inability to establish connectivity preserves historical protection. Derived reservations do not change recorded observation history or invent terrain.

The kernel exposes the complete active branch to domain decisions through a default-compatible decision overload, matching its existing interruption/reconsideration context. Minecraft production uses this branch to preserve suspended parents' routes while a child is active. Direct leaf calls use the leaf's own return constraints. Underground exploration excludes its own route when avoiding revisits, rather than treating every bootstrap footprint as already explored by the new task. A central break admission check preserves reservations, and changed connectivity during an owned break requests release before revising that task.

Five regressions cover the two reduced recorded failures, partial contact, a sole connector, explicit parent waypoints, removal of a bypass, unknown clearance, and stopping/waiting for release when an in-flight break becomes unsafe. Existing tests now express their exploration history/return route in task state instead of using global footing history for both meanings. The compressed fixtures in `src/test/resources/systemone/` record their provenance and retain only nearby remembered cells; they contain no evaluator ground truth. All 155 System 1, 37 evaluator, and 35 Python tests pass.

Against the full v51 canopy observation, the derived protected supports include the current contact (-1,197,14), original entrance support (0,199,15), and necessary connector (0,198,14), while the old top visit (-1,199,15) can be released. The original v54 tick-1036 decision yields a grass-block excavation at (0,67,5), both with recorded observations and with every remembered void-air cell removed. Those are counterfactual decision proofs, not a replayed successful Minecraft trajectory.

A standalone cost probe on a development observation with 19337 known cells and 99 historical footholds measured 12.7 ms for the first reservation calculation, 2.27 ms median and 4.56 ms p95 over 60 calls. This measures the new pure calculation only, not whole-client tick latency.

Live evaluation `20260908-052745-024554-91278` PASSED all three scenarios: blocked return 1891 elapsed ticks / 1821 exactly replayed decisions; search areas 907 / 827; canopy stone 382 / 318. All have harness OK, client exit 0, complete recording coverage, zero LLM calls, inventory success, and released motors. The canopy fixture previously failed under v51 and now completes its access/stone goal. This live run does not independently prove destruction of a historically visited support: its issued breaks did not target cells present in that tick's historical footing set. The precise release rule is covered by recorded-input regressions and the live runs exercise the integrated policy. No original-world reliability or full acceptance gate is claimed closed. Recorder Plays finalized; these particular Plays have not been visually reviewed.


### Measured per-run acceptance

`scripts/check-system-one-run <finalized-scenario-directory> --replay-classpath <matching-classes-and-gson>` recomputes the decision-tape inspection and runs exact replay directly against saved matching classes without rebuilding. It checks recorded goal and inventory, scenario/harness/client outcomes, no-LLM evidence, elapsed ticks, observed horizontal travel, command count, recording coverage, perception evidence, and motor release against `docs/evaluations/system-one-seeded-v1.json`. Missing evidence fails eligibility, including on failed scenarios; absent perception counts never mean zero. Exit 0 means these per-run checks passed; exit 1 means at least one failed or is unavailable. `--output` saves the structured report.

Five tests exercise budget boundaries, invalid/missing measurements, failed-run risk checks, incomplete records/replay, release state, and goal mismatch. All 40 Python tests pass. Running the literal checker against the previously successful v54 development-01 run (`20260908-045254-219031-83590/02-iron-pickaxe-development-01`) verifies exact replay but rejects eligibility solely for unavailable perception-audit status/count. This is intentional negative evidence: scenario success is insufficient for release. The checker covers one run's measured outcomes; world provenance, frozen revision, targeted-suite and visual-review evidence, and the full 8-of-10 held-out release-attempt aggregator remain separate unfinished gates.


### V56 development failures and radius diagnosis

In batch `20260908-053406-471041-92830`, original FAILED in 10905 elapsed ticks / 10883 exactly replayed decisions; development 01 FAILED in 12911 / 12890; development 02 FAILED in 7858 / 7840. These three finalized runs have harness OK, client exit 0, complete recording coverage, released motors, and zero planner turns. Development 03 also FAILED in 7911 elapsed ticks / 7892 recorded decisions. All four finalized runs have zero LLM calls with untruncated call histories; all four decision tapes now replay exactly with saved v56 classes.

Original exploration task 23 reached (-5,16,80), exactly 64 horizontal blocks from its current area origin (-5,69,16), at tick 10262. It had taken 68 local steps. Development 01 reached the same radius boundary in its own world at tick 12273 after 71 steps. Candidate selection excluded new cells outside radius 64, while area transition required the 96-step limit. Both therefore failed `no_observed_underground_step` with two of the three areas unused. The recorder reproduces the failure and isolates this conflict between spatial and step limits. An isolated counterfactual of the original recorded input selects a second area while preserving the return route when radius exhaustion also permits the bounded transition.

Development 02 is a separate case: exploration fails at tick 7240 around observed water and a partially supported position, well inside the area radius. The radius correction does not claim to resolve it. Earlier access failures in these runs were recovered before the terminal exploration failures. Remaining reliability is not established by the targeted v56 fixture passes.


### V57 spatial search-budget correction

Underground search now attempts the same finite area transition when spatial bounds exclude the remaining candidates, as well as at the step limit. In-area work still takes precedence. Both paths require a currently observed standable, eligible stance, observed arrival (a reached navigation destination or previously visited route stance), sufficient displacement from every previous area, and an unused area allowance. The transition retains the full return route and ignored resource targets. A survey or a repair consuming the movement receipt does not erase observed arrival. It does not increase the three-area allowance, travel/command limits, or sensor reach.

The two new regressions fail against v56 and pass against v57. They cover a 64-block radius reached in 68 steps, continued execution in the second area, exhausted allowance, survey after arrival, and resumption without a command receipt. The existing failed-movement regression now uses an actually unreached destination. All 157 System 1, 37 evaluator, and 40 Python tests pass. `system-one-search-radius` is a straight, lit, bedrock-bounded passage with ore beyond radius 64; it requires observed inventory success and an area-2 command. Its live validation and the existing step-budget/return fixtures are next.


### V57 live acquisition failure and V58 approach correction

Batch `20260908-060054-183658-98724` finalized: blocked return PASSED in 1905 elapsed ticks; search areas FAILED in 1801; search radius FAILED in 527. All have harness OK, client exit 0, complete recording coverage, released motors, and zero LLM calls. The bent passage transitioned into area 2 at tick 726 and identified ore at 783. Harvesting then performed generic surveys and traveled away, ultimately exhausting its 24-candidate allowance. The ore was remembered 16 blocks away, but no observed stance within interaction distance was available. This was an acquisition defect, not failure to change area.

V58 harvesting first uses any observed interaction stance as before, then chooses an unvisited, known safe intermediate stance within 12 blocks that reduces distance to the observed target by more than one block. Each movement remains bounded and the next decision uses fresh observations. It cannot choose unknown terrain or mine a remembered target without the existing live visibility/reach checks. A regression verifies directed approach before generic surveys, no invented near-ore support, exclusion of visited stances, and invalidation when the ore is observed gone. It fails under v57 and passes under v58. All 158 System 1, 37 evaluator, and 40 Python tests pass.

The first radius fixture put ore 71 blocks from the entrance, making it visible from inside the 64-block boundary. It therefore did not isolate radius exhaustion. The corrected fixture puts ore 83 blocks away in a correspondingly extended lit corridor; sensor range remains 16. The existing bent search-area fixture is unchanged. Live verification of both fixtures and the distant-harvest regression is next. Development 03's v56 failure also lies inside its search radius and remains separate from the original/development-01 radius defect.


### V58 targeted live verification

Batch `20260908-060745-448802-587` PASSED all three scenarios: distant approach 334 elapsed ticks / 260 exactly replayed decisions; search areas 862 / 791; corrected search radius 606 / 539. All have harness OK, client exit 0, complete recording coverage, zero LLM calls with untruncated histories, inventory success, and released motors. All three tapes replay against saved v58 classes. Recorder Plays finalized; these particular Plays have not been visually reviewed.

In the unchanged bent-corridor fixture, ore was identified around tick 791. At 792, harvesting selected the known intermediate stance (-20,200,38) directly; at 850 it issued the observed ore break at (-20,200,34). The earlier failed v57 run instead started four survey looks and moved backward. In the corrected radius fixture, the first area-2 command was at tick 506 with center (0,200,79), 64 horizontal blocks from the original entrance (0,200,15). Iron inventory success followed. These provide direct live evidence for both corrections, not full-world reliability.

The failed v57 recordings also replay exactly: blocked return 1828 turns, search areas 1729, and the initial radius fixture 458. Their scenario outcomes and the invalid isolation of the first radius fixture remain reported above. Original and development 01 are the next v58 integrated checks; no held-out world has been opened. The other development failures and the remaining redesign gates are still open.


### V58 original-world canopy failure; V59 clearance correction

`20260908-061054-006330-1636/01-iron-pickaxe` FAILED in 777 elapsed ticks / 756 exactly replayed decisions with harness OK and complete recording coverage. It had a wooden pickaxe, but no cobblestone. The final stone-task failure at tick 791 was `local_acquisition_alternatives_exhausted`. Recorded observations show the player at (-2,69,11), freshly opened soil steps at Y68, and spruce leaves in their required entry headroom at Y70. The stone method only accepted soil and stone as clearance materials. It looked at each leaf obstruction, rejected the descent, and repeated until twelve local alternatives were exhausted. This run does not verify the later radius correction because it never reached iron exploration.

V59 passes the Minecraft domain's existing approved access materials into the stone method for entry-headroom preparation. This does not make leaves stone-acquisition targets: the material extension is confined to clearing an already selected descent. A clearance break still requires identified, reachable observed terrain and preserves reserved supports. The generic kernel and sensor remain unchanged; knowledge v8 already contains the relevant access materials. A regression recreates the leaf obstruction, expects its clearance, then expects navigation into the retained lower step after observed removal. It fails before the change and passes after it. The regression explicitly includes observed air at the player's eye; unknown eye-space is not made traversable to satisfy the test. All 159 System 1, 37 evaluator, and 40 Python tests pass.

`system-one-stone-low-canopy` supplies a wooden pickaxe on the standard stone/soil platform under a low persistent spruce-leaf roof. It requires three observed cobblestone plus an issued spruce-leaf break. This is a new fixture for the natural-world failure; it does not alter the original iron-pickaxe scenario. Its live check and the existing canopy fixture are next.

Both workers of the v58 original/development batch were already launched before the v59 build. The remaining development client keeps its loaded v58 classes and saved replay classes remain available; new clients use v59. No later worker from that two-scenario batch can silently load the newer build.


### V59 live canopy proof

`20260908-062128-204495-4100` PASSED both fixtures: existing canopy 646 elapsed ticks / 585 exactly replayed decisions; low canopy 431 / 367. All have harness OK, client exit 0, complete recording coverage, zero LLM calls with untruncated histories, inventory success, and released motors. The low-canopy run issued a spruce-leaf headroom break at tick 119, retained its descent, and obtained three cobblestone. Both tapes replay with saved v59 classes. Recorder Plays finalized; these particular Plays have not been visually reviewed. The natural original-world rerun is next.

The v58 development-01 run (`20260908-061054-006330-1636/02-iron-pickaxe-development-01`) PASSED in 10364 elapsed ticks. All 10340 decisions replay exactly with saved v58 classes. Harness is OK, client exit is 0, recording coverage is complete, LLM calls are zero with an untruncated history, and motor control is released. The sibling original-world run remains FAILED as documented above; one development success is not a reliability gate.

### Recorded stale-rejection counterexample

The v56 development-03 tick-7289 failure is reproducible from the saved tape. Clearing only its rejected-candidate set produces navigation to the already observed safe stance (40,57,6), retaining the search origin, route, area allowance, and known terrain. The floor at (40,56,6) became identified stone at tick 7266, after this candidate had been rejected. This is a counterfactual proposal, not proof of successful resumed gameplay. A future correction should associate rejection with the relevant observation state so new geometry can permit reconsideration while unchanged failures stay bounded. Blindly clearing every rejection is not implemented. The full observed snapshot and probe output are under `/private/tmp/airicraft-v56-development03-rejection.json` and `.log`; these are diagnostic artifacts, not evaluator ground truth.


### V60 observation-scoped exploration rejection

Each rejected candidate now stores a reason (`COMMAND_FAILED`, `BLOCKED_CLEARANCE`, or `UNRESOLVED_OBSERVATION`) and a bounded snapshot of its entry clearance, floor, and adjacent possible placement supports. Candidate rejection is a single map of evidence, replacing the unqualified position set. Changed block identity, identification, or collision/support geometry removes that rejection before the next decision. Observation timestamps and raw light refreshes are excluded. A failed current command records a fresh rejection against the current geometry; an unchanged navigation failure cannot retry itself indefinitely. Existing arrival and finite-area rules still retain their bounds.

A regression follows a real decision sequence: observe an unknown floor, reject that candidate while preparing an alternative, then receive a failed alternative command and newly observed stone support. The old policy terminates; v60 chooses the newly justified navigation. Repeated timestamp/light samples leave rejection intact, and subsequent unchanged navigation failure remains excluded. Existing task-state fixtures now supply evidence maps rather than unqualified position sets. All 162 System 1, 37 evaluator, and 41 Python tests pass.

The production tape includes sparse `search` changes containing the task, candidate, added/removed status, rejection reason, and position/geometry witnesses. Changes are ordered deterministically. Suspended exploration keeps its evidence without emitting spurious removal; actual removal is reported factually without assuming whether it came from changed geometry, arrival, or task termination. Exact replay rejects missing or altered search evidence. `scripts/inspect-system-one-recording` reports counts and the last twelve changes; older tapes report unavailable search evidence instead of a false recorded zero. This makes candidate exclusion inspectable directly, while full decision replay remains separate.

Development 03 and the existing cave-turn/search-area fixtures are the next v60 live checks. The original-world v59 client was already running before these classes were built and continues to use its loaded policy and saved v59 replay classes. No held-out world has been opened.


### V60 live integration and rejection-recording evidence

In `20260908-064011-426693-8281`, cave turn PASSED in 186 elapsed ticks / 111 exactly replayed decisions, and search areas PASSED in 879 / 789. Both have harness OK, client exit 0, complete decision coverage, complete search-evidence coverage, zero LLM calls with untruncated histories, inventory success, and released motors. The literal inspector reports five rejection additions/removals for cave turn and 123 for search areas. Their geometry witnesses are unchanged at removal: these runs validate integration and factual rejection recording, not direct live recovery from a stale geometry-based rejection. The new recovery rule has the failing-before/passing-after regression documented above; development 03 remains running for broader verification.

The updated inspector also reads the finalized v59 low-canopy tape with complete decision coverage while reporting search evidence as unavailable, rather than zero. Recorder Plays for the two v60 fixtures finalized; these particular Plays have not been visually reviewed. The concurrent original-world run `20260908-062441-164695-4961/01-iron-pickaxe` remains running with v59 loaded; no terminal success or failure is claimed for it at this checkpoint.


## Changed-target live fault and v60 development failure

Commit `42e6dce8` adds evaluator-only `system-one-target-change`. The fixture watches a typed motor command identity and target, then replaces the first eligible issued mining target with bedrock on the integrated-server thread. It verifies the expected block before intervention and records the command identity, both explicitly labelled runtime/evaluator ticks, and server world time. Fixture ground truth is never supplied to decisions. The host event now includes typed command tokens and break positions; decision behavior is unchanged.

Live run `20260908-065249-915446-11372/01-system-one-target-change` PASSED in 427 elapsed ticks, harness OK, client exit 0, zero planner/LLM calls, complete untruncated events and decision writer, runtime SUCCEEDED, and motor fully released. At runtime tick 86, task 3 attempt 1 targeted grass at (-1,199,15); the server replaced that exact block. The same attempt returned `observed_target_changed` at tick 89. Task 3 then started attempt 2 against another observed grass block at (0,199,14), and ultimately acquired three cobblestone. All 358 decision turns replay exactly with v60. This establishes changed-target detection and successful alternative acquisition; it is a replacement test, not literal target removal. The Play finalized but has not been visually reviewed.

V60 development 03 (`20260908-064011-426693-8281/01-iron-pickaxe-development-03`) FAILED in 8,436 elapsed ticks with `command_budget_exhausted`. Exact replay verified all 8,410 turns. The player never left (-1,94,6), obtained no inventory, and issued 400 navigation attempts, all failing `observed_route_unavailable`, interspersed with 1,600 look commands. Harvest tasks exhausted local alternatives across unobserved wood recipes. No underground-search task ran, so this result does not test v60 changed-geometry rejection recovery. Source diagnosis: Gather only inserted access repair for an interaction stance near a resource or drop; ordinary survey destinations bypassed repair. The next policy extends the existing bounded repair to those destinations.


## V61 survey travel repair

Gather can now insert the existing bounded Access child after failed travel to an observed safe survey stance, even when no resource surface is known yet. Unknown destinations without a known resource/drop still do not qualify. The parent retains its goal and failed/visited destinations; failed access returns through `access_preparation_failed` rather than recursively starting another repair. TerrainAccess continues to authorize only observed local clearance and supports. No perception or command budgets changed.

A regression first failed on v60 because the next action was another generic look; with v61 it requests access to the original survey destination, clears an observed intervening dirt block, and retains the destination when repair fails. The older resource-only admission test now covers valid observed survey stances and still rejects a destination with unknown support and no identified resource. All 163 System 1, 37 evaluator, and 41 Python tests pass. Integrated v61 gameplay remains to be verified.


## V61 live integration and completed v59 original

`20260908-065840-792336-12890/02-system-one-production-discovery` PASSED in 880 elapsed ticks. All 815 decision turns replay exactly with v61. Harness OK, client exit 0, zero planner/LLM calls, complete writer, runtime SUCCEEDED, and motor fully released. The fixture exercised discovering another wood recipe and producing a stone pickaxe; the Play finalized but has not been visually reviewed.

Development 03 in the same batch remains running. At the inspected prefix it had completed 103 moves and inserted seven access repairs beneath wood-harvest survey tasks, while still holding no acquired wood. This is live integration evidence for the new path, not full acquisition success. Its fresh spawn was (-8.5,107,0.5), whereas v60 started at (-0.5,94,6.5), despite using the same development seed archive. The two runs therefore are not a controlled causal before/after comparison of the stuck spawn. The focused unit regression supplies causal evidence for the changed admission rule.

The original-world v59 run `20260908-062441-164695-4961/01-iron-pickaxe` finished FAILED in 39,607 elapsed ticks. All 39,590 turns replay exactly with v59, harness OK, client exit 0, complete recording, zero LLM calls, and motor fully released. Search task 23 exhausted its underground allowance at tick 38,974; later acquisition alternatives also failed. It ended at (-44,16,201) with 382 cobblestone, one stone pickaxe, and six torches, but no iron. Recorded work included 632 breaks, 952 navigations, 52 placements, 38 crafts, and 14 smelting start/collection pairs. Lighting resupply and tool replacements worked repeatedly; those successes do not satisfy the original goal or prove adequate search coverage. Search quality and repeated work across unavailable material alternatives remain open.


## V62 resource indicators

The v61 development-03 run ended FAILED at 8,280 elapsed ticks with `command_budget_exhausted`. Its observed map included acacia and oak leaves but no logs. Gather moved across nearby ground and repeatedly surveyed for unavailable wood variants, without inspecting those indicators. This is a discovery failure, not missing geological knowledge.

V62 adds an explicit `Discovery(indicators, maxClears)` prior to each harvest rule. Minecraft knowledge v9 associates normal vanilla tree logs with their corresponding leaves and permits at most 16 indicator-clearing attempts per Gather task. Stripped/crafted wood receives no inferred ecological association, and modded names are not guessed. Supply ranking distinguishes an indicated source from both a directly observed resource and a completely unobserved source. The generic task kernel is unchanged.

Gather approaches observed indicators through its ordinary observed-stance/access path. It clears one only if the rule permits it, the material is approved for access, reach/visibility is established, and footing/hazard checks pass. Every issued indicator break consumes the explicit budget, retained across survival and support interruptions. Clearing an indicator does not create a resource coordinate, drop claim, or inventory success. Actual resource surfaces take priority as soon as observed. This does not yet share exhausted spatial search across all recipe alternatives.

Three synthetic-domain tests cover occlusion followed by observed resource discovery, indicator-budget exhaustion, and ordering actual resources above indicators above blind search. All 166 System 1, 37 evaluator, and 41 Python tests pass. New live fixture `system-one-tree-indicator` encloses three logs in persistent foliage and starts with empty inventory; its goal is a wooden pickaxe and its checks also require a leaf-break event. Live validation is pending.


## V63 distinguishes a search origin from a return waypoint

The v62 tree-indicator fixture revealed and harvested logs at runtime ticks 125 and 193. A later Gather started while standing on the remaining log at (0,200,20). After stepping off, it still excluded that observed log because `returnStances` treated Gather's radius origin as an explicit return waypoint. The origin has no return behavior in Gather; actual parent return continuations already own their route reservations.

A probe over the recorded observations at ticks 330, 570, 600, and 770 confirmed that the log stays protected while supporting the player, but is releasable through the existing observed walking bypass after moving away when the unnecessary origin anchor is removed. V63 removes only that anchor. Current/partial contact, historical-footing bypass checks, and explicit parent routes remain protected. No global reservation or perception check is relaxed.

`tree-origin.json.gz` retains the full accumulated authorized observation at tick 570. An initial neighborhood reduction removed part of the bypass and was discarded. With the complete recording, the regression fails under the old origin rule and passes under v63; it also confirms an actual parent return waypoint still prevents the break. All 167 System 1 and 37 evaluator tests pass, alongside the unchanged 41-test Python suite. The development failure from the preceding v61 batch replayed all 8,257 turns exactly with v61 (the recorded policy), confirming `command_budget_exhausted`. New v63 live validation is pending.


## V62 development success and remaining tree pickup failure

`20260908-071051-813945-15866/01-iron-pickaxe-development-03` PASSED in 15,338 elapsed ticks. All 15,314 turns replay exactly with v62. Harness OK, client exit 0, zero planner/LLM calls, untruncated events, complete decision writer, runtime SUCCEEDED with an observed iron pickaxe, and motor fully released. Work included 265 breaks, 364 navigations, 46 edge-placement attempts, 35 placements, 25 crafts, and three smelting start/collection pairs. Rejection evidence is complete (15 additions and 15 removals); these counts alone do not prove geometry-triggered recovery. This is one development-world success with a different spawn from preceding runs, not a held-out result or proof that indicators caused the success. The Play finalized but has not been visually reviewed.

The v62 enclosed-tree fixture in the same batch FAILED its 6,000-tick evaluator budget; cleanup cancelled the unfinished mission and released the motor. Harness OK, exit 0, complete recording. Initial indicator clearance at runtime tick 101 exposed the first observed log at tick 108; harvesting began at 125. This is direct live indicator-to-observation evidence, but the complete wooden-pickaxe goal did not succeed. Exact replay of that failed fixture is pending.

V63 reruns use `20260908-071824-858999-17734`: tree indicator and blocked return. Both are still running at this checkpoint. In the tree run all three log positions became observed air, but the inventory remained empty of logs through the inspected tick-900 prefix (one acacia sapling appeared at 689). Mining attempts included client-predicted air reverting temporarily to log before later observed air; the trace does not by itself locate dropped entities. A live screenshot was inspected but showed the platform away from the tree. The one-tick item-entity debug trace start request timed out, but its completed record was subsequently recovered through the read-only trace API. At client tick 3006 it contains two acacia-log item entities totaling three logs at (0.125,200,20.875) and (0.875,200,20.875), 14.7 and 15.4 blocks from the player. These diagnostic positions were never supplied to autonomous decisions. Pickup still infers destinations from mined-block positions, so actual drop observation and collection need investigation. No v63 fixture pass is claimed.


## V64 observed item pickup

Actual item observations now cross the value boundary only when the item is in the current camera frustum, within range, sufficiently lit, and reachable by an unbroken ray through the freshly observed voxel map. Remembered free space cannot admit an item behind a newly occluding block. The Minecraft adapter reads stack identity/count only after this filter. This extends the geometric sensor; it does not close the broader perception-audit gate.

Production can collect visible matching drops before bootstrapping harvest tools. Gather can suspend for an item-specific pickup child and resume its saved search. Pickup follows the latest observed position, permits ten ticks for collection after arrival, tries other observed stances when inventory does not change, and uses the existing bounded access preparation. Only inventory progress establishes success. Last-seen positions expire after 200 ticks; each pickup has a 600-tick and 16-command bound. Failed entities are retained in the parent's alternatives. The older mined-position inspection fallback remains and still needs evaluation.

Policy v64 / stone tape v12 records the item observations on every turn and reconstructs them during exact replay. Historical block-only unit fixtures explicitly add an empty item list, documented in their README; original live recordings are unchanged. Eleven focused tests cover frustum/range/wrapped yaw, occlusion, darkness, missing current-view cells, delayed collection, moving items, exhausted/stale pickup, failed navigation, production integration, and missing/decision-altering replay evidence. All 178 System 1, 37 evaluator, and 41 Python tests pass. Live v64 validation is pending.

The earlier v62 tree failure now replays all 5,930 turns exactly with its saved policy, ending CANCELLED with `evaluation_finished`. The v63 tree harness reports CAPTURE_ERROR after five bridge-poll failures; this is not a terminal gameplay result. Two read-only JVM thread samples found the blocked-return client's render thread in `ImmutableCollections.MapN` lookups, first while recording terrain deltas and later in support analysis. Its server thread was still alive. Process-to-game-directory mapping confirms these samples belong to blocked return, not the tree client; they do not establish the tree capture error's cause. Terrain-map lookup cost warrants a separate measured correction; a bridge timeout alone was not treated as a stopped client.


## V65 terrain snapshot lookup cost

The v63 blocked-return client remained alive and progressed slowly; JVM samples exposed compact-map linear probing in both support analysis and recorder deltas. A read-only reconstruction of its first 4,759 available decision observations contains 56,792 authorized cells. Twenty repeated passes looking up each cell and its overhead neighbor took 2,618–2,909 ms with `Map.copyOf` versus 10–32 ms with a read-only copied `HashMap` (warm rounds: 2,804–2,909 vs 10–13 ms). Both maps compared equal. The smaller 12,097-cell tree fixture measured 13–27 vs 1 ms after warm-up. Probe source and reconstructed input are `/private/tmp/TerrainLookupProbe.java` and `/private/tmp/airicraft-v63-return-current.json.gz`; these measurements isolate lookup work, not total tick cost.

World snapshots now defensively copy terrain into a read-only HashMap. Source mutation and entry mutation cannot change the snapshot. Policy v65 / stone v13 distinguishes this representation change in recordings; older classes are retained for historical replay. All 179 System 1 and 37 evaluator tests pass; the 41 Python tests last passed at v64. Live performance proof is pending.

V64 tree run `20260908-075044-087193-24757/01-system-one-tree-indicator` is still running. It admitted an actual visible log at tick 190; one pickup failed on unobserved stance at 193. A later pickup collected two logs in inventory at tick 298 and completed at 318, followed by eight planks and four sticks. This verifies partial live collection, not the wooden-pickaxe fixture goal. The remaining resource is under the player and subsequent gathering is still being diagnosed.


## V66 preserve used routes, not unused perches

The v64 tick-639 recording resolves why the last log remained reserved even after the player walked away. Its stance (0,201,20) has four observed walking neighbors. Removing it disconnects an isolated leaf perch at (0,202,21) from the other three neighbors' 765-node component. The perch was never visited at that tick and no continuation required returning there. The old bypass rule nevertheless required every observed neighboring component to remain connected.

Support analysis now permits a fully searched graph component with no historical footing or required return stance to detach. Used components must remain connected; current contact, saved return connectors, unknown/unresolved searches, and the existing 2,048-visit bound remain protected. `tree-unused-perch.json.gz` preserves the complete authorized v64 observation. Its regression fails under v65 and passes under v66, while explicitly visiting the perch or adding a return continuation restores the log's protection. Existing canopy, route-break, partial-contact, and owned-command cancellation regressions also pass: 180 System 1 and 37 evaluator tests. Live v66 validation is pending.

V65 blocked-return run `20260908-075716-824154-26263/01-system-one-return-blocked` PASSED in 1,989 elapsed ticks, harness OK and runtime SUCCEEDED with all motor owners released. All 1,919 decisions replay exactly with saved v65 classes. Recording coverage is complete, LLM records are empty and untruncated, and client exit is 0. Recorder Play finalized; visual playback has not been reviewed. This run exercises the corrected terrain-map representation, with no change to the fixture or its required lighting resupply and blocked-return recovery checks. It does not isolate a total-tick speedup from gameplay path variation.


The v63 blocked-return harness ended BRIDGE_UNAVAILABLE at its last captured 5,616 ticks and stopped its own client normally before the manual cleanup request; its bridge state was already absent. The superseded v64 tree client accepted a graceful stop and ended CLIENT_EXITED at its last captured 4,208 ticks. These are interrupted diagnostic runs, not completed scenario passes.


V66 tree verification `20260908-080317-754152-27825/01-system-one-tree-indicator` PASSED in 661 elapsed ticks. It obtained the wooden pickaxe without changing fixture supplies, the goal, or the visibility lens. All 594 recorded decisions replay exactly with saved v66 classes. Harness status is OK, client exit is 0, runtime is SUCCEEDED, the task stack and every motor owner are empty, recording coverage is complete with no reported problems, events are untruncated, and final LLM records are empty and untruncated. Final inventory includes the wooden pickaxe, three acacia planks, two sticks, and one acacia sapling. Recorder Play finalized; visual review of this Play remains pending. This closes the targeted resource-indicator/pickup fixture, not broader iron-pickaxe reliability or the full redesign gates.


## V67 repair rejected furnace reach

V66 integrated batch `20260908-080643-468836-28718` completed with harness OK and complete decision coverage in both runs. Blocked return PASSED in 1,865 elapsed ticks; all 1,794 decisions replay exactly. Original iron-pickaxe FAILED in 2,901 ticks; all 2,877 decisions reproduce its `furnace_not_observed` failure. The original run harvested three raw iron but the first ingot's furnace interaction ended the entire recipe branch. This is a production recovery failure, not a lack of ore discovery.

At recorded tick 2916 the original player eye is (4.4751,69.62,18.8947), sharing a remembered large-fern voxel. The selected furnace is (1,69,15). The pure visibility model permits sensing out of a noncolliding origin voxel; the motor uses Minecraft's actual outline ray. The fern is a plausible explanation for the rejection, but the recording does not retain the motor's first hit, so this is not claimed as confirmed ray-level causality.

Smelting now retains its request across an OPEN-phase `furnace_not_observed` receipt. It rejects the attempted stance, reacquires the furnace through the existing observed-station task, and retries with the same resource commitments. Four distinct rejected stances exhaust the reach method; other interaction failures retain their existing failure behavior. The regression fails under v66 and passes under v67, and a second test verifies bounded distinct retries without spending inputs. All 182 System 1, 37 evaluator, and 41 Python tests pass. A new evaluator-only `system-one-furnace-reach` fixture shares the initial player position with a large fern and requires the actual failed reach, recovered station, and ingot inventory. Live proof is pending. No original scenario conditions or visibility lens were changed.

The v66 tree Recorder Play rendered successfully with an isolated valid recorder config and Gradle 9.5.1, without changing the external recorder checkout. The no-GUI video has 675 frames at 640x360 / 20 fps, covering recorder ticks 20–694. Frames 180, 300, 600, 640, and 675 were visually inspected: loose logs inside the foliage, canopy inspection, the remaining resource log, and the crafting table are visible. The images do not show final inventory; the evaluator and exact replay establish that result. The external default config was rejected as obsolete; `/private/tmp/airicraft-recorder-render.toml` is the isolated replacement used for rendering.


V67 `20260908-081849-836443-31706/01-system-one-furnace-reach` PASSED in 280 elapsed ticks, harness OK, client exit 0, runtime SUCCEEDED, and motor fully released. All 220 decisions replay exactly, input coverage is complete, and LLM records are empty and untruncated. StartSmelt attempt 1 begins at runtime tick 80 and returns `furnace_not_observed` at 82. The same smelting task suspends for navigation to (0,200,16); at 89 it resumes after `station_observed:minecraft:furnace` and issues StartSmelt attempt 3 against the same furnace (0,200,17), retaining the same raw input and plank fuel. The final ingot confirms actual recovery rather than just a successful movement receipt. This reproduces the fern reach problem live without weakening the interaction ray check.

Tree playback coverage is explicitly partial: the renderer requested global ticks 20–715 but exported 20–694, omitting the final 21 ticks. It reports 82 unsupported packets skipped (78 chunk-unload and 4 player-position packets). The inspected video supports the visible harvesting/workstation sequence; it does not establish exact alignment with runtime ticks or full capture playback coverage. Opening the video in Codex was queued by the app; the frame inspections above were completed directly.


## V67 integrated successes and optional target evidence check

Batch `20260908-082059-069153-32290` PASSED both the unchanged original iron-pickaxe scenario (9,681 elapsed ticks / 9,661 exactly replayed decisions) and frozen development-02 (9,750 ticks / 9,726 decisions). Both have harness OK, client exit 0, complete finished recording writers, runtime SUCCEEDED, empty motor ownership, and zero untruncated LLM records. Detailed outcomes, command counts, travel, survival/repair measurements, trace hashes, and replay provenance are in [the integrated report](evaluations/system-one-v67-integrated.json). These are fresh development-stage runs; spawn and path variation prevent attributing all differences to a single fix. They do not count toward held-out acceptance.

`scripts/inspect-system-one-recording <tape> --audit-break-observations` now reconstructs recorded terrain deltas and checks each parsed Break against its identified occupied target, expected block identity, and non-future observation tick. It distinguishes missing evidence from mismatches, handles removals and absent-world boundaries, and reports incomplete histories or unsupported effect syntax as partial. It is opt-in because reconstruction uses additional memory. The scope explicitly excludes line of sight, light, physical reach, support reservations, sensor reads, and motor reads; the existing global perception-violation metric remains unavailable with a null count.

The check covers all 50 breaks in the v66 failed original recording and all 179/194 breaks in the v67 integrated successes without mismatches or missing target evidence. Six added tests exercise memory reconstruction, removals, identity/identification/time mismatches, missing inputs, truncation, unknown effect syntax, and absent-world boundaries. All 46 Python tests pass. The runtime and recorded policy remain v67; this is offline inspection work, not a new gameplay policy.


## V68: backtrack from exhausted underground frontiers (2026-09-08)

The v67 development-03 run `20260908-083816-320615-36328/02-iron-pickaxe-development-03` failed after 2,143 elapsed ticks. All 2,123 decisions replay exactly with the saved v67 classes; the harness completed normally. At runtime tick 1,383, underground search stopped at `(-7,95,19)` with `no_observed_underground_step`. It had used only 25 of 96 steps, had no dirt/cobblestone for bridging, and had observed air below its forward/side candidates. Its previous supported stance at `(-7,95,18)` remained known, but the visited-route exclusion prevented returning to it. Later harvest/recipe failures were downstream.

`search-dead-end.json.gz` retains that turn's complete reconstructed observation and the prior, route, rejection coordinates, and step count extracted from offline v67 replay. The regression rebuilds the local rejection witnesses from those observations and the recorded pending look. Before the fix, the recorded-state regression and a small forked-corridor regression both fail because search completes without any retreat. The fixture contains sensor evidence, not evaluator ground truth.

Search now tries observed ancestors after exhausting local candidates. Loop erasure over the recorded route selects ancestors while the full history remains retained for visited-branch exclusion, lighting repair, and return support. Return navigation requires known standing support and eligible feet/head/floor, uses the existing 12-block/200-tick motor bound, retains failed-destination exclusions, and consumes the existing movement budget. Returning does not grant another area allowance. A synthetic corridor verifies retreat to a junction, entry into an unvisited branch, and finite exhaustion without cycling; another rejects missing or hazardous return support.

Policy is `reactive-production-v68`. All 185 System 1 tests, 37 evaluator tests, and 46 Python tests pass (268 total). The evaluator fixture `system-one-search-dead-end` has a lit preferred corridor ending beyond a side junction, with ore hidden beyond the side branch's bend. Live run `20260908-085414-506691-41901/02-system-one-search-dead-end` PASSED in 279 elapsed ticks, harness OK and client exit 0. All 181 decisions replay exactly; the motor is released and recording coverage complete. Replay confirms four backtracks from z=22 to the junction at z=18 (runtime ticks 197, 202, 208, 215), then entry into the unvisited east branch at tick 221 and eventual raw iron collection. Recorded search steps increase from 7 through 11 across those moves. [Selected evidence](evaluations/system-one-v68-backtracking.json). The development-03 rerun in the same batch remains running. This change does not establish broad search reliability, full perception compliance, or game completion.

The other v67 development run `20260908-083816-320615-36328/01-iron-pickaxe-development-01` also FAILED (13,665 elapsed ticks; 13,644 exact replay turns; harness OK). Bootstrap recovered from multiple wood/pickup failures. Underground search later stopped with `no_observed_underground_step` at tick 12,573, followed by harvest fallback exhaustion. This is additional evidence against v67 reliability; it is not a held-out run.

The v68 dead-end Recorder Play rendered to `20260908-085414-506691-41901/02-system-one-search-dead-end/recording-render-no-gui/fpv.mp4` (160 frames, 640x360, 20 fps). Frames 60, 95, 125, and 160 were inspected: bedrock corridor geometry and final breaking particles are visible. The precise retreat/branch chronology comes from exact decision replay. Rendered global ticks 29–188 omit the final 20 of the requested 29–208 range; 82 unsupported packets were reported (78 chunk-unload, 4 player-position). Final inventory is evaluator/replay evidence. The optional identity/time audit matches the one recorded iron-ore break; full perception violations remain unavailable. Final LLM records are empty (latest sequence 0, untruncated).

A one-step offline comparison at v67 development-01 runtime tick 12,573 also selects an observed retreat under v68: `(-64,34,-507) -> (-65,34,-507)`, where v67 returns `no_observed_underground_step`. This secondary probe uses the complete reconstructed observation, recorded route/prior/area/destination, and unchanged local exclusion coordinates; its rejection witnesses are minimized for the fixed input, so it is not a full counterfactual run. The original complete v67 recording still replays exactly. The live v68 development-01 rerun is `20260908-090108-404624-43598/01-iron-pickaxe-development-01`; development-03 continues under `20260908-085414-506691-41901/01-iron-pickaxe-development-03`. Neither has a terminal outcome at this checkpoint.


## World context invalidation and unavailable observations (2026-09-08, v69 / motor v13)

The host previously ticked the motor before checking world availability and only recognized a null world. A loaded-world-to-loaded-world replacement therefore retained an old mission and unnamespaced terrain. Motor loss of context could also return `Released` before the kernel requested a stop; delayed completion delivery could leave a later stop addressing an already released motor.

The evaluator-only `system-one-world-change` probe records a separate real System 1 host, switches the player from the Overworld to a safe Nether platform after a block command starts, checks cancellation/release/terrain, then returns before starting the normal stone goal. It does not pass destination or fixture state to the agent. Baseline `20260908-091318-721206-47025/01-system-one-world-change` finalized a 306-turn probe tape whose old mission continued in the new world and ended only after `world_probe_cleanup`. A check requiring automatic `world_changed`/`world_left` cancellation fails on that tape. The surrounding scenario later timed out during return preparation; that timeout is not the causal proof.

The host now binds each mission to its client world and cancels before admitting observations from a replacement world. The motor binds each command to world/player/interaction-manager identity, drops obsolete container/edge continuations on replacement, stops inputs, and reports physical release. A stop matching the most recently released command remains acknowledged even if completion delivery was delayed. Sensor memory is cleared across worlds. Published and previously captured Baritone terrain views check their originating world, so clearing the current view cannot leave an old captured view usable in a replacement world. These changes establish invalidation; they do not implement portal travel or resume a cross-dimension mission.

Intermediate run `20260908-092207-149512-49384` verifies the motor change with policy v68: death recovery PASSED in 328 elapsed ticks / 242 exact-replay turns, and delayed-feedback handling PASSED in 442 ticks / 373 exact-replay turns. The world probe itself PASSED: automatic `world_changed` cancellation, zero retained terrain cells, invalidated captured view, and acknowledged release despite withholding the first `Finished` receipt. Its 35 turns replay exactly. The following stone goal FAILED because all four initial looks produced empty remembered terrain after returning to the Overworld; this run is not an overall scenario pass.

A new regression provides no observation between a completed command and resumption. It first failed with a null-world dereference, then exposed stale terrain in replay once waiting was implemented. Production now preserves task state and pending receipts while observation is unavailable. The Minecraft sensor reports unavailable input when the player's client chunk has not arrived; it does not treat that absence as observed air or a failed resource search. Replay clears its reconstruction across a null input, because the next recorded observation is a full snapshot. Policy is now v69; motor is v13. The fixture also records client chunk/light state on return to distinguish missing chunks from other causes of empty perception.

All 186 System 1, 37 evaluator, and 46 Python tests pass (269 total). Fresh world-transition and Baritone-terrain fixtures are running with v69. Disconnect, in-flight workstation/container changes, and autonomous dimension traversal still need broader live coverage.

## V68 development results and recorder overflow

Both development reruns exhausted the 2,000-command allowance: development-03 in `20260908-085414-506691-41901/01-iron-pickaxe-development-03` FAILED after 27,406 elapsed ticks; development-01 in `20260908-090108-404624-43598/01-iron-pickaxe-development-01` FAILED after 25,583 ticks. Both harness outcomes are `DECISION_TRACE_INCOMPLETE` from `trace_queue_overflow`. The development-03 writer stopped accepting inputs very early (53 rows, 52 turns, last recorded tick 131), before the later transition-probe client was launched. Development-01 retained 15,581 rows. Neither recording supports a complete replay or a reliability claim. Final runtime reports and event history remain diagnostic evidence. The evaluator currently discovers irrecoverable decision-recording failure only during terminal cleanup; it needs prompt failure reporting/cleanup rather than spending the rest of a long run without the required trace. Command-budget exhaustion also remains an unresolved search/resource-efficiency failure; increasing that budget is not evidence of a solution.


V69 serial live proof: `20260908-094118-163181-54799/01-system-one-world-change` PASSED in 1,051 elapsed ticks, harness OK, client exit 0, zero LLM records, complete writer, and fully released motor. The transition probe's 38 decisions replay exactly: the old mission cancels with `world_changed`, captured old terrain becomes inaccessible, and the stop receives acknowledgement despite withholding `Finished`. Its subsequent mission replays all 539 decisions exactly. At return tick 535, the client chunk was explicitly unavailable; ticks 536–720 contain 185 null observations and zero effects. After the chunk arrives, the same goal proceeds and obtains three cobblestone. This directly verifies the unavailable-input boundary rather than inferring it from empty terrain. [Selected evidence](evaluations/system-one-v69-world-context.json).

The preceding parallel attempt `20260908-093518-126702-52937` stalled both clients during world startup and ended with `CAPTURE_ERROR` / bridge poll timeouts, before gameplay evidence. Both were stopped by the harness with exit 130. Its startup cause remains unproven; the later serial world-transition success does not diagnose that stall. A serial v69 `system-one-terrain` run is now checking the existing Baritone isolation probe against the world-bound view implementation.


## Required recording failure ends evaluation promptly (2026-09-08)

`ScenarioEvaluationRunner` now checks the recorder's immutable failure value before goal submission, planner heartbeats, or success checks. Irrecoverable decision-capture failure ends the report as `NEEDS_REVIEW` with the concrete reason, triggering the existing acknowledged motor-release and recorder-flush cleanup. It does not label an unfinished gameplay goal as a gameplay failure or permit a later writer recovery to validate already lost inputs. The writer remains bounded and nonblocking; its queue capacity is unchanged.

A regression first demonstrated that an otherwise passing check could win despite required capture failure. It now verifies both failure-before-start and failure-during-execution, including a frozen terminal report. All 186 System 1, 38 evaluator, and 46 Python tests pass (270 total).

`system-one-recording-overflow` is an explicit negative fixture: it defers the real writer worker until the real bounded queue reports overflow, then drains it to an incomplete footer. Live batch `20260908-095113-003908-57871` verifies the complete path. The negative scenario ends `NEEDS_REVIEW` after 95 elapsed ticks, with `Required decision recording failed: trace_queue_overflow`; the harness returns the expected `DECISION_TRACE_INCOMPLETE`. The tape retains its header plus 31 turns through runtime tick 116 and an explicit incomplete footer. The separate event log records release requested at tick 118 and command 2 acknowledged released at 119. Final runtime is CANCELLED (`evaluation_finished`), all motor fields are released, and the client exits 0. This is successful detection/cleanup of an intentionally invalid recording, not a passed or replayable gameplay run.

The following normal `system-one-stone` scenario PASSED in 420 elapsed ticks, harness OK, complete recording, normal client exit, and all 355 decisions replay exactly. Neither scenario made an LLM call. The negative fixture's expected nonzero batch exit must remain visible; it must not be counted in the gameplay pass rate.

The serial v69 Baritone check `20260908-094520-448886-56089/01-system-one-terrain` also PASSED in 401 elapsed ticks with 329 exact-replay decisions, harness OK, and client exit 0. Its adapter probe remains equal after changing 102 hidden cells; an unloaded client chunk remains an unknown, non-supporting sample. This closes the specific world-bound terrain-view regression check, not the full autonomous perception audit.

Recorder throughput remains unresolved. In the early incomplete development-03 recording (52 turns), 100,302 of 104,240 changed-cell entries (96.2%) only changed the observation timestamp. Repeating full block identity/geometry for timestamp refreshes is a concrete optimization candidate; this count alone does not prove the cause of thread stalls or establish throughput under load. Measure the encoding cost and preserve exact observation-time reconstruction before changing the tape format or queue limit.


[Structured recording-failure evidence](evaluations/system-one-required-recording-failure.json) retains the negative/normal outcomes separately, including release ticks, writer state, and trace hashes. The normal control replays 355 decisions; the intentional overflow remains explicitly incomplete.

The [offline timestamp-refresh prototype](evaluations/benchmarks/README.md) now measures the candidate representation on that 52-turn fragment. Every reconstructed terrain snapshot equals the original. Over ten alternating warmed measurements, serialization plus in-memory gzip takes a median 146.91 ms for the current representation and 72.49 ms for the prototype. Raw bytes fall from 18,460,619 to 4,824,771; gzip bytes from 504,235 to 377,689. [Samples and provenance](evaluations/trace-refresh-prototype.json). This excludes disk, scheduling, startup, and live rendering; the format has not changed in production. It supplies a measured next step without claiming that encoding alone caused the observed queue overflows.

## Compact observation recording (2026-09-08)

Production wire format 3 / stone format 2 encode timestamp-only sightings as a required `refreshed` position list. The policy remains v69 and motor v13. Full values still record changes to identity, geometry, lighting, and non-current timestamps. Shared replay reconstruction checks unknown refreshes and overlapping/duplicate deltas before mutation; null observations clear terrain memory. Current replay requires all delta lists and rejects older wire formats. Historical recordings remain inspectable with the Python inspector and replayable with their saved classes.

Six new Java regressions cover exact observation equality, all cell fields, removals/null gaps, malformed deltas, and both replay entry points; one Python regression covers refreshed failure context and corruption detection. All 277 tests pass.

The implemented encoder reconstructs every input in the failed development-03 52-turn fragment exactly: 100,302 timestamp refreshes and 3,938 full changes, reducing raw bytes from 18,309,238 to 4,824,835 and gzip from 502,666 to 377,653. The incomplete footer remains incomplete. Re-encoding the earlier v69 world-change mission preserves all inputs and its 539 decisions replay exactly. These are offline checks, not throughput proof.

Live batch `20260908-101037-126137-62463` used the ordinary bounded writer and external-drive paths, serial clients, unchanged scenario budgets. Stone PASSED in 417 elapsed ticks with 353 exact replay turns. World-change PASSED in 995 elapsed ticks with 493 exact mission turns, including 161 null-observation turns with no effects, plus 30 exact transition-probe turns ending `world_changed`. Both harnesses OK, complete recording, zero planner turns, normal client exit, released motors. The world probe invalidated the captured view, but did not withhold a Finished receipt on this run; earlier v69 evidence covers that fault. See [structured evidence](evaluations/system-one-compact-recording.json). Sustained development-run capture and command-budget/search efficiency remain open.

## Tool replacement with remote ingredients (policy v70, live proof pending)

The retained v68 development-01 tape reproduced 8,656 prefix decisions through tick 8,703. Its pickaxe replacement was already active, but needed another stick. The ingredient chain tried to gather dark-oak logs underground; its Access child requested diorite breaks with no pickaxe. This is not evidence that the search forgot to request a replacement. Replacement acquisition lacked the outward resupply route used for lighting. The source tape remains incomplete. See [bounded failure evidence](evaluations/system-one-tool-materials-gap.json).

Policy v70 shares outward supply selection between lighting and replacement tools when direct recipe inputs are unavailable. Parent search, commitments, rejected-tool alternatives, and the return route remain intact. A new regression fails on v69 and passes on v70; local crafting still avoids the outbound trip. 193 System 1 and 38 evaluator tests pass (Python remains 47 passing tests from the unchanged scripts). `system-one-remote-tool` supplies a two-use pickaxe and cobblestone, puts the obstruction 25 blocks inside a forty-block cave, and leaves wood/table at the entrance. Its live proof is pending; the v69 development-03 recorder run is still active.

## Complete development-03 recording and success (v69 / production format 3)

`20260908-101439-750985-63653/01-iron-pickaxe-development-03` PASSED in 20,243 elapsed ticks. All 20,225 decisions replay exactly using saved v69 classes with format 3; writer complete, harness OK, zero planner turns, client exit 0, final iron pickaxe observed and motor released. All 323 break targets matched recorded identity/time; the full perception audit remains unavailable. This is sustained live capture evidence with the unchanged bounded writer and budgets. The spawn differs from previous runs, so it is not a controlled gameplay comparison. See [structured evidence](evaluations/system-one-v69-development03-complete.json).

Raw iron quantity three was reached at tick 14,780. Later workstation preparation repeatedly searched for acacia wood and stripped acacia variants after normal-log acquisition failed. The run used 813 Looks of 1,717 total starts. The registry catalog currently exposes harvesting for every log-tagged variant; it does not distinguish observed harvesting from ecological search. Making that distinction explicit in game knowledge is a next efficiency task. This successful run predates the v70 remote-tool repair. Its live fixture batch is `20260908-103240-128262-68045`, still pending at this checkpoint.

## Live remote-tool resupply proof and playback defect (v70)

Batch `20260908-103240-128262-68045`: remote-tool PASSED in 849 elapsed ticks / 786 exact replay turns; local-material tool-wear PASSED in 551 elapsed ticks / 487 exact turns. Both harnesses OK, complete writer, zero planner/LLM calls, client exit 0, released motors. The remote repair retained exploration frame 3 and destination (0,195,40); it navigated outward from observed feet (0,196,39) via z23 to the entrance at (0,200,15), then returned via z30 to z39 and resumed the same exploration frame. See [structured live/replay evidence](evaluations/system-one-v70-tool-resupply.json).

Playback is not visual proof: both normal and `--no-gui` renders produced 897 frames, but inspected frames 300,460,520,660,860 stayed beside the entrance crafting table. Capture `playerState` records contain changing positions through the full trip, so a static capture pose is ruled out; the replay-entity/camera/export cause remains unresolved. Each render requested server ticks 20..917 and produced 20..916, ignoring 550 unsupported packets (546 chunk unloads, 4 player positions). External renderer/dashboard worktree conflicts were inspected read-only and left untouched. This is a concrete flight-recorder inspection finding; a successful renderer exit does not establish useful footage.

## Ecological discovery priors (v71 / knowledge v10)

`Discovery.searchMode` separates `OBSERVED_ONLY` from `LOCAL_SURVEY`. The mode participates in method applicability, recipe supply estimates, and post-target-loss gathering. It is required in serialized knowledge; replay cannot silently default a missing mode to surveying. The Minecraft module declares local surveys and leaf indicators for its supported natural tree species. Other log-tagged variants remain harvestable when observed, including modded variants without supplied ecological priors. Ore harvesting is observed-only; geological search remains its explicit separate method. No game names or search policy entered `TaskKernel`.

The complete v69 development-03 tape recorded zero identified acacia-wood, stripped-acacia-log, or stripped-acacia-wood cells, yet their harvesting dependencies used 384 Look commands. Four behavior tests distinguish absent, present, vanished, and recipe-ranked sources; three failed before the domain change. A fifth regression rejects missing serialized search modes. All 283 targeted tests pass.

Live batch `20260908-105153-668897-72481`: observed processed wood PASSED in 151 elapsed ticks / 84 exact turns; remote tool PASSED in 926 / 863; natural-tree indicator PASSED in 660 / 596. All harnesses OK, recordings complete, zero planner/LLM calls, client exit 0, motors released. Recorded catalogs confirm natural oak uses leaf-backed local surveys while stripped oak and acacia wood are observed-only. Four read-only live screenshots of the tree fixture show leaf clearing and an exposed log; final inventory comes from evaluator/replay evidence. See [structured evidence](evaluations/system-one-v71-discovery-priors.json). Integrated efficiency and release remain unproven at this checkpoint.

## v71 original iron-pickaxe integration

`20260908-105813-695172-74307/01-iron-pickaxe` PASSED with the original world archive and unchanged scenario. The harness completed OK and the client exited 0. All 14,797 decisions replay exactly to the observed iron-pickaxe inventory outcome; the writer completed, and the final runtime is SUCCEEDED with the motor fully released. There were zero planner calls, 795.322 observed horizontal blocks of travel, 266 Break commands, and no observed health loss or death. All 266 recorded break targets matched their recorded identity/time evidence; this is not the still-missing full perception audit. See [machine-readable evidence](evaluations/system-one-v71-original.json). Development-01 FAILED in the same batch after 2,352 ticks; its complete 2,332-turn tape replays exactly. Both first local-access repairs exhausted their 48-work bounds; later retries failed on top of a giant mushroom before obtaining cobblestone. Mushroom caps/stems were absent from the recorded clearance knowledge. This is a concrete gap, not yet proof of the complete cause. See [failure evidence](evaluations/system-one-v71-development01-failure.json).

## Mushroom access knowledge and live regression

Knowledge v11 adds brown/red mushroom caps and mushroom stems to the removable surface-access materials. The generic scheduler, geometric perception, motor, and production method remain unchanged (method v71). Development-01 exposed this missing prior while stone acquisition was stranded on a giant mushroom; other access inefficiencies remain unresolved.

The focused `system-one-mushroom-obstruction` fixture FAILED with knowledge v10 in 694 ticks (622 exact replay turns, complete recording). With v11 it PASSED in 434 ticks (364 exact turns), breaking one observed mushroom-cap block and acquiring three cobblestone. The existing low spruce canopy regression PASSED in 415 ticks (347 exact turns). All three harnesses completed OK, LLM records were empty/untruncated, and motors released before normal client exit. All 283 automated checks pass. See [before/after evidence](evaluations/system-one-mushroom-clearance.json).

Renderer diagnosis is still open. An offline decode of the original tool-resupply ZIP confirms 292 movement snapshots and 284 unique positions for the player. An isolated renderer with camera probes produced visibly changing tunnel/surface frames, while removing those probes reproduced frozen output. No renderer fix is claimed. The isolated checkout is source-clean; the external repository's unmerged files were untouched. See [probe evidence and coverage limits](evaluations/system-one-renderer-camera-probe.json).

## Development-01 success with mushroom access knowledge

`20260908-113002-730645-85092/01-iron-pickaxe-development-01` PASSED in 9,250 elapsed ticks on production method v71, motor v13, knowledge v11. All 9,230 decisions replay exactly to the iron-pickaxe inventory outcome. The writer completed, harness finished OK, LLM records were empty/untruncated, and motor control released before client exit 0. Travel was 384.328 observed horizontal blocks; all 190 recorded break targets matched identity/time evidence. Full perception auditing is still unavailable. See [complete evidence](evaluations/system-one-knowledge11-development01.json).

This fresh-spawn run is not a controlled before/after comparison with the prior failure. Together with the dedicated mushroom regression it supports the clearance correction and establishes a development-01 success. Original, development-01, development-02, and development-03 now have successful integrated runs at different checkpoints; a single frozen current build has not passed that entire set or the held-out gate.

## v72 geological recipe inputs and reduced-light bootstrap

Separating observed harvesting from resource discovery had left the recipe estimator treating unseen ore as unavailable despite an explicit geological search method. The regression fails with the previous estimator (with a usable tool supplied), then passes after ranking that search as discoverable. A second assertion keeps an actually observed source ahead of the prior. This changes production method v72; the knowledge catalog remains v11 and motor v13.

`20260908-114153-640973-88558/01-system-one-dim-coal-bootstrap` PASSED in 223 elapsed ticks with 156 exact decisions. The agent started torch production with one stick and a wooden pickaxe, selected coal search, and cleared observed soil at light level 5. The one exploration allowance ran from tick 97 with deadline 177; the dirt block was observed empty at tick 117. Coal collection and crafting then completed the four-torch goal. The replay-backed [allowance audit](evaluations/benchmarks/LightingAllowanceAudit.java) verifies the command, its time/region bounds, successful feedback, and changed observation. It rejects the successful bright charcoal recording as a negative control. This proves active exploration under a bounded allowance; it does not claim maintained lighting during subsequent Gather, Pickup, Access, or Craft work.

`20260908-114558-407945-89703` also PASSED charcoal production in 584 ticks (516 exact decisions) and local lighting repair in 301 ticks (227 exact decisions). All three harnesses completed OK with complete writers, zero LLM calls, normal client exit, and released motors. See [evidence](evaluations/system-one-v72-geological-ranking.json). Original and all three development scenarios are the next same-policy integration gate.

## v72 integrated batch and replay presentation follow-up

The [same-policy batch](evaluations/system-one-v72-integrated.json) completed three gameplay runs: original and development-02 PASSED, development-01 FAILED. Exact replay verifies 6574, 5029, and 2296 decisions respectively. Development-03 encountered a client-thread timeout before entering its world and needs a fresh isolated run. The canopy failure persists with mushroom clearance enabled: access task 32 exhausted 48 commands, and task 33 cleared a mushroom cap block but found no local approach to the ground. This distinguishes permissive clearance knowledge from an actual descent strategy; no new movement fix is claimed.

Two [uninstrumented renderer experiments](evaluations/system-one-render-tail-experiments.json) on the completed remote-tool Play produced moving frames. The clean control has 877 frames / 857 unique PNG hashes, with frame 401 showing surface travel and frame 651 showing the return tunnel. It ends at replay tick 879 of 899; the shorter-tail experiment ends at 859. Both exited normally. Instrumentation is not necessary for moving playback, but early tail coverage remains unresolved and earlier full-coverage exports froze. The isolated renderer checkout is restored clean; the original external checkout remains untouched.

Development-03 startup retry `20260908-121037-612623-97368/01-iron-pickaxe-development-03` PASSED under v72 / knowledge11 in 21438 elapsed ticks. All 21418 decisions replay exactly; capture is complete, harness OK, zero planner calls, normal client exit, and motor released. See [the evidence](evaluations/system-one-v72-development03.json). This finishes the missing v72 gameplay run, but development-01 remains failed and newer access changes require their own live validation.

## Access proposals stop at unknown support

The tall-trunk fixture reduced development-01 canopy descent to a two-by-two observed trunk above stone. Permitting logs alone was insufficient: a route could rely on a floor that an earlier step had cleared or reserved, and speculative routes continued through unknown air outside the trunk. v75 tracks route effects and ends a proposal at its first unknown footing. It can expose an observed covering block, then inspect the newly revealed support, before authorizing movement. Two bounded search passes share the previous 512-expansion limit. Landing recovery retains the chosen route only after a changed position near its observed support; an unchanged failed recovery rejects that edge.

Live v73/v74 attempts FAILED in 488/487 elapsed ticks and replay exactly. v75 PASSED in 901 elapsed ticks with 835 exact decisions, zero planner calls, complete capture, normal client exit and released motor. The final inventory contains three cobblestone and six dark-oak logs; 14 Break and 14 Navigate commands include two recovered route failures. [Detailed evidence](evaluations/system-one-v75-trunk-descent.json) distinguishes the simulated return-support checks from the successful descent; live return to this trunk top and full perception audit remain unproved.

The [v75 shared-access batch](evaluations/system-one-v75-access-regressions.json) PASSED mushroom obstruction (422 elapsed / 347 exact decisions), blocked return (1987 / 1918), and canopy descent (486 / 422). Every capture is complete, harness OK, zero planner calls, normal client exit and released motor. This permits the full development-01 retry; it does not establish its outcome or the remaining maintained-condition and perception gates.

## v75 development-01 integrated retry

`20260908-125703-984840-13409/01-iron-pickaxe-development-01` PASSED in 9,490 elapsed ticks. All 9,468 decisions replay exactly to the observed iron pickaxe. Capture is complete, harness OK, zero planner/LLM calls, normal client exit 0, and motor released. This follows the trunk and shared-access regressions; the fresh spawn is not a controlled before/after comparison. See [structured evidence](evaluations/system-one-v75-development01.json). The original and other development successes remain v72 evidence; a frozen current-policy set, held-out release and all-task lighting remain open.

## v76 reduced-light allowance revocation

An active exploration allowance now observes the player's health and life identity. A health decrease, life change, or observed burning/lava latches revocation; healing and fresh torch supplies do not reactivate it. Restored working light clears the maintenance episode. The original deadline and region remain unchanged. The failing kernel regression reproduced v75 continuing its pending command after health loss; v76 stops that token and waits for release before withdrawal. All 290 targeted checks pass.

`20260908-131051-430804-17595`: the undamaged dim-coal control PASSED in 196 elapsed ticks / 124 exact decisions. The injury fixture FAILED its torch inventory goal as designed in 223 ticks / 153 exact decisions, with harness OK and complete capture. Recorded health falls from 20 to 18 at tick 109; Stop occurs immediately, release is acknowledged at 110, before the original allowance deadline 184. The explicit health-loss withdrawal is recorded and no coal mining follows. The player was already at its route origin, so this is not proof of a traveled retreat. Seventy-two subsequent Looks expose the still-open maintenance gap in recipe prerequisites outside Explore. Both clients made zero planner/LLM calls, exited normally, and released their motors. See [structured evidence](evaluations/system-one-v76-lighting-revocation.json).

The replay-backed `LightingRevocationAudit` accepts the injury recording and rejects the healthy control. `LightingAllowanceAudit` accepts the healthy control and rejects the failed injury run. The control exposed a verifier gap: soil became observed empty and the search observed coal before mining's Finished callback, so successful search completion released the command. The audit now recognizes that lifecycle only with observed target clearance and an acknowledged release accompanying successful resource observation; it does not infer clearance from Stop alone.

All-task maintenance still requires moving light state and interruption ownership out of Explore, preserving one episode across gathering, pickup, access, workstation work, and passive waits. Repair and outward/return travel need their own bounded permissions; the current 80-tick/local-radius exploration allowance must not become a blanket exemption for those tasks. No generic kernel observation API or replacement maintenance owner has been added yet.

## Mission-owned lighting (v77), followed by retained work context (v78)

`TaskKernel.Domain.observe` updates immutable domain values once per input tick, including waiting parents and releasing frames. The kernel preserves frame order, identity, phase, results, motor tokens and budgets. Synthetic-domain tests verify that fresh parent state reaches interruption decisions, passive waits stay asleep, and observation cannot add/remove frames. The hook contains no game knowledge.

A Mission now owns `WorkingLight`; Explore no longer owns or assesses a light policy. Repairs suspend the active task through `ResumeWork`, preserve its results and return destination, and wait for acknowledged motor release. Ordinary inventory transactions drain before light interruption, while passive cooking can be interrupted without inventing a motor owner. A restored continuation revalidates the saved work instead of classifying preemption as a failed target. Recipe inputs remain domain data.

Progress allowances retain the existing 80-tick / 4-block parameters. Light-restoration work has explicit recorded 2,400-tick / 32-block bounds, preventing the earlier exploration-only budget from resetting at each coal/pickup/crafting transition. Repair travel stays within the remembered route corridor; new supply work is bounded around its selected supply site. These are initial domain policy parameters, not a reliability claim for arbitrary terrain. Health deterioration, repair deadline, and inaccessible return paths remain explicit failures.

Live v77 run `20260908-133725-853628-24786` passed dim bootstrap (207 elapsed ticks / 140 exact decisions), darkness during gathering (148 / 80), and darkness during a furnace wait (276 / 211). Gathering task 3 stopped at tick 96, released at 97, and resumed at 102 after working light returned. Cooking task 3 was interrupted while Sleeping at tick 99 and resumed at 104 with deadline 495 unchanged. All harnesses OK, complete captures, zero LLM/planner calls, normal client exits and released motors. `MissionLightingAudit` verifies these boundaries through exact replay and rejects the uninterrupted bootstrap recording as a negative control.

The live gather image exposed a torch attached to the coal ore being mined. v78 excludes supports needed by suspended harvesting/clearance from light placement. A second failing regression showed that resetting mission route memory at every bright observation lost the supply exit; v78 retains a loop-erased observed route across lit waypoints. It still revalidates terrain before movement. Both regressions fail on v77 and pass on v78. The targeted v78 live run includes the long cave resupply scenario; completion is pending at this checkpoint.

## v78 maintained-light regression results

Run `20260908-134706-409513-27450` completed with four inventory successes: dim bootstrap 185 elapsed ticks / 119 exact decisions; gathering 146 / 75; furnace wait 284 / 213; long cave resupply 1,846 / 1,777. All captures complete, harnesses OK, zero planner/LLM calls, normal client exits and released motors. The health-injection scenario deliberately FAILED inventory acquisition in 80 ticks / 8 exact decisions. Health loss observed at tick 105 stopped the command immediately; release arrived at 106, with zero subsequent Start commands. It was already at the remembered origin, so it terminated for lack of a safe refuge instead of launching the 72 unmaintained Looks seen in v76.

The bootstrap recording carries one light-restoration episode through Explore, Gather, Pickup, and Acquire/Craft. The repaired gather places its torch on bedrock at (-1,199,15), leaving the coal ore available for harvesting. Cooking preserves deadline 514. Cave repair retains exploration task 3, walks from observed feet (0,196,70) through z54/38/22 to the supply origin at (0,200,15), then returns through z30/46/62 to z70 and resumes the same task at tick 1763. These sequences are checked by exact-replay audits with negative controls. See [structured evidence](evaluations/system-one-v78-mission-lighting.json).

This verifies the selected cross-task and cave-repair cases, not all survival policies, hidden-read auditing, current-policy iron reliability or release. The original scenario and the frozen development set are the next integrated check. No held-out terrain has been generated or inspected.
