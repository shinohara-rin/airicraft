# Actionsets

Actionsets are declarative methods for achieving typed facts.

- `builtin/`: repo-owned actionsets.
- `operator/`: local operator-authored actionsets.
- `enabled/`: validated actionsets promoted for resolver use.
- `planner_drafts/`: planner-authored drafts. These are validated for diagnostics but never loaded into the resolver index.
